# 🎯 Следующие шаги для полного запуска ЛИС МД

## Текущий статус: ✅ Базовая инфраструктура готова

- ✅ Веб-приложение: http://185.247.185.145/
- ✅ OpenVPN: Подключен к офисной сети
- ✅ NAS: Смонтирован и доступен
- ✅ Watcher: Следит за новыми файлами

---

## 📋 Что нужно сделать дальше

### 1. ⚠️ КРИТИЧНО: Настроить формат файлов

**Проблема:** На NAS файлы названы по ФИО, а система работает с номерами.

**Решение:**
- Попросите лабораторию сохранять файлы в формате: `НОМЕР_ИССЛЕДОВАНИЯ.pdf`
- Папка для сохранения: `\\192.168.100.177\laba\lab_results\`

**Примеры правильных имен:**
```
12345.pdf
МИД-001234.pdf
ЛАБ-12345.pdf
```

**Подробнее:** `/opt/lis-md/WORKFLOW.md`

---

### 2. 🔗 Интеграция с 1С

**Что нужно сделать в 1С:**

#### Шаг 1: Создать HTTP-сервис в 1С

В конфигурации "1С: Управление медицинским центром" создайте HTTP-сервис:

1. Откройте конфигуратор 1С
2. Добавьте новый HTTP-сервис: "ЛабораторныеРезультаты"
3. URL корня: `/lab`
4. Создайте метод: `attachResult` (POST)

#### Шаг 2: Код метода attachResult

```1c
Функция attachResult(Запрос)
    Попытка
        // Читаем JSON из тела запроса
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(Запрос.ПолучитьТелоКакСтроку());
        Данные = ПрочитатьJSON(ЧтениеJSON);
        
        НомерИсследования = Данные["research_number"];
        PDFBase64 = Данные["pdf_base64"];
        
        // Найти заказ по номеру
        Заказ = Справочники.ЗаказыНаИсследования.НайтиПоНомеру(НомерИсследования);
        
        Если Заказ.Пустая() Тогда
            Возврат НовыйОтветJSON(404, "Заказ не найден");
        КонецЕсли;
        
        // Декодировать PDF
        ДвоичныеДанные = Base64Значение(PDFBase64);
        
        // Сохранить файл в заказ
        Вложение = Заказ.Вложения.Добавить();
        Вложение.Файл = Новый ХранилищеЗначения(ДвоичныеДанные);
        Вложение.ИмяФайла = НомерИсследования + ".pdf";
        Заказ.Записать();
        
        // Вернуть email пациента
        EmailПациента = Заказ.Пациент.Email;
        
        Ответ = Новый Структура;
        Ответ.Вставить("success", Истина);
        Ответ.Вставить("patient_email", EmailПациента);
        
        Возврат НовыйОтветJSON(200, Ответ);
        
    Исключение
        Возврат НовыйОтветJSON(500, ОписаниеОшибки());
    КонецПопытки;
КонецФункции

Функция НовыйОтветJSON(КодСостояния, Данные)
    Ответ = Новый HTTPСервисОтвет(КодСостояния);
    Ответ.Заголовки.Вставить("Content-Type", "application/json");
    
    ЗаписьJSON = Новый ЗаписьJSON;
    ЗаписьJSON.УстановитьСтроку();
    ЗаписатьJSON(ЗаписьJSON, Данные);
    Ответ.УстановитьТелоИзСтроки(ЗаписьJSON.Закрыть());
    
    Возврат Ответ;
КонецФункции
```

#### Шаг 3: Опубликовать HTTP-сервис

1. Опубликуйте конфигурацию на сервере 1С
2. В веб-сервере (IIS/Apache) настройте публикацию
3. URL будет: `http://192.168.100.234/ваша_база/hs/lab/attachResult`

#### Шаг 4: Создать токен авторизации

В 1С создайте пользователя для API или используйте базовую авторизацию.

**Токен** = `имя_пользователя:пароль` в Base64

Например:
```bash
echo -n "api_user:api_password" | base64
# Результат: YXBpX3VzZXI6YXBpX3Bhc3N3b3Jk
```

#### Шаг 5: Обновить конфигурацию ЛИС МД

Отредактируйте `/opt/lis-md/.env`:

```bash
API_1C_URL=http://192.168.100.234/ваша_база/hs/lab/attachResult
API_1C_TOKEN=YXBpX3VzZXI6YXBpX3Bhc3N3b3Jk
```

Перезапустите приложение:
```bash
cd /opt/lis-md && docker compose restart app
```

**Подробнее:** `/opt/lis-md/docs/1c-integration.md`

---

### 3. 📧 Настройка Email уведомлений

#### Вариант 1: Gmail

Отредактируйте `/opt/lis-md/.env`:

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш_email@gmail.com
SMTP_PASSWORD=ваш_пароль_приложения
SMTP_FROM=noreply@it-mydoc.ru
```

**Важно:** Для Gmail используйте "пароль приложения", а не основной пароль.

#### Вариант 2: Yandex

```bash
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_USER=ваш_email@yandex.ru
SMTP_PASSWORD=ваш_пароль
SMTP_FROM=noreply@it-mydoc.ru
```

#### Вариант 3: Mail.ru

```bash
SMTP_HOST=smtp.mail.ru
SMTP_PORT=587
SMTP_USER=ваш_email@mail.ru
SMTP_PASSWORD=ваш_пароль
SMTP_FROM=noreply@it-mydoc.ru
```

#### Применить изменения

```bash
cd /opt/lis-md && docker compose restart app
```

**Подробнее:** `/opt/lis-md/docs/email-setup.md`

---

### 4. 🔒 DNS и SSL (опционально, но рекомендуется)

#### Шаг 1: Настроить DNS

В панели управления доменом создайте A-запись:

```
Тип:  A
Имя:  lis.it-mydoc.ru
Значение: 185.247.185.145
TTL:  3600
```

Проверка (через 5-10 минут после создания):
```bash
nslookup lis.it-mydoc.ru
```

#### Шаг 2: Получить SSL сертификат

После того как DNS заработает:

```bash
# Остановить Nginx
systemctl stop nginx

# Получить сертификат
certbot certonly --standalone -d lis.it-mydoc.ru

# Запустить Nginx
systemctl start nginx

# Перезапустить контейнеры
cd /opt/lis-md && docker compose restart nginx
```

#### Шаг 3: Включить HTTPS в Nginx

Раскомментируйте секцию SSL в `/opt/lis-md/nginx/conf.d/lis-md.conf`:

```nginx
server {
    listen 443 ssl http2;
    server_name lis.it-mydoc.ru;

    ssl_certificate /etc/letsencrypt/live/lis.it-mydoc.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/lis.it-mydoc.ru/privkey.pem;
    
    # ... остальная конфигурация
}
```

Перезапустить:
```bash
cd /opt/lis-md && docker compose restart nginx
```

---

## 🧪 Тестирование полного цикла

После настройки всех интеграций:

### 1. Создать тестовый файл

Положите тестовый PDF в папку на NAS:
```bash
# С сервера
cp test.pdf /mnt/nas/lab_results/TEST-12345.pdf

# Или из Windows
# Скопируйте файл в \\192.168.100.177\laba\lab_results\TEST-12345.pdf
```

### 2. Следить за логами

```bash
docker logs -f lis-md-app
```

Вы должны увидеть:
```
[Watcher] New file detected: TEST-12345.pdf
[Watcher] Research number: TEST-12345
[Integrator] Sending to 1C...
[Integrator] Response from 1C: success
[Mailer] Sending email to patient@example.com
[Mailer] Email sent successfully
[Watcher] File moved to archive
```

### 3. Проверить веб-интерфейс

Откройте http://185.247.185.145/dashboard

- Статистика должна обновиться
- В разделе "Записи" должна появиться новая запись
- Статус должен быть "completed"

### 4. Проверить архив

```bash
ls -la /mnt/nas/archive/
# Должен появиться файл TEST-12345.pdf
```

---

## 📊 Приоритеты

1. **ВЫСОКИЙ:** Формат файлов (без этого система не работает)
2. **ВЫСОКИЙ:** Интеграция с 1С (основная функция)
3. **СРЕДНИЙ:** Email уведомления (можно отложить)
4. **НИЗКИЙ:** DNS и SSL (для удобства и безопасности)

---

## ❓ Вопросы?

- Документация: `/opt/lis-md/README.md`
- Рабочий процесс: `/opt/lis-md/WORKFLOW.md`
- Логи: `docker logs -f lis-md-app`
- Статус: http://185.247.185.145/dashboard
