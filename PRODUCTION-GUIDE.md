# 🚀 ИНСТРУКЦИЯ ПО РАБОТЕ С РЕАЛЬНЫМИ ФАЙЛАМИ

## ✅ ЧТО УЖЕ РАБОТАЕТ

Система полностью протестирована и готова к работе с реальными файлами:

1. ✅ Парсинг PDF
2. ✅ Интеграция с 1С
3. ✅ Автоматическая обработка

---

## 🔄 АВТОМАТИЧЕСКИЙ ПОТОК ОБРАБОТКИ

### Схема работы:

```
1. PDF попадает на NAS
   ↓
2. Watcher обнаруживает новый файл (каждые 30 сек)
   ↓
3. Создается запись в базе данных
   ↓
4. Integrator берет файл в обработку
   ↓
5. PDF парсится (извлекаются данные)
   ↓
6. Данные отправляются в 1С
   ↓
7. Файл архивируется
   ↓
8. Готово! ✅
```

---

## 📁 КУДА КЛАСТЬ ФАЙЛЫ

### Вариант 1: Через NAS (РЕКОМЕНДУЕТСЯ)

**Путь:** `\\192.168.100.177\laba\lab_results\`

1. Положите PDF в эту папку
2. Система автоматически обнаружит файл
3. Обработка начнется автоматически

### Вариант 2: Напрямую на сервер

**Путь на сервере:** `/mnt/nas/lab_results/`

```bash
# С любого компьютера в сети:
scp analysis.pdf root@185.247.185.145:/mnt/nas/lab_results/
```

---

## 📊 МОНИТОРИНГ ПРОЦЕССА

### Веб-интерфейс

**URL:** https://lis.it-mydoc.ru/

- **Логин:** admin
- **Пароль:** admin

**Страницы:**
- **Dashboard** - статистика обработки
- **Записи** - все обработанные файлы
- **Журнал** - подробные логи

### Статусы файлов

- **pending** - ожидает обработки
- **processing** - в процессе
- **completed** - успешно обработан и отправлен в 1С
- **failed** - ошибка (попадает в карантин)

---

## 📋 ФОРМАТ ИМЕНИ ФАЙЛА

### Рекомендуется:

```
<ФИО_пациента>.pdf
или
<Номер_анализа>.pdf
```

**Примеры:**
- `Иванов_ИИ.pdf`
- `12345.pdf`
- `Климова_СА_17112025.pdf`

Система обработает любой PDF с результатами анализов.

---

## 🔍 ПРОВЕРКА РАБОТЫ

### Шаг 1: Положить тестовый файл

```bash
# На NAS
\\192.168.100.177\laba\lab_results\test.pdf
```

### Шаг 2: Проверить логи

```bash
# На сервере
ssh root@185.247.185.145
cd /opt/lis-md
docker compose logs app -f
```

**Ожидаемый вывод:**
```
[Watcher] New file: test.pdf
[Integrator] Processing test.pdf...
[Integrator] ✓ Parsed PDF: Иванов И.И.
[Integrator] ✓ Sent to 1C: test.pdf
```

### Шаг 3: Проверить в веб-интерфейсе

1. Открыть https://lis.it-mydoc.ru/
2. Войти (admin/admin)
3. Перейти в "Записи"
4. Найти файл `test.pdf` со статусом **completed**

---

## ⚙️ НАСТРОЙКИ

### Изменить параметры обработки

**Файл:** `/opt/lis-md/.env`

```bash
# Интервал проверки новых файлов (секунды)
WATCHER_INTERVAL=30

# URL 1С
C1_BASE_URL=http://192.168.100.234/BITtest/hs/lab

# Логин/пароль для 1С
C1_USERNAME=Администратор
C1_PASSWORD=1234

# Количество попыток при ошибке
API_1C_RETRY_COUNT=3

# Задержка между попытками (секунды)
API_1C_RETRY_DELAY=5
```

После изменения перезапустить:
```bash
cd /opt/lis-md
docker compose restart app
```

---

## 🗂️ АРХИВАЦИЯ И КАРАНТИН

### Архив

**Путь:** `/mnt/nas/archive/`

- Успешно обработанные файлы автоматически перемещаются сюда
- Сортировка по датам: `YYYY-MM-DD/`

### Карантин

**Путь:** `/mnt/nas/quarantine/`

- Файлы с ошибками попадают сюда
- Можно исправить и переместить обратно в `lab_results/`

---

## 🐛 TROUBLESHOOTING

### Файл не обрабатывается

1. **Проверить что файл в правильной папке:**
   ```bash
   ls /mnt/nas/lab_results/
   ```

2. **Проверить логи:**
   ```bash
   docker compose logs app --tail=50
   ```

3. **Проверить статус в веб-интерфейсе:**
   - Войти на https://lis.it-mydoc.ru/
   - Перейти в "Записи"
   - Найти файл и посмотреть статус/ошибку

### Файл в статусе "failed"

1. **Посмотреть причину ошибки:**
   - В веб-интерфейсе → "Записи" → найти файл
   - Или в "Журнал" → найти события для этого файла

2. **Типичные ошибки:**
   - **"Ошибка парсинга PDF"** - файл поврежден или не текстовый
   - **"Connection refused"** - нет связи с 1С (проверить VPN)
   - **"404 Not Found"** - HTTP-сервис в 1С не работает

3. **Повторная обработка:**
   - Через веб-интерфейс: нажать кнопку "Retry"
   - Или переместить файл из карантина обратно в `lab_results/`

### 1С не получает данные

1. **Проверить доступность 1С:**
   ```bash
   curl http://192.168.100.234/BITtest/hs/lab/test
   ```

2. **Проверить OpenVPN:**
   ```bash
   ping 192.168.100.234
   ```

3. **Проверить в логах 1С:**
   - Открыть журнал регистрации 1С
   - Искать ошибки HTTP-сервиса

---

## 📧 EMAIL УВЕДОМЛЕНИЯ (опционально)

Если настроить SMTP, система может автоматически отправлять результаты пациентам.

**Файл:** `/opt/lis-md/.env`

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@it-mydoc.ru
```

---

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ

### Проверить состояние системы

```bash
cd /opt/lis-md
docker compose ps
docker compose logs app --tail=100
```

### Перезапустить систему

```bash
cd /opt/lis-md
docker compose restart
```

### Очистить базу данных (ВНИМАНИЕ!)

```bash
cd /opt/lis-md
rm -f data/lis.db
docker compose restart app
```

### Посмотреть статистику

```bash
# Общая статистика
curl -s https://lis.it-mydoc.ru/api/stats -H "Authorization: Bearer YOUR_TOKEN" | jq

# Последние записи
curl -s https://lis.it-mydoc.ru/api/records -H "Authorization: Bearer YOUR_TOKEN" | jq
```

---

## 🎯 ЧЕКЛИСТ ДЛЯ ПРОДАКШН

- [ ] Система развернута и работает
- [ ] OpenVPN подключен (пингуется 192.168.100.234)
- [ ] NAS примонтирован
- [ ] HTTP-сервис 1С доступен
- [ ] Протестирован 1 файл через NAS
- [ ] Проверены логи (нет ошибок)
- [ ] Проверен веб-интерфейс
- [ ] Настроены бэкапы базы данных
- [ ] Документирован процесс для команды

---

## 📞 КОНТАКТЫ

**Сервер:** 185.247.185.145  
**Веб:** https://lis.it-mydoc.ru/  
**GitHub:** https://github.com/gr33njj/lis-mini  

---

## 🎉 ГОТОВО К РАБОТЕ!

Система полностью автоматическая. Просто кладите PDF файлы в папку на NAS, и они будут обработаны автоматически!

