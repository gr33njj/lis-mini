# 📋 СОЗДАНИЕ ПРИЕМА В 1С АВТОМАТИЧЕСКИ

## 🎯 Задача
Автоматически создавать документ "Прием" с:
- **Врач**: МД Lab
- **Клиент**: ФИО из названия PDF
- **Шаблон**: "МД Биохимический анализ крови" (заполненный)

---

## 📝 Шаг 1: Добавить новый метод в HTTP-сервис

Открой HTTP-сервис `LabIntegration` и добавь новый метод:

### Свойства метода `createAppointment`:
- **Имя**: `createAppointment`
- **Шаблон URL**: `createAppointment`
- **HTTP метод**: `POST`
- **Аутентификация**: `Basic` (снята)

### Код метода `createAppointmentPOST`:

```1c
Функция createAppointmentPOST(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    
    Попытка
        // 1. Читаем JSON
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(Запрос.ПолучитьТелоКакСтроку());
        ДанныеJSON = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // 2. Извлекаем данные
        ИмяПациента = ДанныеJSON["patient_name"];
        Возраст = ДанныеJSON["age"];
        Пол = ДанныеJSON["gender"];
        ДатаЗабора = ДанныеJSON["sample_date"];
        ДатаРезультата = ДанныеJSON["result_date"];
        РезультатыАнализов = ДанныеJSON["test_results"];
        
        // 3. Ищем или создаём пациента (Клиента)
        Клиент = НайтиИлиСоздатьКлиента(ИмяПациента, Возраст, Пол);
        
        // 4. Ищем врача "МД Lab"
        Врач = НайтиВрача("МД Lab");
        Если Врач = Неопределено Тогда
            Врач = НайтиВрача("Lab"); // Попытка без префикса
        КонецЕсли;
        
        // 5. Создаём документ "Прием"
        Прием = Документы.Прием.СоздатьДокумент();
        Прием.Дата = ТекущаяДата();
        Прием.Клиент = Клиент;
        Прием.Врач = Врач;
        
        // 6. Заполняем HTML шаблон
        ШаблонHTML = ПолучитьШаблон("МД Биохимический анализ крови");
        Если ШаблонHTML = Неопределено Тогда
            ВызватьИсключение("Шаблон 'МД Биохимический анализ крови' не найден");
        КонецЕсли;
        
        // Заполняем мета-поля
        ЗаполненныйHTML = ШаблонHTML;
        ЗаполненныйHTML = СтрЗаменить(ЗаполненныйHTML, 
            "id=""f7bece55-cafa-11e5-9bc7-50af732359f4"" href=""about:"">[...]", 
            "id=""f7bece55-cafa-11e5-9bc7-50af732359f4"" href=""about:"">" + ИмяПациента);
        
        ЗаполненныйHTML = СтрЗаменить(ЗаполненныйHTML, 
            "id=""4d22b3c0-03bd-11e3-943d-1c6f653fefc3"" href=""about:"">[...]", 
            "id=""4d22b3c0-03bd-11e3-943d-1c6f653fefc3"" href=""about:"">" + Возраст);
        
        ЗаполненныйHTML = СтрЗаменить(ЗаполненныйHTML, 
            "id=""94c2e827-1449-11f0-a70e-d066716536da"" href=""about:"">[...]", 
            "id=""94c2e827-1449-11f0-a70e-d066716536da"" href=""about:"">" + Пол);
        
        ЗаполненныйHTML = СтрЗаменить(ЗаполненныйHTML, 
            "id=""edf1c744-22a4-11e2-87b5-002618dcef2c"" href=""about:"">[...]", 
            "id=""edf1c744-22a4-11e2-87b5-002618dcef2c"" href=""about:"">" + ДатаЗабора);
        
        ЗаполненныйHTML = СтрЗаменить(ЗаполненныйHTML, 
            "id=""8beddfdf-d17f-11e1-b361-1803736d59cd"" href=""about:"">[...]", 
            "id=""8beddfdf-d17f-11e1-b361-1803736d59cd"" href=""about:"">" + ДатаРезультата);
        
        // Заполняем результаты анализов
        Для Каждого КлючЗначение Из РезультатыАнализов Цикл
            ИдПоля = КлючЗначение.Ключ;
            Значение = КлючЗначение.Значение;
            
            ЗаполненныйHTML = СтрЗаменить(ЗаполненныйHTML, 
                "id=""" + ИдПоля + """ href=""about:"">[...]", 
                "id=""" + ИдПоля + """ href=""about:"">" + Значение);
        КонецЦикла;
        
        // 7. Сохраняем шаблон в прием
        Прием.РезультатыАнализов = ЗаполненныйHTML; // Или соответствующее поле
        
        // 8. Записываем документ
        Прием.Записать(РежимЗаписиДокумента.Проведение);
        
        // 9. Возвращаем успех
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("status", "ok");
        РезультатJSON.Вставить("appointment_ref", Строка(Прием.Ссылка));
        РезультатJSON.Вставить("client_name", ИмяПациента);
        РезультатJSON.Вставить("doctor", ?(Врач = Неопределено, "Не найден", Строка(Врач)));
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
        
    Исключение
        Ответ.КодСостояния = 500;
        
        ОшибкаJSON = Новый Структура;
        ОшибкаJSON.Вставить("status", "error");
        ОшибкаJSON.Вставить("error", ОписаниеОшибки());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, ОшибкаJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции


// ═══════════════════════════════════════════════════════════════
// ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
// ═══════════════════════════════════════════════════════════════

Функция НайтиИлиСоздатьКлиента(ИмяПолное, Возраст, Пол)
    
    // Ищем существующего клиента по ФИО
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Клиенты.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Клиенты КАК Клиенты
    |ГДЕ
    |    Клиенты.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", ИмяПолное);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        // Создаём нового клиента
        НовыйКлиент = Справочники.Клиенты.СоздатьЭлемент();
        НовыйКлиент.Наименование = ИмяПолное;
        // Дополнительные поля (если есть в вашей конфигурации):
        // НовыйКлиент.Возраст = Возраст;
        // НовыйКлиент.Пол = Пол;
        НовыйКлиент.Записать();
        Возврат НовыйКлиент.Ссылка;
    КонецЕсли;
    
КонецФункции


Функция НайтиВрача(ИмяВрача)
    
    // Ищем врача в справочнике Врачи или Сотрудники
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Врачи.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Врачи КАК Врачи
    |ГДЕ
    |    Врачи.Наименование ПОДОБНО &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", "%" + ИмяВрача + "%");
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция ПолучитьШаблон(НаименованиеШаблона)
    
    // Ищем шаблон в справочнике или хранилище
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Шаблоны.ТекстHTML КАК ТекстHTML
    |ИЗ
    |    Справочник.ШаблоныАнализов КАК Шаблоны
    |ГДЕ
    |    Шаблоны.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", НаименованиеШаблона);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.ТекстHTML;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции
```

---

## ⚙️ Шаг 2: Адаптация под вашу конфигурацию

Этот код — **шаблон**! Тебе нужно адаптировать его под свою конфигурацию 1С:

### 🔧 Что нужно изменить:

1. **Имя документа "Прием"**:
   ```1c
   Прием = Документы.Прием.СоздатьДокумент();
   ```
   Замени `Документы.Прием` на правильное имя документа в твоей конфигурации.

2. **Справочник клиентов**:
   ```1c
   Справочник.Клиенты
   ```
   Замени на правильное имя справочника пациентов.

3. **Справочник врачей**:
   ```1c
   Справочник.Врачи
   ```
   Замени на правильное имя справочника врачей/сотрудников.

4. **Справочник шаблонов**:
   ```1c
   Справочник.ШаблоныАнализов
   ```
   Замени на то, где хранятся HTML шаблоны в твоей системе.

5. **Поле для сохранения HTML**:
   ```1c
   Прием.РезультатыАнализов = ЗаполненныйHTML;
   ```
   Укажи правильное поле документа "Прием", где должен храниться заполненный HTML.

---

## 📝 Шаг 3: Создай врача "МД Lab"

В справочнике врачей/сотрудников создай запись:
- **Наименование**: `МД Lab`
- Или просто: `Lab`

---

## 📝 Шаг 4: Сохрани шаблон HTML

Создай элемент в справочнике шаблонов:
- **Наименование**: `МД Биохимический анализ крови`
- **ТекстHTML**: (вставь HTML код шаблона, который ты мне прислал)

---

## 🧪 Шаг 5: Тестирование

После настройки можно протестировать через curl:

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/createAppointment \
  -u "Администратор:1234" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Иванов Иван Иванович",
    "age": "45",
    "gender": "Мужской",
    "sample_date": "15.11.2025",
    "result_date": "17.11.2025",
    "test_results": {
      "ae584a2f-957f-11f0-a7be-eca0f0014d7d": "25.5",
      "d284601d-957f-11f0-a7be-eca0f0014d7d": "30.2",
      "e5e808bc-957f-11f0-a7be-eca0f0014d7d": "4.5"
    }
  }'
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "appointment_ref": "Документ.Прием.000000001",
  "client_name": "Иванов Иван Иванович",
  "doctor": "МД Lab"
}
```

---

## 🚨 Возможные проблемы:

1. **"Шаблон не найден"** → Проверь имя шаблона и справочник
2. **"Врач не найден"** → Создай врача "МД Lab" в справочнике
3. **Ошибка записи документа** → Проверь обязательные поля документа "Прием"
4. **Права доступа** → Убедись что пользователь "Администратор" может создавать документы

---

## 📞 Что дальше?

После того как настроишь метод в 1С и протестируешь его через curl, я обновлю Python интегратор, чтобы он вызывал `createAppointment` вместо `fillTemplate`.

