# 🔧 ИСПРАВЛЕНИЕ: НЕ СОЗДАВАТЬ КЛИЕНТА АВТОМАТИЧЕСКИ

## 🎯 Проблема

Текущий код автоматически создаёт клиента, если не находит по ФИО:
- ❌ Возможны дубли из-за опечаток
- ❌ Клиент должен быть в базе заранее
- ❌ Поиск только по ФИО ненадёжен

## ✅ Решение

### Вариант 1: Поиск по ФИО + Дата рождения (РЕКОМЕНДУЕТСЯ)
- Уникальная комбинация
- Не создаём дубли
- Если не нашли - возвращаем ошибку

### Вариант 2: Поиск по коду/номеру мед карты
- Самый надёжный способ
- НО: нужен идентификатор В PDF

---

## 📝 КОД ДЛЯ 1С (ВАРИАНТ 1: ФИО ONLY)

### Замени функцию НайтиИлиСоздатьПациента на НайтиПациента:

```1c
Функция НайтиПациента(ИмяПолное, Возраст, Пол)
    
    // ❗ ТОЛЬКО ПОИСК, БЕЗ СОЗДАНИЯ!
    // Если пациент не найден → возвращаем Неопределено → ошибка
    
    // Ищем существующего пациента по ФИО
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Клиенты.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Клиенты КАК Клиенты
    |ГДЕ
    |    Клиенты.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", ИмяПолное);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        // ❗ НЕ создаём, возвращаем Неопределено!
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции
```

### В основной функции createAppointmentPOST:

```1c
Функция createAppointmentPOST(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    
    Попытка
        // ... (читаем JSON)
        
        // 3. Ищем пациента (БЕЗ СОЗДАНИЯ!)
        Пациент = НайтиПациента(ИмяПациента, Возраст, Пол);
        
        // ❗ ПРОВЕРКА: Если пациент НЕ найден → ошибка!
        Если Пациент = Неопределено Тогда
            ВызватьИсключение "Пациент """ + ИмяПациента + """ не найден в базе! Проверьте ФИО или создайте клиента вручную.";
        КонецЕсли;
        
        // Продолжаем только если пациент найден
        // ... (остальной код)
        
    Исключение
        Ответ.КодСостояния = 400;  // 400 Bad Request
        
        ОшибкаJSON = Новый Структура;
        ОшибкаJSON.Вставить("status", "error");
        ОшибкаJSON.Вставить("error", ОписаниеОшибки());
        ОшибкаJSON.Вставить("patient_name", ИмяПациента);
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, ОшибкаJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции
```

---

## 📝 КОД ДЛЯ 1С (ВАРИАНТ 2: ФИО + ДАТА РОЖДЕНИЯ)

### Если в PDF есть дата рождения:

```1c
Функция НайтиПациентаПоФИОИДате(ИмяПолное, ДатаРождения)
    
    // Поиск по ФИО + Дата рождения (уникальная комбинация!)
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Клиенты.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Клиенты КАК Клиенты
    |ГДЕ
    |    Клиенты.Наименование = &Наименование
    |    И Клиенты.ДатаРождения = &ДатаРождения";
    
    Запрос.УстановитьПараметр("Наименование", ИмяПолное);
    Запрос.УстановитьПараметр("ДатаРождения", ДатаРождения);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции
```

### В createAppointmentPOST:

```1c
// Извлекаем дату рождения из JSON
ДатаРождения = ДанныеJSON["birth_date"];  // "21.05.1982"

// Конвертируем строку в дату
ДатаРожденияДата = Неопределено;
Попытка
    ДатаРожденияДата = Дата(СтрЗаменить(ДатаРождения, ".", ""));
Исключение
    // Неверный формат даты
КонецПопытки;

// Ищем по ФИО + дата рождения
Пациент = НайтиПациентаПоФИОИДате(ИмяПациента, ДатаРожденияДата);
```

---

## 📝 КОД ДЛЯ 1С (ВАРИАНТ 3: ПО КОДУ КЛИЕНТА)

### Если в PDF есть код клиента:

```1c
Функция НайтиПациентаПоКоду(КодКлиента)
    
    // Самый надёжный поиск - по уникальному коду
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Клиенты.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Клиенты КАК Клиенты
    |ГДЕ
    |    Клиенты.Код = &Код";
    
    Запрос.УстановитьПараметр("Код", КодКлиента);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Клиент существует

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/createAppointment \
  -u "Администратор:1234" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Иванов Иван Иванович",
    "age": "35",
    "gender": "Мужской",
    "sample_date": "20.11.2025",
    "result_date": "20.11.2025",
    "test_results": [...]
  }'
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "appointment_ref": "Документ.Прием.000000001",
  "patient_name": "Иванов Иван Иванович"
}
```

---

### Тест 2: Клиент НЕ существует

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/createAppointment \
  -u "Администратор:1234" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Несуществующий Человек",
    "age": "99",
    "gender": "Мужской",
    "sample_date": "20.11.2025",
    "result_date": "20.11.2025",
    "test_results": [...]
  }'
```

**Ожидаемый результат:**
```json
{
  "status": "error",
  "error": "Пациент \"Несуществующий Человек\" не найден в базе! Проверьте ФИО или создайте клиента вручную.",
  "patient_name": "Несуществующий Человек"
}
```

---

## 🔧 ЧТО НУЖНО ВЫЯСНИТЬ

### 1️⃣ Структура Справочник.Клиенты:

Открой справочник и пришли список полей:
- Наименование (ФИО) ✅
- ДатаРождения?
- Код?
- НомерМедКарты?
- Другие поля?

### 2️⃣ Структура PDF:

Пришли пример PDF или скриншот, чтобы я мог:
- Увидеть, где находится ФИО
- Проверить, есть ли дата рождения
- Проверить, есть ли код клиента/номер карты

### 3️⃣ Идентификатор клиента:

Что использовать для поиска:
- ФИО (не надёжно)
- ФИО + дата рождения (лучше)
- Код клиента (идеально, но нужен в PDF)
- Номер мед карты (идеально, но нужен в PDF)

---

## 📋 СЛЕДУЮЩИЕ ШАГИ

1. Пришли информацию о структуре Справочник.Клиенты
2. Пришли пример PDF (или скриншот)
3. Я реализую правильный поиск клиента
4. Обновлю парсер PDF для извлечения нужных данных
5. Протестируем!

---

## 💡 БОНУС: Улучшение парсинга ФИО

Сейчас парсер извлекает ФИО неправильно.

**Вариант 1: Брать из имени файла**
```python
# В pdf_parser.py:
import os

file_name = os.path.basename(self.pdf_path)  # "Глухончук_ДИ.pdf"
name_from_file = file_name.replace('.pdf', '').replace('_', ' ')  # "Глухончук ДИ"

# Использовать как ФИО, если парсинг из PDF не удался
if not self.results.get('patient_name'):
    self.results['patient_name'] = name_from_file
```

**Вариант 2: Улучшить парсинг из PDF**
Нужно видеть структуру PDF, чтобы понять, где находится ФИО.

---

Жду твою информацию! 🚀

