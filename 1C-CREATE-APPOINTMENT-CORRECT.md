# 📋 СОЗДАНИЕ ПРИЕМА В 1С (ПРАВИЛЬНАЯ ВЕРСИЯ)

> ✅ **На основе реального ТЗ от разработчика конфигурации**
> - Параметры хранятся ТОЛЬКО в регистре `ЗначенияПараметровHTML`
> - НЕТ ТаблицыПараметров в документе (это клиентская механика)
> - Поиск параметров по GUID через `УникальныйИдентификатор()`
> - Прямая запись в регистр без использования форм

---

## 🎯 Ключевое понимание архитектуры

### ❌ ЧТО НЕ РАБОТАЕТ (старый подход):
- ~~`ТаблицаПараметров`~~ - это реквизит ФОРМЫ, не документа
- ~~`ДополнительныеСвойства["ПараметрыПриема"]`~~ - работает только через форму
- ~~`РаботаСШаблонамиHTML.ЗаписатьЗначенияПараметровДокумента()`~~ - не использует переданные параметры

### ✅ ЧТО РАБОТАЕТ (правильный подход):
- **Прямая запись в регистр**: `РегистрыСведений.ЗначенияПараметровHTML`
- **Поиск по GUID**: через `УникальныйИдентификатор()` элемента ПВХ
- **Печатная форма читает**: ТОЛЬКО из регистра `ЗначенияПараметровHTML`

---

## 📐 Архитектура хранения параметров

### 1. План Видов Характеристик: `ПараметрыHTML`
Каждый параметр (АЛАТ, АСАТ и т.д.) - это элемент ПВХ со своим GUID:
```1c
// Пример элемента:
Параметр.Наименование = "АЛАТ"
Параметр.Ссылка.УникальныйИдентификатор() = "ae584a2f-957f-11f0-a7be-eca0f0014d7d"
```

### 2. HTML шаблон использует GUID в атрибуте `id`:
```html
<p>АЛАТ: <a id="ae584a2f-957f-11f0-a7be-eca0f0014d7d">???</a> ед/л</p>
```

### 3. Регистр сведений: `ЗначенияПараметровHTML`
Структура записи:
- **Документ** - ссылка на документ Прием
- **ИндексШаблона** - номер шаблона в табличной части (обычно 0)
- **Параметр** - ссылка на элемент ПВХ ПараметрыHTML
- **Значение** - значение параметра (число, строка)

### 4. Печатная форма
При формировании печатной формы:
1. Вызывается `ДокументыПриемаСервер.АктуализироватьТекстОсмотраШаблоновПриема`
2. Вызывается `РаботаСШаблонамиHTMLКлиентСервер.СформироватьВерсиюДляПечатиШаблонаОсмотра`
3. **Читает значения ТОЛЬКО из регистра `ЗначенияПараметровHTML`**
4. Заменяет `<a id="guid">???</a>` на `<a id="guid">25.5</a>`

---

## 📝 Шаг 1: Добавить новый метод в HTTP-сервис

### Код метода `createAppointmentPOST`:

```1c
Функция createAppointmentPOST(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    
    Попытка
        // 1. Читаем JSON
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(Запрос.ПолучитьТелоКакСтроку());
        ДанныеJSON = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // 2. Извлекаем данные
        ИмяПациента = ДанныеJSON["patient_name"];
        Возраст = ДанныеJSON["age"];
        Пол = ДанныеJSON["gender"];
        ДатаЗабора = ДанныеJSON["sample_date"];
        ДатаРезультата = ДанныеJSON["result_date"];
        РезультатыАнализов = ДанныеJSON["test_results"];
        
        // 3. Ищем или создаём пациента
        Пациент = НайтиИлиСоздатьПациента(ИмяПациента, Возраст, Пол);
        
        // 4. Ищем врача "МД Lab"
        Врач = НайтиВрача("МД Lab");
        Если Врач = Неопределено Тогда
            Врач = НайтиВрача("Lab");
        КонецЕсли;
        
        // 5. Ищем шаблон
        Шаблон = НайтиШаблон("МД Биохимический анализ крови");
        
        // 6. Создаём документ "Прием"
        Прием = Документы.Прием.СоздатьДокумент();
        Прием.Дата = ТекущаяДата();
        Прием.Клиент = Пациент;
        Прием.Врач = Врач;
        
        // 7. Добавляем шаблон в табличную часть
        Если Шаблон <> Неопределено Тогда
            НоваяСтрокаШаблона = Прием.ШаблоныПриема.Добавить();
            НоваяСтрокаШаблона.ШаблонПриема = Шаблон;
        КонецЕсли;
        
        // 8. Записываем документ
        Прием.Записать(РежимЗаписиДокумента.Проведение);
        
        // 9. 🔑 КЛЮЧЕВОЙ МОМЕНТ: Заполняем регистр ЗначенияПараметровHTML напрямую!
        КоличествоЗаполненных = ЗаполнитьРегистрПараметров(Прием.Ссылка, РезультатыАнализов);
        
        // 10. Возвращаем успех
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("status", "ok");
        РезультатJSON.Вставить("appointment_ref", Строка(Прием.Ссылка));
        РезультатJSON.Вставить("appointment_number", Прием.Номер);
        РезультатJSON.Вставить("patient_name", ИмяПациента);
        РезультатJSON.Вставить("doctor", ?(Врач = Неопределено, "Не найден", Строка(Врач)));
        РезультатJSON.Вставить("template", ?(Шаблон = Неопределено, "Не найден", Строка(Шаблон)));
        РезультатJSON.Вставить("templates_count", Прием.ШаблоныПриема.Количество());
        РезультатJSON.Вставить("parameters_filled", КоличествоЗаполненных);
        РезультатJSON.Вставить("parameters_received", РезультатыАнализов.Количество());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
        
    Исключение
        Ответ.КодСостояния = 500;
        
        ОшибкаJSON = Новый Структура;
        ОшибкаJSON.Вставить("status", "error");
        ОшибкаJSON.Вставить("error", ОписаниеОшибки());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, ОшибкаJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции


// ═══════════════════════════════════════════════════════════════
// ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
// ═══════════════════════════════════════════════════════════════

Функция НайтиИлиСоздатьПациента(ИмяПолное, Возраст, Пол)
    
    // Ищем существующего пациента по ФИО
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Клиенты.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Клиенты КАК Клиенты
    |ГДЕ
    |    Клиенты.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", ИмяПолное);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        // Создаём нового пациента
        НовыйПациент = Справочники.Клиенты.СоздатьЭлемент();
        НовыйПациент.Наименование = ИмяПолное;
        НовыйПациент.Записать();
        Возврат НовыйПациент.Ссылка;
    КонецЕсли;
    
КонецФункции


Функция НайтиВрача(ИмяВрача)
    
    // Ищем врача в справочнике Сотрудники
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Сотрудники.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Сотрудники КАК Сотрудники
    |ГДЕ
    |    Сотрудники.Наименование ПОДОБНО &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", "%" + ИмяВрача + "%");
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция НайтиШаблон(НаименованиеШаблона)
    
    // Ищем шаблон в справочнике ШаблоныHTML
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Шаблоны.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.ШаблоныHTML КАК Шаблоны
    |ГДЕ
    |    Шаблоны.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", НаименованиеШаблона);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция ЗаполнитьРегистрПараметров(СсылкаДокумента, РезультатыАнализов)
    
    // 🔑 ПРАВИЛЬНЫЙ СПОСОБ: Прямая запись в регистр ЗначенияПараметровHTML
    // РезультатыАнализов приходит как Массив объектов:
    // [{"parameter_id": "ae584a2f-957f-11f0-a7be-eca0f0014d7d", "value": "25.5"}, ...]
    
    Счетчик = 0;
    
    Попытка
        // Создаём набор записей регистра
        Набор = РегистрыСведений.ЗначенияПараметровHTML.СоздатьНаборЗаписей();
        Набор.Отбор.Документ.Установить(СсылкаДокумента);
        
        // Читаем существующие записи (если есть)
        Набор.Прочитать();
        
        // Очищаем старые записи
        Набор.Очистить();
        
        // Заполняем новыми значениями
        Для Каждого ЭлементМассива Из РезультатыАнализов Цикл
            
            GUID = ЭлементМассива["parameter_id"];
            Значение = ЭлементМассива["value"];
            
            // Находим параметр по GUID
            Параметр = НайтиПараметрПоGUID(GUID);
            
            Если Параметр <> Неопределено Тогда
                // Добавляем запись в регистр
                НоваяЗапись = Набор.Добавить();
                НоваяЗапись.Документ = СсылкаДокумента;
                НоваяЗапись.ИндексШаблона = 0;  // Первый шаблон в табличной части
                НоваяЗапись.Параметр = Параметр;
                НоваяЗапись.Значение = Значение;
                
                Счетчик = Счетчик + 1;
            КонецЕсли;
            
        КонецЦикла;
        
        // Записываем весь набор
        Набор.Записать();
        
    Исключение
        // Если что-то пошло не так - возвращаем 0
        Счетчик = 0;
    КонецПопытки;
    
    Возврат Счетчик;
    
КонецФункции


Функция НайтиПараметрПоGUID(ИскомыйGUID)
    
    // 🔑 ПРАВИЛЬНЫЙ СПОСОБ: Поиск параметра по УникальномуИдентификатору
    // В HTML шаблоне используются GUID параметров: <a id="ae584a2f-...">
    // Этот GUID совпадает с УникальныйИдентификатор() элемента ПВХ
    
    Попытка
        // Приводим к нижнему регистру для сравнения
        ИскомыйGUIDНижний = НРег(ИскомыйGUID);
        
        // Запрашиваем все параметры
        Запрос = Новый Запрос;
        Запрос.Текст = 
        "ВЫБРАТЬ
        |    ПараметрыHTML.Ссылка КАК Ссылка
        |ИЗ
        |    ПланВидовХарактеристик.ПараметрыHTML КАК ПараметрыHTML";
        
        Результат = Запрос.Выполнить();
        Выборка = Результат.Выбрать();
        
        // Перебираем все параметры и сравниваем GUID
        Пока Выборка.Следующий() Цикл
            // Получаем GUID элемента
            GUIDЭлемента = НРег(Строка(Выборка.Ссылка.УникальныйИдентификатор()));
            
            // Сравниваем с искомым GUID
            Если GUIDЭлемента = ИскомыйGUIDНижний Тогда
                Возврат Выборка.Ссылка;
            КонецЕсли;
        КонецЦикла;
        
    Исключение
        // Если что-то пошло не так - возвращаем Неопределено
    КонецПопытки;
    
    Возврат Неопределено;
    
КонецФункции
```

---

## 🔑 Ключевые отличия от старого подхода

### ❌ СТАРЫЙ (НЕ РАБОТАЕТ):
```1c
// Попытка использовать форменную механику
ТаблицаПараметров = Новый ТаблицаЗначений;
// ...заполнение таблицы...
Прием.ДополнительныеСвойства.Вставить("ПараметрыПриема", ...);
РаботаСШаблонамиHTML.ЗаписатьЗначенияПараметровДокумента(...);
```

**Почему не работает:**
- `ТаблицаПараметров` существует только в ФОРМЕ, не в объекте документа
- `ДополнительныеСвойства` работают только при записи через форму
- `ЗаписатьЗначенияПараметровДокумента` не использует переданные параметры в вашей конфигурации

---

### ✅ НОВЫЙ (РАБОТАЕТ):
```1c
// Прямая запись в регистр
Набор = РегистрыСведений.ЗначенияПараметровHTML.СоздатьНаборЗаписей();
Набор.Отбор.Документ.Установить(СсылкаДокумента);
Набор.Прочитать();
Набор.Очистить();

// Добавляем записи
НоваяЗапись = Набор.Добавить();
НоваяЗапись.Документ = СсылкаДокумента;
НоваяЗапись.ИндексШаблона = 0;
НоваяЗапись.Параметр = НайденныйПараметр;  // По GUID!
НоваяЗапись.Значение = "25.5";

Набор.Записать();
```

**Почему работает:**
- ✅ Пишем НАПРЯМУЮ в регистр `ЗначенияПараметровHTML`
- ✅ Печатная форма читает ИМЕННО из этого регистра
- ✅ Не зависим от форм и клиентской механики
- ✅ Поиск параметров по GUID через `УникальныйИдентификатор()`

---

## 🧪 Шаг 2: Тестирование

### Тест с реальными GUID:

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/createAppointment \
  -u "Администратор:1234" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Тестов Тест Тестович",
    "age": "35",
    "gender": "Мужской",
    "sample_date": "20.11.2025",
    "result_date": "20.11.2025",
    "test_results": [
      {"parameter_id": "ae584a2f-957f-11f0-a7be-eca0f0014d7d", "value": "25.5"},
      {"parameter_id": "d284601d-957f-11f0-a7be-eca0f0014d7d", "value": "44.1"},
      {"parameter_id": "8bf99265-9580-11f0-a7be-eca0f0014d7d", "value": "5.2"}
    ]
  }'
```

**Ожидаемый ответ:**
```json
{
  "status": "ok",
  "appointment_ref": "Документ.Прием.000000001",
  "appointment_number": "00000001",
  "patient_name": "Тестов Тест Тестович",
  "doctor": "МД Lab",
  "template": "Справочник.ШаблоныHTML.XXXXX",
  "templates_count": 1,
  "parameters_filled": 3,
  "parameters_received": 3
}
```

---

## ✅ Проверка результата

### Шаг 1: Откройте созданный документ
- Документы → Прием → Найдите созданный документ
- Проверьте, что заполнены: Клиент, Врач, Дата
- Проверьте, что в табличной части `ШаблоныПриема` есть шаблон

### Шаг 2: Проверьте регистр напрямую
```1c
// В консоли запросов:
ВЫБРАТЬ
    ЗначенияПараметровHTML.Документ,
    ЗначенияПараметровHTML.ИндексШаблона,
    ЗначенияПараметровHTML.Параметр,
    ЗначенияПараметровHTML.Значение
ИЗ
    РегистрСведений.ЗначенияПараметровHTML КАК ЗначенияПараметровHTML
ГДЕ
    ЗначенияПараметровHTML.Документ = &Документ
```

**Должны увидеть:**
```
Документ                      | ИндексШаблона | Параметр      | Значение
Прием.000000001              | 0              | АЛАТ          | 25.5
Прием.000000001              | 0              | АСАТ          | 44.1
Прием.000000001              | 0              | Глюкоза       | 5.2
```

### Шаг 3: Печатная форма
- Откройте документ
- Нажмите "Печать" → выберите шаблон
- **Параметры должны подставиться!** 🎉

```html
<!-- Было: -->
<p>АЛАТ: <a id="ae584a2f-957f-11f0-a7be-eca0f0014d7d">???</a> ед/л</p>

<!-- Стало: -->
<p>АЛАТ: <a id="ae584a2f-957f-11f0-a7be-eca0f0014d7d">25.5</a> ед/л</p>
```

---

## 🚨 Возможные проблемы и решения

### Проблема 1: "parameters_filled": 0

**Причина**: GUID из JSON не совпадает с GUID элементов ПВХ

**Решение**:
1. Откройте `ПланВидовХарактеристик.ПараметрыHTML`
2. Найдите элемент "АЛАТ"
3. В конфигураторе посмотрите его `УникальныйИдентификатор`
4. Или выполните запрос:
```1c
ВЫБРАТЬ
    ПараметрыHTML.Наименование,
    ПараметрыHTML.Ссылка.УникальныйИдентификатор() КАК GUID
ИЗ
    ПланВидовХарактеристик.ПараметрыHTML КАК ПараметрыHTML
```

### Проблема 2: Параметры не подставляются в печатную форму

**Причина**: Несовпадение GUID в HTML шаблоне и в регистре

**Решение**:
1. Откройте HTML шаблон: `Справочник.ШаблоныHTML` → "МД Биохимический анализ крови"
2. Найдите `<a id="...">` в HTML
3. Сравните эти GUID с GUID в регистре
4. Убедитесь, что они совпадают (регистр не важен)

### Проблема 3: Ошибка "Тип не определен (НаборЗаписей)"

**Причина**: Неправильное имя регистра

**Решение**: Проверьте точное имя регистра в конфигураторе:
- Метаданные → Регистры сведений → ?
- Должно быть: `ЗначенияПараметровHTML`

---

## 📋 Список GUID параметров (из ЛИС)

Используй эти GUID для тестирования (из предыдущих разборов):

| Параметр | GUID |
|----------|------|
| АЛАТ | `ae584a2f-957f-11f0-a7be-eca0f0014d7d` |
| АСАТ | `d284601d-957f-11f0-a7be-eca0f0014d7d` |
| Холестерин | `e5e808bc-957f-11f0-a7be-eca0f0014d7d` |
| гамма-ГТ | `ef9d787d-957f-11f0-a7be-eca0f0014d7d` |
| Щелочн. фосф-за | `0190f459-9580-11f0-a7be-eca0f0014d7d` |
| Триглицериды | `0bf4b4f9-9580-11f0-a7be-eca0f0014d7d` |
| ЛПВП | `19dd5baa-9580-11f0-a7be-eca0f0014d7d` |
| ЛПНП | `3b7519c0-9580-11f0-a7be-eca0f0014d7d` |
| ЛДГ | `43f2ce34-9580-11f0-a7be-eca0f0014d7d` |
| Мочевина | `4b6251cb-9580-11f0-a7be-eca0f0014d7d` |
| альфа-Амилаза | `562a56ba-9580-11f0-a7be-eca0f0014d7d` |
| Билирубин общий | `62c0a29c-9580-11f0-a7be-eca0f0014d7d` |
| Билирубин прямой | `681a2d53-9580-11f0-a7be-eca0f0014d7d` |
| Кальций | `740e37da-9580-11f0-a7be-eca0f0014d7d` |
| Креатинин | `8007eafe-9580-11f0-a7be-eca0f0014d7d` |
| Глюкоза | `8bf99265-9580-11f0-a7be-eca0f0014d7d` |
| Общий белок | `97ee2791-9580-11f0-a7be-eca0f0014d7d` |
| Железо | `9decbf26-9580-11f0-a7be-eca0f0014d7d` |
| Мочевая кислота | `bc144e0a-a67f-11f0-a7d7-d01c04d652e6` |

---

## 🎯 Итоги

### ✅ Правильный подход:
1. Создаём документ `Прием`
2. Заполняем табличную часть `ШаблоныПриема`
3. Записываем документ
4. **Напрямую заполняем регистр `ЗначенияПараметровHTML`**
5. Поиск параметров по GUID через `УникальныйИдентификатор()`

### ❌ Неправильный подход (старый):
1. ~~Попытки использовать `ТаблицаПараметров`~~ (это форменная механика)
2. ~~`ДополнительныеСвойства`~~ (работает только через форму)
3. ~~Поиск по `.Код`~~ (в HTML используются GUID)

### 🚀 Результат:
После внедрения этого кода:
- ✅ Документ создаётся автоматически
- ✅ Параметры записываются в регистр
- ✅ Печатная форма подставляет значения
- ✅ Система полностью автоматизирована!

---

## 📞 Следующие шаги

1. Скопируй ВЕСЬ код из этого файла в HTTP-сервис 1С
2. Обнови конфигурацию БД (Ctrl+F7)
3. Протестируй через curl
4. Проверь регистр напрямую
5. Открой печатную форму
6. Пришли результат! 🎉

