# 🚀 Инструкция по обновлению до версии 2.0

## 📋 Что нужно сделать

### ✅ Шаг 1: Обновить Python код (5 минут)

```bash
# Подключиться к серверу
ssh root@185.247.185.145

# Перейти в директорию проекта
cd /opt/lis-md

# Остановить систему
docker compose down

# Обновить код (если используется git)
git pull

# Или скопировать новые файлы:
# scp app/pdf_parser.py root@185.247.185.145:/opt/lis-md/app/

# Пересобрать контейнеры
docker compose build --no-cache

# Запустить систему
docker compose up -d

# Проверить логи
docker compose logs -f app
```

**Что изменилось в коде:**
- ✅ `app/pdf_parser.py` - новая функция парсинга имени файла
- ✅ Автоматический расчёт возраста из даты рождения
- ✅ Разделение мета-данных и результатов анализов

---

### ✅ Шаг 2: Обновить код в 1С (10 минут)

#### 2.1 Открыть Конфигуратор

1. Запустить 1С:Предприятие в режиме Конфигуратора
2. Найти HTTP-сервис `LabIntegration`

#### 2.2 Добавить новую функцию НайтиПациента

Откройте файл: **`docs/1c-integration-FIXED.md`**

Найдите раздел: **"Часть 2: Вспомогательные функции"**

Добавьте функцию `НайтиПациента` (после `ПроверитьТокен`):

```1c
Функция НайтиПациента(ИмяПолное, ДатаРождения)
    
    // ТОЛЬКО ПОИСК, БЕЗ СОЗДАНИЯ!
    // Поиск по уникальной комбинации: ФИО + Дата рождения
    
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Клиенты.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Клиенты КАК Клиенты
    |ГДЕ
    |    Клиенты.Наименование = &Наименование
    |    И Клиенты.ДатаРождения = &ДатаРождения
    |    И НЕ Клиенты.ПометкаУдаления";
    
    Запрос.УстановитьПараметр("Наименование", ИмяПолное);
    
    // Конвертируем строку даты в формат даты 1С
    ДатаРожденияДата = Неопределено;
    Попытка
        ЧастиДаты = СтрРазделить(ДатаРождения, ".", Ложь);
        Если ЧастиДаты.Количество() = 3 Тогда
            День = Число(ЧастиДаты[0]);
            Месяц = Число(ЧастиДаты[1]);
            Год = Число(ЧастиДаты[2]);
            ДатаРожденияДата = Дата(Год, Месяц, День);
        КонецЕсли;
    Исключение
        // Неверный формат даты
    КонецПопытки;
    
    Запрос.УстановитьПараметр("ДатаРождения", ДатаРожденияДата);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        // ❗ НЕ создаём пациента, возвращаем Неопределено
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции
```

#### 2.3 Обновить метод createAppointment

Найдите метод `createAppointmentPOST` и обновите код поиска пациента:

**БЫЛО:**
```1c
Пациент = НайтиИлиСоздатьПациента(ИмяПациента, Возраст, Пол);
```

**СТАЛО:**
```1c
// Извлекаем дату рождения из запроса
ДатаРождения = ДанныеЗапроса.Получить("birth_date");  // "12.12.1999"

// Валидация
Если ПустаяСтрока(ДатаРождения) Тогда
    ВызватьИсключение "Не указана дата рождения";
КонецЕсли;

// Ищем пациента (БЕЗ СОЗДАНИЯ!)
Пациент = НайтиПациента(ИмяПациента, ДатаРождения);

// Проверка: Если пациент НЕ найден → ошибка!
Если Пациент = Неопределено Тогда
    ВызватьИсключение 
        "Пациент """ + ИмяПациента + """ (дата рождения: " + ДатаРождения + 
        ") не найден в базе! Проверьте ФИО и дату рождения.";
КонецЕсли;
```

Полный код в файле: **`docs/1c-integration-FIXED.md`** → **"Часть 2.1"**

#### 2.4 Опубликовать HTTP-сервис

1. Администрирование → Публикация на веб-сервере
2. Проверить, что `LabIntegration` опубликован

---

### ✅ Шаг 3: Тестирование (5 минут)

#### Тест 1: Клиент существует

```bash
# Создать тестовый файл с правильным именем
# Важно: Используйте ФИО и дату рождения существующего пациента из 1С!

cd /tmp
echo "Test" > "Иванов Иван Иванович 21.05.1982.pdf"

# Поместить на NAS
cp "Иванов Иван Иванович 21.05.1982.pdf" /mnt/nas/lab_results/

# Смотреть логи
docker compose logs -f app
```

**Ожидаемый результат:**
```
✅ Файл обработан
✅ Пациент найден в 1С
✅ Документ "Прием" создан
✅ Файл в архиве
```

#### Тест 2: Клиент НЕ существует

```bash
# Создать файл с несуществующим пациентом
echo "Test" > "Несуществующий Человек 01.01.1900.pdf"
cp "Несуществующий Человек 01.01.1900.pdf" /mnt/nas/lab_results/

# Смотреть логи
docker compose logs -f app
```

**Ожидаемый результат:**
```
❌ Ошибка: "Пациент не найден в базе"
❌ Файл в карантине: /mnt/nas/quarantine/
❌ Лог с подробной информацией
```

#### Проверка в веб-интерфейсе

1. Открыть: https://lis.it-mydoc.ru
2. Логин: `admin` (или ваш)
3. Проверить статистику
4. Проверить карантин (если есть ошибки)

---

### ✅ Шаг 4: Настроить лабораторию (зависит от лаборатории)

#### Вариант А: Лаборатория может экспортировать в нужном формате

Попросите лабораторию именовать файлы как:
```
Фамилия Имя Отчество ДД.ММ.ГГГГ.pdf
```

#### Вариант Б: Лаборатория экспортирует как есть (например, 123456.pdf)

Нужен промежуточный скрипт переименования:

```python
# rename_lab_files.py
import os
import json

# Маппинг номер заказа → ФИО + дата рождения
# Можно загружать из Excel/CSV
mapping = {
    "123456": {"fio": "Иванов Иван Иванович", "birth": "21.05.1982"},
    "123457": {"fio": "Петрова Мария Сергеевна", "birth": "15.03.1995"},
    # ... остальные
}

source_dir = "/mnt/nas/lab_results"
for filename in os.listdir(source_dir):
    if filename.endswith('.pdf'):
        order_no = filename.replace('.pdf', '')
        if order_no in mapping:
            patient = mapping[order_no]
            new_name = f"{patient['fio']} {patient['birth']}.pdf"
            old_path = os.path.join(source_dir, filename)
            new_path = os.path.join(source_dir, new_name)
            os.rename(old_path, new_path)
            print(f"✅ {filename} → {new_name}")
```

---

## 📊 Чеклист обновления

- [ ] **Шаг 1:** Обновлён Python код (`app/pdf_parser.py`)
- [ ] **Шаг 1:** Контейнеры пересобраны и запущены
- [ ] **Шаг 2:** Добавлена функция `НайтиПациента` в 1С
- [ ] **Шаг 2:** Обновлён метод `createAppointmentPOST` в 1С
- [ ] **Шаг 2:** HTTP-сервис опубликован
- [ ] **Шаг 3:** Тест с существующим пациентом пройден
- [ ] **Шаг 3:** Тест с несуществующим пациентом показал ошибку
- [ ] **Шаг 3:** Веб-интерфейс работает корректно
- [ ] **Шаг 4:** Настроена лаборатория или скрипт переименования

---

## 🐛 Проблемы и решения

### Проблема 1: "Файл не парсится"

**Причина:** Неправильное имя файла

**Решение:**
```
✅ Проверить формат: "Фамилия Имя Отчество ДД.ММ.ГГГГ.pdf"
✅ Точки между числами даты (не дефисы!)
✅ Пробелы между словами ФИО
```

### Проблема 2: "Пациент не найден в базе"

**Причина:** ФИО или дата рождения не совпадают с 1С

**Решение:**
```
1. Открыть 1С → Справочник → Клиенты
2. Найти пациента по фамилии
3. Проверить:
   ✅ Наименование точно совпадает с именем файла
   ✅ ДатаРождения точно совпадает с датой в файле
4. Исправить либо имя файла, либо данные в 1С
5. Повторить обработку через веб-интерфейс
```

### Проблема 3: "Дубликаты всё ещё создаются"

**Причина:** Старая версия кода в 1С

**Решение:**
```
✅ Убедитесь, что используете НайтиПациента (не НайтиИлиСоздатьПациента)
✅ Убедитесь, что HTTP-сервис опубликован
✅ Перезапустите 1С сервер
```

---

## 📚 Документация

После обновления изучите:

1. **FILE-NAMING-GUIDE.md** - подробное руководство по именованию
2. **docs/1c-integration-FIXED.md** - полный код для 1С
3. **VERSION-2.0-SUMMARY.md** - краткая сводка изменений
4. **CHANGELOG.md** - полная история изменений

---

## 📞 Поддержка

Если что-то не работает:

1. Проверить логи: `docker compose logs -f app`
2. Проверить систему: `./check-system.sh`
3. Проверить журнал 1С: Администрирование → Журнал регистрации
4. Обратиться к администратору

---

**Версия:** 2.0  
**Дата:** 21.11.2025  
**Время обновления:** ~20 минут  
**Статус:** ✅ Готово к продакшн

---

## 🎉 Поздравляем!

После обновления:
- ✅ Нет дубликатов пациентов
- ✅ Надёжная идентификация по ФИО + дата рождения
- ✅ Явные ошибки вместо тихих сбоев
- ✅ Контроль качества данных

**GitHub:** https://github.com/gr33njj/lis-mini

