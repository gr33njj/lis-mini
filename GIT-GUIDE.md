# 📦 Работа с GitHub репозиторием lis-mini

## 🔗 Репозиторий

**URL:** https://github.com/gr33njj/lis-mini

## 🔑 Первоначальная настройка (один раз)

### Вариант 1: Personal Access Token (рекомендуется)

1. **Создайте токен на GitHub:**
   - Откройте: https://github.com/settings/tokens/new
   - Name: `lis-md-deployment`
   - Expiration: `90 days` (или больше)
   - Scopes: ✅ `repo` (полный доступ к репозиторию)
   - Нажмите "Generate token" и **СКОПИРУЙТЕ** токен

2. **Настройте Git с токеном:**
   ```bash
   cd /opt/lis-md
   git remote set-url origin https://ВАШ_ТОКЕН@github.com/gr33njj/lis-mini.git
   ```

   **Пример:**
   ```bash
   git remote set-url origin https://ghp_xxxxxxxxxxxxxxxxxxxx@github.com/gr33njj/lis-mini.git
   ```

3. **Проверьте:**
   ```bash
   git remote -v
   ```

### Вариант 2: SSH ключ

Если у вас уже настроен SSH ключ на GitHub:

```bash
cd /opt/lis-md
git remote set-url origin git@github.com:gr33njj/lis-mini.git
```

## 📤 Отправка изменений в GitHub

### Способ 1: Использовать скрипт (проще)

```bash
cd /opt/lis-md
./git-push.sh
```

Скрипт автоматически:
- Покажет изменённые файлы
- Запросит подтверждение
- Попросит ввести сообщение коммита
- Отправит изменения на GitHub

### Способ 2: Вручную

```bash
cd /opt/lis-md

# 1. Проверить изменения
git status

# 2. Добавить все изменения
git add -A

# 3. Создать коммит
git commit -m "Описание изменений"

# 4. Отправить на GitHub
git push origin main
```

## 📥 Получение изменений с GitHub

Если вы работаете с нескольких машин:

```bash
cd /opt/lis-md
git pull origin main
```

## 🔍 Полезные команды

### Проверить статус
```bash
git status
```

### Посмотреть историю коммитов
```bash
git log --oneline --graph --all -20
```

### Посмотреть изменения в файле
```bash
git diff имя_файла
```

### Отменить изменения в файле (до коммита)
```bash
git restore имя_файла
```

### Посмотреть последние изменения
```bash
git show
```

## 🔒 Безопасность

### Файлы, которые НЕ попадают в GitHub (.gitignore):

- `.env` - переменные окружения
- `.nas-credentials` - учётные данные NAS
- `CREDENTIALS.txt` - пароли
- `*.pem`, `*.ovpn` - сертификаты VPN
- `data/` - база данных
- `*.log` - логи

**ВАЖНО:** Никогда не коммитьте конфиденциальные данные!

### Если случайно закоммитили секреты:

```bash
# Удалить файл из последнего коммита
git rm --cached файл_с_секретами
git commit --amend -m "Удалён файл с секретами"

# Если уже запушили - нужно изменить токены!
```

## 🔄 Рабочий процесс

### Обычный день работы:

1. Вносите изменения в систему
2. Периодически сохраняйте в Git:
   ```bash
   ./git-push.sh
   ```
3. Изменения попадают на GitHub

### Восстановление на новом сервере:

1. Клонировать репозиторий:
   ```bash
   git clone https://github.com/gr33njj/lis-mini.git /opt/lis-md
   ```

2. Настроить `.env` и другие конфиденциальные файлы вручную

3. Запустить систему:
   ```bash
   cd /opt/lis-md
   ./setup-server.sh
   ```

## 📊 Что хранится в GitHub:

✅ **Хранится:**
- Исходный код приложения
- Конфигурации (без секретов)
- Документация
- Скрипты развёртывания
- Docker конфигурации
- Nginx конфигурации

❌ **НЕ хранится:**
- Пароли и токены
- VPN сертификаты
- База данных
- Логи
- Пользовательские данные

## 🆘 Решение проблем

### Ошибка: "fatal: could not read Username"

Токен не настроен. Выполните шаги из раздела "Первоначальная настройка".

### Ошибка: "rejected: non-fast-forward"

Кто-то изменил репозиторий. Получите изменения:
```bash
git pull origin main --rebase
git push origin main
```

### Ошибка: "conflict"

Конфликт изменений. Решите конфликты вручную:
```bash
# Посмотреть конфликтующие файлы
git status

# Отредактировать файлы, удалив маркеры конфликта
nano конфликтующий_файл

# Добавить решённые файлы
git add конфликтующий_файл

# Завершить merge
git commit
```

## 🎯 Лучшие практики

1. **Коммитьте часто** - маленькие изменения проще отследить
2. **Пишите понятные сообщения** - описывайте ЧТО и ЗАЧЕМ изменили
3. **Проверяйте перед push** - используйте `git status` и `git diff`
4. **Не коммитьте секреты** - проверяйте `.gitignore`
5. **Делайте backup** - GitHub это не замена backup базы данных

## 📚 Дополнительные ресурсы

- Документация Git: https://git-scm.com/doc
- GitHub Docs: https://docs.github.com/
- Репозиторий проекта: https://github.com/gr33njj/lis-mini

---

**Вопросы?** Смотрите официальную документацию или спросите администратора.

