# 🎯 КОД 1С v6.0: С выбором шаблона по префиксу

## ✅ НОВОЕ: Выбор шаблона из названия файла

**Формат файла:** `[ПРЕФИКС] Фамилия Имя Отчество ДД.ММ.ГГГГ.pdf`

**Префиксы:**
- `БХ` → "МД Биохимический анализ крови"
- `КГ` → "МД Анализ крови - Коагулограмма"
- `ЭЛ` → "МД Анализ крови на Электролиты"
- `ОАК` → "МД ОАК"

**Примеры:**
- `БХ Тестов Тест Тестович 18.11.1988.pdf`
- `КГ Иванов Иван Иванович 01.01.1980.pdf`
- `ЭЛ Петрова Анна Сергеевна 15.05.1975.pdf`
- `ОАК Сидоров Петр Петрович 20.12.1990.pdf`

---

## 📝 ПОЛНЫЙ КОД 1С

```1c
Функция createAppointmentPOST(Запрос)
    Ответ = Новый HTTPСервисОтвет(200);
    
    Попытка
        // 1. Читаем JSON
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(Запрос.ПолучитьТелоКакСтроку());
        ДанныеJSON = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // 2. Извлекаем данные
        ИмяПациента = ДанныеJSON["patient_name"];
        ДатаРождения = ДанныеJSON["birth_date"];
        Возраст = "";
        Попытка
            Возраст = Строка(ДанныеJSON["age"]);
        Исключение
            Возраст = "";
        КонецПопытки;
        
        // ⭐ НОВОЕ: Код шаблона
        КодШаблона = "";
        Попытка
            КодШаблона = ДанныеJSON["template_code"];  // "biochemistry", "coagulogram", "electrolytes", "cbc"
        Исключение
            КодШаблона = "biochemistry";  // По умолчанию
        КонецПопытки;
        
        РезультатыАнализов = ДанныеJSON["test_results"];
        
        // 3. Валидация
        Если ПустаяСтрока(ИмяПациента) Тогда
            ВызватьИсключение "Не указано ФИО пациента";
        КонецЕсли;
        
        Если ПустаяСтрока(ДатаРождения) Тогда
            ВызватьИсключение "Не указана дата рождения";
        КонецЕсли;
        
        // 4. Ищем пациента
        Пациент = НайтиПациента(ИмяПациента, ДатаРождения);
        
        Если Пациент = Неопределено Тогда
            ВызватьИсключение "Пациент """ + ИмяПациента + """ (дата рождения: " + ДатаРождения + ") не найден в базе!";
        КонецЕсли;
        
        // 5. Получаем пол пациента
        ПолПациента = "";
        Попытка
            ПолПациента = Строка(Пациент.Пол);
        Исключение
            ПолПациента = "";
        КонецПопытки;
        
        // 6. Формируем даты
        ТекущаяДата = ТекущаяДата();
        ДатаВзятияСтрока = Формат(ТекущаяДата, "ДФ=dd.MM.yyyy");
        ДатаВыдачиСтрока = Формат(ТекущаяДата, "ДФ=dd.MM.yyyy");
        
        // 7. Ищем врача
        Врач = НайтиВрача("МД Lab");
        Если Врач = Неопределено Тогда
            Врач = НайтиВрача("Lab");
        КонецЕсли;
        
        // 8. ⭐ НОВОЕ: Ищем шаблон по коду
        Шаблон = НайтиШаблонПоКоду(КодШаблона);
        
        // 9. Создаём документ "Прием"
        Прием = Документы.Прием.СоздатьДокумент();
        Прием.Дата = ТекущаяДата;
        Прием.Клиент = Пациент;
        Прием.Врач = Врач;
        
        // 10. Добавляем шаблон
        Если Шаблон <> Неопределено Тогда
            НоваяСтрокаШаблона = Прием.ШаблоныПриема.Добавить();
            НоваяСтрокаШаблона.ШаблонПриема = Шаблон;
        КонецЕсли;
        
        // 11. Записываем документ
        Прием.Записать(РежимЗаписиДокумента.Проведение);
        
        // 12. Заполняем HTML параметры
        КоличествоЗаполненных = ЗаполнитьРегистрПараметров(
            Прием.Ссылка,
            РезультатыАнализов,
            ИмяПациента,
            ДатаРождения,
            Возраст,
            ПолПациента,
            ДатаВзятияСтрока,
            ДатаВыдачиСтрока
        );
        
        // 13. Возвращаем успех
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("status", "ok");
        РезультатJSON.Вставить("appointment_ref", Строка(Прием.Ссылка));
        РезультатJSON.Вставить("appointment_number", Прием.Номер);
        РезультатJSON.Вставить("patient_name", ИмяПациента);
        РезультатJSON.Вставить("birth_date", ДатаРождения);
        РезультатJSON.Вставить("gender", ПолПациента);
        РезультатJSON.Вставить("template_code", КодШаблона);
        РезультатJSON.Вставить("template", ?(Шаблон = Неопределено, "Не найден", Строка(Шаблон)));
        РезультатJSON.Вставить("doctor", ?(Врач = Неопределено, "Не найден", Строка(Врач)));
        РезультатJSON.Вставить("parameters_filled", КоличествоЗаполненных);
        РезультатJSON.Вставить("parameters_received", РезультатыАнализов.Количество());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
        
    Исключение
        Ответ.КодСостояния = 400;
        
        ОшибкаJSON = Новый Структура;
        ОшибкаJSON.Вставить("status", "error");
        ОшибкаJSON.Вставить("error", ОписаниеОшибки());
        
        Попытка
            Если ДанныеJSON <> Неопределено Тогда
                ОшибкаJSON.Вставить("patient_name", ДанныеJSON["patient_name"]);
                ОшибкаJSON.Вставить("birth_date", ДанныеJSON["birth_date"]);
            КонецЕсли;
        Исключение
        КонецПопытки;
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, ОшибкаJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
    КонецПопытки;
    
    Возврат Ответ;
КонецФункции


// ═══════════════════════════════════════════════════════════════
// ⭐ НОВАЯ ФУНКЦИЯ: Выбор шаблона по коду
// ═══════════════════════════════════════════════════════════════

Функция НайтиШаблонПоКоду(КодШаблона)
    
    // Соответствие кода шаблона и его названия в 1С
    НаименованиеШаблона = "";
    
    Если КодШаблона = "biochemistry" Тогда
        НаименованиеШаблона = "МД Биохимический анализ крови";
    ИначеЕсли КодШаблона = "coagulogram" Тогда
        НаименованиеШаблона = "МД Анализ крови - Коагулограмма";
    ИначеЕсли КодШаблона = "electrolytes" Тогда
        НаименованиеШаблона = "МД Анализ крови на Электролиты";
    ИначеЕсли КодШаблона = "cbc" Тогда
        НаименованиеШаблона = "МД ОАК";
    Иначе
        // По умолчанию биохимия
        НаименованиеШаблона = "МД Биохимический анализ крови";
    КонецЕсли;
    
    // Ищем шаблон по названию
    Возврат НайтиШаблон(НаименованиеШаблона);
    
КонецФункции


// ═══════════════════════════════════════════════════════════════
// ОСТАЛЬНЫЕ ФУНКЦИИ (БЕЗ ИЗМЕНЕНИЙ)
// ═══════════════════════════════════════════════════════════════

Функция НайтиПациента(ИмяПолное, ДатаРождения)
    
    Попытка
        ИмяПолное = СокрЛП(ИмяПолное);
        Пока СтрНайти(ИмяПолное, "  ") > 0 Цикл
            ИмяПолное = СтрЗаменить(ИмяПолное, "  ", " ");
        КонецЦикла;
        
        Фамилия = "";
        Имя = "";
        Отчество = "";
        
        СписокФИО = СтрРазделить(ИмяПолное, " ", Ложь);
        Если СписокФИО.Количество() >= 1 Тогда Фамилия = СписокФИО[0]; КонецЕсли;
        Если СписокФИО.Количество() >= 2 Тогда Имя = СписокФИО[1]; КонецЕсли;
        Если СписокФИО.Количество() >= 3 Тогда Отчество = СписокФИО[2]; КонецЕсли;
        
        ДатаРожденияДата = Неопределено;
        Если НЕ ПустаяСтрока(ДатаРождения) Тогда
            Попытка
                Части = СтрРазделить(ДатаРождения, ".", Ложь);
                Если Части.Количество() = 3 Тогда
                    ДатаРожденияДата = Дата(Число(Части[2]), Число(Части[1]), Число(Части[0]));
                КонецЕсли;
            Исключение
            КонецПопытки;
        КонецЕсли;
        
        Запрос = Новый Запрос;
        
        ТекстЗапроса = 
        "ВЫБРАТЬ ПЕРВЫЕ 1
        |    Клиенты.Ссылка КАК Ссылка
        |ИЗ
        |    Справочник.Клиенты КАК Клиенты
        |ГДЕ
        |    Клиенты.Фамилия = &Фамилия
        |    И Клиенты.Имя = &Имя";
        
        Запрос.УстановитьПараметр("Фамилия", Фамилия);
        Запрос.УстановитьПараметр("Имя", Имя);
        
        Если НЕ ПустаяСтрока(Отчество) Тогда
            ТекстЗапроса = ТекстЗапроса + "
            |    И Клиенты.Отчество = &Отчество";
            Запрос.УстановитьПараметр("Отчество", Отчество);
        КонецЕсли;
        
        Если ДатаРожденияДата <> Неопределено Тогда
            ТекстЗапроса = ТекстЗапроса + "
            |    И Клиенты.ДатаРождения = &ДатаРождения";
            Запрос.УстановитьПараметр("ДатаРождения", ДатаРожденияДата);
        КонецЕсли;
        
        ТекстЗапроса = ТекстЗапроса + "
        |    И НЕ Клиенты.ПометкаУдаления";
        
        Запрос.Текст = ТекстЗапроса;
        
        Результат = Запрос.Выполнить();
        
        Если НЕ Результат.Пустой() Тогда
            Выборка = Результат.Выбрать();
            Выборка.Следующий();
            Возврат Выборка.Ссылка;
        Иначе
            Возврат Неопределено;
        КонецЕсли;
        
    Исключение
        Возврат Неопределено;
    КонецПопытки;
    
КонецФункции


Функция НайтиВрача(ИмяВрача)
    
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Сотрудники.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Сотрудники КАК Сотрудники
    |ГДЕ
    |    Сотрудники.Наименование ПОДОБНО &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", "%" + ИмяВрача + "%");
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция НайтиШаблон(НаименованиеШаблона)
    
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Шаблоны.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.ШаблоныHTML КАК Шаблоны
    |ГДЕ
    |    Шаблоны.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", НаименованиеШаблона);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция ЗаполнитьРегистрПараметров(СсылкаДокумента, РезультатыАнализов, ИмяПациента, ДатаРождения, Возраст, Пол, ДатаВзятия, ДатаВыдачи)
    
    Счетчик = 0;
    
    Попытка
        Набор = РегистрыСведений.ЗначенияПараметровHTML.СоздатьНаборЗаписей();
        Набор.Отбор.Документ.Установить(СсылкаДокумента);
        Набор.Прочитать();
        Набор.Очистить();
        
        // ШАПКА
        Параметр = НайтиПараметрПоGUID("f7bece55-cafa-11e5-9bc7-50af732359f4");
        Если Параметр <> Неопределено Тогда
            НоваяЗапись = Набор.Добавить();
            НоваяЗапись.Документ = СсылкаДокумента;
            НоваяЗапись.ИндексШаблона = 0;
            НоваяЗапись.Параметр = Параметр;
            НоваяЗапись.Значение = ИмяПациента;
            Счетчик = Счетчик + 1;
        КонецЕсли;
        
        Параметр = НайтиПараметрПоGUID("93750e7d-5161-11ea-80c9-ac1f6bd849f8");
        Если Параметр <> Неопределено И НЕ ПустаяСтрока(ДатаРождения) Тогда
            НоваяЗапись = Набор.Добавить();
            НоваяЗапись.Документ = СсылкаДокумента;
            НоваяЗапись.ИндексШаблона = 0;
            НоваяЗапись.Параметр = Параметр;
            НоваяЗапись.Значение = ДатаРождения;
            Счетчик = Счетчик + 1;
        КонецЕсли;
        
        Параметр = НайтиПараметрПоGUID("4d22b3c0-03bd-11e3-943d-1c6f653fefc3");
        Если Параметр <> Неопределено И НЕ ПустаяСтрока(Возраст) Тогда
            НоваяЗапись = Набор.Добавить();
            НоваяЗапись.Документ = СсылкаДокумента;
            НоваяЗапись.ИндексШаблона = 0;
            НоваяЗапись.Параметр = Параметр;
            НоваяЗапись.Значение = Возраст;
            Счетчик = Счетчик + 1;
        КонецЕсли;
        
        Параметр = НайтиПараметрПоGUID("94c2e827-1449-11f0-a70e-d066716536da");
        Если Параметр <> Неопределено И НЕ ПустаяСтрока(Пол) Тогда
            НоваяЗапись = Набор.Добавить();
            НоваяЗапись.Документ = СсылкаДокумента;
            НоваяЗапись.ИндексШаблона = 0;
            НоваяЗапись.Параметр = Параметр;
            НоваяЗапись.Значение = Пол;
            Счетчик = Счетчик + 1;
        КонецЕсли;
        
        Параметр = НайтиПараметрПоGUID("edf1c744-22a4-11e2-87b5-002618dcef2c");
        Если Параметр <> Неопределено Тогда
            НоваяЗапись = Набор.Добавить();
            НоваяЗапись.Документ = СсылкаДокумента;
            НоваяЗапись.ИндексШаблона = 0;
            НоваяЗапись.Параметр = Параметр;
            НоваяЗапись.Значение = ДатаВзятия;
            Счетчик = Счетчик + 1;
        КонецЕсли;
        
        Параметр = НайтиПараметрПоGUID("8beddfdf-d17f-11e1-b361-1803736d59cd");
        Если Параметр <> Неопределено Тогда
            НоваяЗапись = Набор.Добавить();
            НоваяЗапись.Документ = СсылкаДокумента;
            НоваяЗапись.ИндексШаблона = 0;
            НоваяЗапись.Параметр = Параметр;
            НоваяЗапись.Значение = ДатаВыдачи;
            Счетчик = Счетчик + 1;
        КонецЕсли;
        
        // РЕЗУЛЬТАТЫ
        Для Каждого ЭлементМассива Из РезультатыАнализов Цикл
            GUID = ЭлементМассива["parameter_id"];
            Значение = ЭлементМассива["value"];
            
            Параметр = НайтиПараметрПоGUID(GUID);
            
            Если Параметр <> Неопределено Тогда
                НоваяЗапись = Набор.Добавить();
                НоваяЗапись.Документ = СсылкаДокумента;
                НоваяЗапись.ИндексШаблона = 0;
                НоваяЗапись.Параметр = Параметр;
                НоваяЗапись.Значение = Значение;
                Счетчик = Счетчик + 1;
            КонецЕсли;
        КонецЦикла;
        
        Набор.Записать();
        
    Исключение
        Счетчик = 0;
    КонецПопытки;
    
    Возврат Счетчик;
    
КонецФункции


Функция НайтиПараметрПоGUID(ИскомыйGUID)
    
    Попытка
        ИскомыйGUIDНижний = НРег(ИскомыйGUID);
        
        Запрос = Новый Запрос;
        Запрос.Текст = 
        "ВЫБРАТЬ
        |    ПараметрыHTML.Ссылка КАК Ссылка
        |ИЗ
        |    ПланВидовХарактеристик.ПараметрыHTML КАК ПараметрыHTML";
        
        Результат = Запрос.Выполнить();
        Выборка = Результат.Выбрать();
        
        Пока Выборка.Следующий() Цикл
            GUIDЭлемента = НРег(Строка(Выборка.Ссылка.УникальныйИдентификатор()));
            
            Если GUIDЭлемента = ИскомыйGUIDНижний Тогда
                Возврат Выборка.Ссылка;
            КонецЕсли;
        КонецЦикла;
        
    Исключение
    КонецПопытки;
    
    Возврат Неопределено;
    
КонецФункции
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Биохимия

```cmd
curl -X POST "http://192.168.100.234/BITtest/hs/lab/createAppointment" -u "Администратор:1234" -H "Content-Type: application/json" -d "{\"patient_name\":\"Тестов Тест Тестович\",\"birth_date\":\"18.11.1988\",\"age\":\"36\",\"template_code\":\"biochemistry\",\"test_results\":[{\"parameter_id\":\"ae584a2f-957f-11f0-a7be-eca0f0014d7d\",\"value\":\"25.5\"}]}"
```

### Тест 2: Коагулограмма

```cmd
curl -X POST "http://192.168.100.234/BITtest/hs/lab/createAppointment" -u "Администратор:1234" -H "Content-Type: application/json" -d "{\"patient_name\":\"Иванов Иван Иванович\",\"birth_date\":\"01.01.1980\",\"age\":\"45\",\"template_code\":\"coagulogram\",\"test_results\":[{\"parameter_id\":\"7d5a1ff0-97d7-11f0-a7c1-83d4d16bba95\",\"value\":\"12\"}]}"
```

### Тест 3: Электролиты

```cmd
curl -X POST "http://192.168.100.234/BITtest/hs/lab/createAppointment" -u "Администратор:1234" -H "Content-Type: application/json" -d "{\"patient_name\":\"Петрова Анна Сергеевна\",\"birth_date\":\"15.05.1975\",\"age\":\"50\",\"template_code\":\"electrolytes\",\"test_results\":[{\"parameter_id\":\"aadd6ad5-9580-11f0-a7be-eca0f0014d7d\",\"value\":\"4.5\"}]}"
```

---

**Версия:** 6.0  
**Дата:** 25.11.2025  
**Статус:** ✅ С выбором шаблона по префиксу
