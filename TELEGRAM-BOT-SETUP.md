# 🤖 НАСТРОЙКА TELEGRAM-БОТА ДЛЯ УВЕДОМЛЕНИЙ

## ✅ Что делает бот

Telegram-бот автоматически отправляет уведомления о каждой загрузке лабораторного документа:

**При успехе:**
```
✅ ДОКУМЕНТ СОЗДАН

👤 Пациент: И.И.И.
📄 Документ: №МД-00000123
🗂 Шаблон: МД Биохимический анализ крови
📊 Параметров: 15
```

**При ошибке:**
```
❌ ОШИБКА ЗАГРУЗКИ

👤 Пациент: И.И.И.
📁 Файл: БХ Иванов Иван Иванович 01.01.1980.pdf

⚠️ Ошибка:
Пациент не найден в базе данных 1С
```

---

## 📋 ПОШАГОВАЯ НАСТРОЙКА

### Шаг 1: Создайте бота в Telegram

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Введите название бота (например: `ЛИС МД Уведомления`)
4. Введите username бота (например: `lis_md_notifications_bot`)
5. **Скопируйте токен** (выглядит примерно так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Получите Chat ID

**Вариант А: Через готовый скрипт**

```bash
cd /opt/lis-md
python3 telegram_get_chat_id.py YOUR_BOT_TOKEN
```

Затем напишите боту любое сообщение в Telegram. Скрипт выведет ваш chat_id.

**Вариант Б: Вручную**

1. Напишите боту любое сообщение (например: `/start`)
2. Откройте в браузере: 
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
3. Найдите поле `"chat":{"id":123456789}` - это ваш chat_id

### Шаг 3: Добавьте данные в .env

Откройте файл `/opt/lis-md/.env` и заполните:

```bash
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

### Шаг 4: Пересоберите Docker контейнер

```bash
cd /opt/lis-md
docker-compose down
docker-compose up -d --build
```

### Шаг 5: Проверьте работу

Проверьте логи:
```bash
docker logs -f lis-md-app
```

Вы должны увидеть:
```
[Telegram] ✅ Бот инициализирован, chat_id: 123456789
```

Положите тестовый PDF файл в `/mnt/nas/lab_results/` и проверьте, что пришло уведомление.

---

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Проверка модуля

```bash
cd /opt/lis-md/app
export TELEGRAM_BOT_TOKEN="your_token"
export TELEGRAM_CHAT_ID="your_chat_id"
python3 telegram_notifier.py
```

Должны прийти 2 тестовых сообщения в Telegram.

### Тест 2: Реальный файл

Положите файл `БХ Тестов Тест Тестович 18.11.1988.pdf` в `/mnt/nas/lab_results/`.

Должно прийти уведомление о создании документа.

---

## 🔧 ОТКЛЮЧЕНИЕ УВЕДОМЛЕНИЙ

Если нужно временно отключить Telegram уведомления, просто удалите или закомментируйте строки в `.env`:

```bash
#TELEGRAM_BOT_TOKEN=
#TELEGRAM_CHAT_ID=
```

Пересоберите контейнер:
```bash
docker-compose down && docker-compose up -d --build
```

В логах будет:
```
[Telegram] ⚠️  Бот ВЫКЛЮЧЕН (не указан токен или chat_id)
```

---

## 📊 ФОРМАТИРОВАНИЕ СООБЩЕНИЙ

### ФИО → Инициалы

Система автоматически преобразует полное ФИО в инициалы:

| Полное ФИО | Инициалы |
|------------|----------|
| Иванов Иван Иванович | И.И.И. |
| Петрова Анна Сергеевна | П.А.С. |
| Сидоров Петр | С.П. |

Это сделано для:
- ✅ Конфиденциальности (не показываем полное ФИО в чате)
- ✅ Компактности сообщений

### Обрезка длинных ошибок

Текст ошибки ограничен **500 символами**, чтобы сообщения не были слишком длинными.

---

## 🔍 ДИАГНОСТИКА ПРОБЛЕМ

### ❌ Бот не отправляет сообщения

**Проверьте:**

1. Токен указан правильно в `.env`:
   ```bash
   grep TELEGRAM_BOT_TOKEN /opt/lis-md/.env
   ```

2. Chat ID указан правильно:
   ```bash
   grep TELEGRAM_CHAT_ID /opt/lis-md/.env
   ```

3. Бот запущен в контейнере:
   ```bash
   docker logs lis-md-app | grep Telegram
   ```

4. Вы написали боту хотя бы одно сообщение (иначе он не может отправлять)

### ❌ Ошибка "Unauthorized"

**Причина:** Неправильный токен бота.

**Решение:** Проверьте токен в `.env`, создайте нового бота если нужно.

### ❌ Ошибка "Chat not found"

**Причина:** Неправильный chat_id или вы не писали боту.

**Решение:** Напишите боту `/start` в Telegram, затем получите chat_id заново.

### ❌ Бот молчит, но в логах нет ошибок

**Причина:** Уведомления отключены (пустые значения в `.env`).

**Решение:** Заполните `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`.

---

## 🔐 БЕЗОПАСНОСТЬ

⚠️ **ВАЖНО:**

1. **НЕ КОММИТЬТЕ** токен бота в Git!
2. Храните токен только в `.env` файле
3. Добавьте `.env` в `.gitignore`
4. Не делитесь токеном с посторонними
5. Если токен скомпрометирован - создайте нового бота у @BotFather

---

## 📞 ПОДДЕРЖКА

Если что-то не работает:

1. Проверьте логи: `docker logs -f lis-md-app`
2. Убедитесь, что бот создан и токен правильный
3. Убедитесь, что chat_id получен корректно
4. Протестируйте модуль: `python3 app/telegram_notifier.py`

---

**Версия:** 1.0  
**Дата:** 26.11.2025  
**Статус:** ✅ Готово к использованию
