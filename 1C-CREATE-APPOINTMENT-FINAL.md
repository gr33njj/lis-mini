# 📋 СОЗДАНИЕ ПРИЕМА В 1С (ФИНАЛЬНАЯ ВЕРСИЯ)

> ✅ **Адаптировано под вашу конфигурацию!**
> - Документ: `Документ.Прием`
> - План Видов Характеристик: `ПараметрыHTML`
> - Справочники: `Клиенты`, `Сотрудники`, `ШаблоныHTML`

---

## 🎯 Задача

Автоматически создавать документ **"Прием"** с:
- **Врач**: МД Lab (из `Справочник.Сотрудники`)
- **Клиент**: ФИО из названия PDF (из `Справочник.Клиенты`)
- **ШаблонОсмотра**: "МД Биохимический анализ крови" (из `Справочник.ШаблоныHTML`)
- **Параметры**: Заполненные значениями из PDF через `ПланВидовХарактеристик.ПараметрыHTML`

---

## 📝 Шаг 1: Добавить новый метод в HTTP-сервис

Открой HTTP-сервис `LabIntegration` и добавь новый метод:

### Свойства метода `createAppointment`:
- **Имя**: `createAppointment`
- **Шаблон URL**: `createAppointment`
- **HTTP метод**: `POST`
- **Аутентификация**: `Basic` (Администратор:1234)

### Код метода `createAppointmentPOST`:

```1c
Функция createAppointmentPOST(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    
    Попытка
        // 1. Читаем JSON (простой способ, работает в любой версии 1С)
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(Запрос.ПолучитьТелоКакСтроку());
        ДанныеJSON = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // 2. Извлекаем данные
        // Python отправляет test_results как массив: [{"parameter_id": "uuid", "value": "123"}, ...]
        ИмяПациента = ДанныеJSON["patient_name"];
        Возраст = ДанныеJSON["age"];
        Пол = ДанныеJSON["gender"];
        ДатаЗабора = ДанныеJSON["sample_date"];
        ДатаРезультата = ДанныеJSON["result_date"];
        РезультатыАнализов = ДанныеJSON["test_results"];
        
        // 3. Ищем или создаём пациента
        Пациент = НайтиИлиСоздатьПациента(ИмяПациента, Возраст, Пол);
        
        // 4. Ищем врача "МД Lab"
        Врач = НайтиВрача("МД Lab");
        Если Врач = Неопределено Тогда
            Врач = НайтиВрача("Lab"); // Попытка без префикса
        КонецЕсли;
        
        // 5. Ищем шаблон (если не найден - продолжаем, но вернём info в ответе)
        Шаблон = НайтиШаблон("МД Биохимический анализ крови");
        
        // 6. Создаём документ "Прием"
        Прием = Документы.Прием.СоздатьДокумент();
        Прием.Дата = ТекущаяДата();
        Прием.Клиент = Пациент;  // Реквизит называется "Клиент"
        Прием.Врач = Врач;
        
        // Устанавливаем шаблон только если найден (реквизит называется "ШаблонОсмотра")
        Если Шаблон <> Неопределено Тогда
            Прием.ШаблонОсмотра = Шаблон;
        КонецЕсли;
        
        // 7. Заполняем параметры через План Видов Характеристик
        КоличествоЗаполненных = ЗаполнитьПараметрыПриема(Прием, РезультатыАнализов);
        
        // 8. Записываем документ
        Прием.Записать(РежимЗаписиДокумента.Проведение);
        
        // 9. Возвращаем успех с отладочной информацией
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("status", "ok");
        РезультатJSON.Вставить("appointment_ref", Строка(Прием.Ссылка));
        РезультатJSON.Вставить("appointment_number", Прием.Номер);
        РезультатJSON.Вставить("patient_name", ИмяПациента);
        РезультатJSON.Вставить("doctor", ?(Врач = Неопределено, "Не найден", Строка(Врач)));
        РезультатJSON.Вставить("template", ?(Шаблон = Неопределено, "Не найден", Строка(Шаблон)));
        РезультатJSON.Вставить("parameters_filled", КоличествоЗаполненных);
        РезультатJSON.Вставить("parameters_total", РезультатыАнализов.Количество());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
        
    Исключение
        Ответ.КодСостояния = 500;
        
        ОшибкаJSON = Новый Структура;
        ОшибкаJSON.Вставить("status", "error");
        ОшибкаJSON.Вставить("error", ОписаниеОшибки());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, ОшибкаJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции


// ═══════════════════════════════════════════════════════════════
// ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
// ═══════════════════════════════════════════════════════════════

Функция НайтиИлиСоздатьПациента(ИмяПолное, Возраст, Пол)
    
    // Ищем существующего пациента по ФИО
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Клиенты.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Клиенты КАК Клиенты
    |ГДЕ
    |    Клиенты.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", ИмяПолное);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        // Создаём нового пациента
        НовыйПациент = Справочники.Клиенты.СоздатьЭлемент();
        НовыйПациент.Наименование = ИмяПолное;
        // Можно добавить дополнительные поля:
        // НовыйПациент.Возраст = Возраст;
        // НовыйПациент.Пол = Пол;
        НовыйПациент.Записать();
        Возврат НовыйПациент.Ссылка;
    КонецЕсли;
    
КонецФункции


Функция НайтиВрача(ИмяВрача)
    
    // Ищем врача в справочнике Сотрудники
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Сотрудники.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Сотрудники КАК Сотрудники
    |ГДЕ
    |    Сотрудники.Наименование ПОДОБНО &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", "%" + ИмяВрача + "%");
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция НайтиШаблон(НаименованиеШаблона)
    
    // Ищем шаблон в справочнике ШаблоныHTML
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Шаблоны.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.ШаблоныHTML КАК Шаблоны
    |ГДЕ
    |    Шаблоны.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", НаименованиеШаблона);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция ЗаполнитьПараметрыПриема(Прием, РезультатыАнализов)
    
    // РезультатыАнализов приходит как Массив объектов:
    // [{"parameter_id": "uuid", "value": "123"}, {"parameter_id": "uuid2", "value": "456"}, ...]
    
    Счетчик = 0;
    
    // Заполняем табличную часть ТаблицаПараметров
    Для Каждого ЭлементМассива Из РезультатыАнализов Цикл
        
        Попытка
            // Извлекаем UUID и значение из объекта
            UUID = ЭлементМассива["parameter_id"];
            Значение = ЭлементМассива["value"];
            
            // Находим характеристику по коду (UUID)
            Характеристика = НайтиХарактеристикуПоКоду(UUID);
            
            Если Характеристика <> Неопределено Тогда
                // Добавляем строку в табличную часть ТаблицаПараметров
                НоваяСтрока = Прием.ТаблицаПараметров.Добавить();
                НоваяСтрока.Параметр = Характеристика;
                НоваяСтрока.Значение = Значение;
                
                // Заполняем дополнительные поля (опционально)
                // НоваяСтрока.ТипЗначения = Характеристика.ТипЗначения;
                // НоваяСтрока.Представление = Строка(Значение);
                
                Счетчик = Счетчик + 1;
            Иначе
                // Характеристика не найдена - это нормально, просто пропускаем
                // (UUID может быть из другого шаблона или не создан в системе)
            КонецЕсли;
        Исключение
            // Если ошибка при добавлении строки - возможно неправильное имя ТЧ
            // Просто продолжаем, но не останавливаем весь процесс
        КонецПопытки;
        
    КонецЦикла;
    
    Возврат Счетчик;
    
КонецФункции


Функция НайтиХарактеристикуПоКоду(UUID)
    
    // Ищем характеристику в плане видов характеристик ПараметрыHTML
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    ПараметрыHTML.Ссылка КАК Ссылка
    |ИЗ
    |    ПланВидовХарактеристик.ПараметрыHTML КАК ПараметрыHTML
    |ГДЕ
    |    ПараметрыHTML.Код = &Код";
    
    Запрос.УстановитьПараметр("Код", UUID);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции
```

---

## 📝 Шаг 2: Создай необходимые данные

### 1️⃣ Создай врача "МД Lab"
- Открой `Справочник.Сотрудники`
- Создай нового сотрудника
- **Наименование**: `МД Lab`

### 2️⃣ Проверь шаблон "МД Биохимический анализ крови"
- Открой `Справочник.ШаблоныHTML`
- Найди или создай шаблон с наименованием: `МД Биохимический анализ крови`

### 3️⃣ Создай характеристики (параметры) в `ПараметрыHTML`
Для каждого из 18 показателей создай элемент плана видов характеристик:

| Наименование | Код (UUID) | Тип Значения |
|--------------|------------|--------------|
| АЛАТ | `ae584a2f-957f-11f0-a7be-eca0f0014d7d` | Число |
| АСАТ | `d284601d-957f-11f0-a7be-eca0f0014d7d` | Число |
| Холестерин | `e5e808bc-957f-11f0-a7be-eca0f0014d7d` | Число |
| гамма-ГТ | `ef9d787d-957f-11f0-a7be-eca0f0014d7d` | Число |
| Щелочн. фосф-за | `0190f459-9580-11f0-a7be-eca0f0014d7d` | Число |
| Триглицериды | `0bf4b4f9-9580-11f0-a7be-eca0f0014d7d` | Число |
| ЛПВП | `19dd5baa-9580-11f0-a7be-eca0f0014d7d` | Число |
| ЛПНП | `3b7519c0-9580-11f0-a7be-eca0f0014d7d` | Число |
| ЛДГ | `43f2ce34-9580-11f0-a7be-eca0f0014d7d` | Число |
| Мочевина | `4b6251cb-9580-11f0-a7be-eca0f0014d7d` | Число |
| альфа-Амилаза | `562a56ba-9580-11f0-a7be-eca0f0014d7d` | Число |
| Билирубин общий | `62c0a29c-9580-11f0-a7be-eca0f0014d7d` | Число |
| Билирубин прямой | `681a2d53-9580-11f0-a7be-eca0f0014d7d` | Число |
| Кальций | `740e37da-9580-11f0-a7be-eca0f0014d7d` | Число |
| Креатинин | `8007eafe-9580-11f0-a7be-eca0f0014d7d` | Число |
| Глюкоза | `8bf99265-9580-11f0-a7be-eca0f0014d7d` | Число |
| Общий белок | `97ee2791-9580-11f0-a7be-eca0f0014d7d` | Число |
| Железо | `9decbf26-9580-11f0-a7be-eca0f0014d7d` | Число |
| Мочевая кислота | `bc144e0a-a67f-11f0-a7d7-d01c04d652e6` | Число |

---

## 🧪 Шаг 3: Тестирование

### Тест через curl:

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/createAppointment \
  -u "Администратор:1234" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Тестов Тест Тестович",
    "age": "35",
    "gender": "Мужской",
    "sample_date": "18.11.2025",
    "result_date": "18.11.2025",
    "test_results": {
      "ae584a2f-957f-11f0-a7be-eca0f0014d7d": "25.5",
      "8bf99265-9580-11f0-a7be-eca0f0014d7d": "5.2"
    }
  }'
```

**Ожидаемый ответ**:
```json
{
  "status": "ok",
  "appointment_ref": "Документ.Прием.000000001",
  "appointment_number": "00000001",
  "patient_name": "Тестов Тест Тестович",
  "doctor": "МД Lab",
  "parameters_filled": 2
}
```

---

## ✅ Структура параметров (адаптировано под вашу конфигурацию)

Код уже адаптирован под вашу структуру:

**Табличная часть документа**: `ТаблицаПараметров`

**Поля**:
- `Параметр` → `ПланВидовХарактеристикСсылка.ПараметрыHTML`
- `Значение` → числовое/текстовое значение параметра
- `ФорматнаяСтрока`, `Представление`, `ТипЗначения`, `ИдентификаторСтрокиШаблона` → заполняются автоматически или не требуются для базовой работы

Функция `ЗаполнитьПараметрыПриема` создает записи в `ТаблицаПараметров` со следующими полями:
- `НоваяСтрока.Параметр` = ссылка на характеристику
- `НоваяСтрока.Значение` = значение из PDF

При необходимости можно раскомментировать строки для заполнения дополнительных полей (`ТипЗначения`, `Представление`).

---

## 🚨 Возможные проблемы

### ❌ "Характеристика не найдена"
**Причина**: UUID не создан в `ПланВидовХарактеристик.ПараметрыHTML`
**Решение**: Создайте все 18 характеристик (см. таблицу в Шаге 2)

### ❌ "Шаблон не найден"
**Причина**: Шаблон с таким именем не существует
**Решение**: Создайте шаблон с именем **точно** `МД Биохимический анализ крови`

### ❌ "Врач не найден"
**Причина**: Сотрудника "МД Lab" нет в справочнике
**Решение**: Создайте сотрудника `МД Lab` в `Справочник.Сотрудники`

---

## 📞 Что дальше?

1. **Скопируй** код в HTTP-сервис `LabIntegration`
2. **Создай** врача "МД Lab"
3. **Проверь** шаблон "МД Биохимический анализ крови"
4. **Создай** все 18 характеристик в `ПараметрыHTML` (или убедись, что они уже есть)
5. **Уточни**, какой вариант хранения параметров используется (табличная часть или регистр)
6. **Протестируй** через curl
7. **Если работает** → закидывай реальные PDF!

---

## 📚 Полезные файлы

- `/opt/lis-md/PRODUCTION-GUIDE.md` - Работа с системой
- `/opt/lis-md/REAL-WORKFLOW-GUIDE.md` - Работа с файлами

