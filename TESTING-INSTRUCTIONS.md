# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ ПАРСИНГА PDF И ИНТЕГРАЦИИ С 1С

## ✅ ЧТО УЖЕ ГОТОВО

1. **Парсер PDF** ✅
   - Извлекает мета-информацию (ФИО, возраст, пол, даты)
   - Парсит результаты анализов из текста
   - Маппит значения на ID полей HTML шаблона

2. **Интегратор 1С** ✅
   - Использует Basic Auth (Администратор:1234)
   - Отправляет JSON данные в 1С
   - Endpoint: `http://192.168.100.234/lab/fillTemplate`

3. **API Endpoints для тестирования** ✅
   - `/api/test-pdf-parser` - тестирование парсера
   - `/api/test-1c-connection` - тестирование соединения с 1С
   - `/api/test-full-chain` - полная цепочка (PDF → Parse → 1C)

---

## 📋 ШАГ 1: НАСТРОЙКА 1С

### 1.1. Создай HTTP-сервис в 1С

1. Открой конфигуратор 1С
2. Создай HTTP-сервис "LabIntegration" (или открой существующий)
3. Свойства HTTP-сервиса:
   - **Root URL**: `/lab`
   - **Аутентификация**: `<Не использовать>`

### 1.2. Создай URL-шаблоны

Создай 2 URL-шаблона:

#### a) `test` (GET/POST)
- **Шаблон**: `test`
- **Обработчик**: `testPOST`
- **Методы**: GET, POST
- **Аутентификация**: СНЯТА

#### b) `fillTemplate` (POST)
- **Шаблон**: `fillTemplate`
- **Обработчик**: `fillTemplatePOST`
- **Методы**: POST
- **Аутентификация**: СНЯТА

### 1.3. Код модуля HTTP-сервиса

Скопируй код из файла: `/opt/lis-md/1C-FILL-HTML.txt`

Или напрямую из этого файла:

```1c
Функция testPOST(Запрос)
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json");
    Ответ.УстановитьТелоИзСтроки("{""status"":""ok"",""message"":""Test OK""}");
    Возврат Ответ;
КонецФункции


Функция fillTemplatePOST(Запрос)
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json");
    
    Попытка
        // 1. Читаем тело запроса
        Тело = Запрос.ПолучитьТелоКакСтроку();
        
        Если ПустаяСтрока(Тело) Тогда
            Ответ.КодСостояния = 400;
            Ответ.УстановитьТелоИзСтроки("{""error"":""Пустое тело запроса""}");
            Возврат Ответ;
        КонецЕсли;
        
        // 2. Парсим JSON
        Чтение = Новый ЧтениеJSON;
        Чтение.УстановитьСтроку(Тело);
        Данные = ПрочитатьJSON(Чтение);
        Чтение.Закрыть();
        
        // 3. Извлекаем данные
        ИмяПациента = "";
        Возраст = "";
        Пол = "";
        ДатаВзятия = "";
        ДатаВыдачи = "";
        Результаты = Неопределено;
        
        Если ТипЗнч(Данные) = Тип("Соответствие") Тогда
            ИмяПациента = Данные.Получить("patient_name");
            Возраст = Данные.Получить("age");
            Пол = Данные.Получить("gender");
            ДатаВзятия = Данные.Получить("sample_date");
            ДатаВыдачи = Данные.Получить("result_date");
            Результаты = Данные.Получить("test_results");
        ИначеЕсли ТипЗнч(Данные) = Тип("Структура") Тогда
            Если Данные.Свойство("patient_name") Тогда ИмяПациента = Данные.patient_name; КонецЕсли;
            Если Данные.Свойство("age") Тогда Возраст = Данные.age; КонецЕсли;
            Если Данные.Свойство("gender") Тогда Пол = Данные.gender; КонецЕсли;
            Если Данные.Свойство("sample_date") Тогда ДатаВзятия = Данные.sample_date; КонецЕсли;
            Если Данные.Свойство("result_date") Тогда ДатаВыдачи = Данные.result_date; КонецЕсли;
            Если Данные.Свойство("test_results") Тогда Результаты = Данные.test_results; КонецЕсли;
        КонецЕсли;
        
        // 4. Формируем HTML (здесь упрощенный вариант для теста)
        // В реальной версии: HTML = ПолучитьШаблонHTML();
        
        // 5. Заполняем HTML данными (логика заполнения)
        // Например: HTML = СтрЗаменить(HTML, "[f7bece55-cafa-11e5-9bc7-50af732359f4]", ИмяПациента);
        
        // 6. Возвращаем успех
        ТекстОтвета = "{""status"":""ok"",""patient"":""" + ИмяПациента + """,""tests"":" + Формат(?(Результаты = Неопределено, 0, Результаты.Количество()), "ЧГ=0") + "}";
        Ответ.УстановитьТелоИзСтроки(ТекстОтвета);
        
    Исключение
        Ответ.КодСостояния = 500;
        ОписаниеОшибки = СтрЗаменить(ОписаниеОшибки(), """", """""");
        Ответ.УстановитьТелоИзСтроки("{""error"":""" + ОписаниеОшибки + """}");
    КонецПопытки;
    
    Возврат Ответ;
КонецФункции


Функция ОбработкаGET(Запрос)
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.УстановитьТелоИзСтроки("Lab Integration Service OK");
    Возврат Ответ;
КонецФункции
```

### 1.4. Опубликуй

- Сохрани модуль
- Обнови конфигурацию базы данных
- Опубликуй HTTP-сервис

---

## 📋 ШАГ 2: ТЕСТИРОВАНИЕ С СЕРВЕРА

Зайди на сервер через SSH:

```bash
ssh root@185.247.185.145
```

### 2.1. Тест парсера PDF

```bash
# Получи токен
TOKEN=$(curl -s -X POST https://lis.it-mydoc.ru/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin"}' | jq -r '.access_token')

# Тест парсера
curl -X POST "https://lis.it-mydoc.ru/api/test-pdf-parser?file_path=/data/test-sample.pdf" \
  -H "Authorization: Bearer $TOKEN" | jq
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "file_path": "/data/test-sample.pdf",
  "parsed_data": {
    "patient_name": "Климова С А",
    "age": "",
    "gender": "Женский",
    "result_date": "17.11.2025",
    "sample_date": "17.11.2025",
    "test_results": {
      "8bf99265-9580-11f0-a7be-eca0f0014d7d": "6.61"
    }
  }
}
```

### 2.2. Тест соединения с 1С

```bash
# Тест соединения
curl -X POST "https://lis.it-mydoc.ru/api/test-1c-connection" \
  -H "Authorization: Bearer $TOKEN" | jq
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "status_code": 200,
  "response": "{\"status\":\"ok\",\"message\":\"Test OK\"}"
}
```

### 2.3. Тест полной цепочки (PDF → Parse → 1C)

```bash
# Полная цепочка
curl -X POST "https://lis.it-mydoc.ru/api/test-full-chain?file_path=/data/test-sample.pdf" \
  -H "Authorization: Bearer $TOKEN" | jq
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "file_path": "/data/test-sample.pdf",
  "parsed_data": {
    "patient_name": "Климова С А",
    "gender": "Женский",
    "result_date": "17.11.2025",
    "test_results": {...}
  },
  "send_to_1c_result": {
    "success": true,
    "status_code": 200,
    "response": {"status": "ok", "patient": "Климова С А", "tests": 1}
  }
}
```

---

## 📋 ШАГ 3: ТЕСТИРОВАНИЕ НАПРЯМУЮ С 1С (через Curl)

### Из офиса (на компьютере где доступен 1С):

```bash
# Тест 1: Проверка /test
curl -u "Администратор:1234" http://192.168.100.234/lab/test

# Тест 2: Отправка данных в fillTemplate
curl -u "Администратор:1234" -X POST http://192.168.100.234/lab/fillTemplate \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Климова С А",
    "age": "43",
    "gender": "Женский",
    "sample_date": "17.11.2025",
    "result_date": "17.11.2025",
    "test_results": {
      "8bf99265-9580-11f0-a7be-eca0f0014d7d": "6.61"
    }
  }'
```

---

## 🐛 TROUBLESHOOTING

### Ошибка: "Connection refused"
- Проверь что OpenVPN работает: `systemctl status openvpn@lis-server`
- Проверь маршруты: `ip route | grep 192.168.100`

### Ошибка: 401 Unauthorized от 1С
- Убедись что в настройках URL-шаблонов аутентификация СНЯТА
- Проверь что HTTP-сервис опубликован

### Ошибка: "File not found"
- Проверь что PDF файл существует: `ls -la /data/test-sample.pdf`

### Ошибка парсинга PDF
- Проверь структуру PDF: `docker compose exec app python /app/test_pdf_debug.py`

---

## 📊 СТРУКТУРА ДАННЫХ

### Формат JSON для 1С:

```json
{
  "patient_name": "Иванов И.И.",
  "age": "45",
  "gender": "Мужской",
  "sample_date": "17.11.2025",
  "result_date": "17.11.2025",
  "test_results": {
    "ae584a2f-957f-11f0-a7be-eca0f0014d7d": "0.31",  // АЛАТ
    "d284601d-957f-11f0-a7be-eca0f0014d7d": "0.45",  // АСАТ
    "e5e808bc-957f-11f0-a7be-eca0f0014d7d": "0.31",  // Холестерин
    "8bf99265-9580-11f0-a7be-eca0f0014d7d": "6.61"   // Глюкоза
    // ... остальные показатели
  }
}
```

### ID полей HTML шаблона:

- **АЛАТ**: `ae584a2f-957f-11f0-a7be-eca0f0014d7d`
- **АСАТ**: `d284601d-957f-11f0-a7be-eca0f0014d7d`
- **Холестерин**: `e5e808bc-957f-11f0-a7be-eca0f0014d7d`
- **Гамма-ГТ**: `ef9d787d-957f-11f0-a7be-eca0f0014d7d`
- **Щелочн. фосф-за**: `0190f459-9580-11f0-a7be-eca0f0014d7d`
- **Триглицериды**: `0bf4b4f9-9580-11f0-a7be-eca0f0014d7d`
- **ЛПВП**: `19dd5baa-9580-11f0-a7be-eca0f0014d7d`
- **ЛПНП**: `3b7519c0-9580-11f0-a7be-eca0f0014d7d`
- **ЛДГ**: `43f2ce34-9580-11f0-a7be-eca0f0014d7d`
- **Мочевина**: `4b6251cb-9580-11f0-a7be-eca0f0014d7d`
- **Альфа-Амилаза**: `562a56ba-9580-11f0-a7be-eca0f0014d7d`
- **Билирубин общий**: `62c0a29c-9580-11f0-a7be-eca0f0014d7d`
- **Билирубин прямой**: `681a2d53-9580-11f0-a7be-eca0f0014d7d`
- **Кальций**: `740e37da-9580-11f0-a7be-eca0f0014d7d`
- **Креатинин**: `8007eafe-9580-11f0-a7be-eca0f0014d7d`
- **Глюкоза**: `8bf99265-9580-11f0-a7be-eca0f0014d7d`
- **Общий белок**: `97ee2791-9580-11f0-a7be-eca0f0014d7d`
- **Железо**: `9decbf26-9580-11f0-a7be-eca0f0014d7d`

---

## ✅ CHECKLIST

- [ ] HTTP-сервис создан в 1С
- [ ] URL-шаблоны созданы (test, fillTemplate)
- [ ] Код модуля скопирован
- [ ] HTTP-сервис опубликован
- [ ] Тест парсера PDF прошел
- [ ] Тест соединения с 1С прошел
- [ ] Тест полной цепочки прошел
- [ ] Данные успешно отправляются в 1С

---

**СЛЕДУЮЩИЙ ШАГ**: После успешных тестов нужно добавить логику сохранения HTML в 1С (в функцию `fillTemplatePOST`).

