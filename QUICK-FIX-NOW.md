# ⚡ БЫСТРОЕ ИСПРАВЛЕНИЕ (ПРЯМО СЕЙЧАС!)

## 🎯 Суть проблемы в одном предложении

**Параметры НЕ хранятся в документе - они хранятся в регистре `ЗначенияПараметровHTML`, и нужно писать ТУДА напрямую!**

---

## ✅ Что делать (3 шага):

### Шаг 1: Открой файл с правильным кодом
```bash
/opt/lis-md/1C-CREATE-APPOINTMENT-CORRECT.md
```

### Шаг 2: Скопируй ВСЕ функции в HTTP-сервис 1С
В конфигураторе:
- HTTP-сервисы → `LabIntegration`
- Метод `createAppointment`
- **ПОЛНОСТЬЮ ЗАМЕНИ** старый код на новый

### Шаг 3: Обнови конфигурацию и протестируй
- Ctrl+F7 (Обновить конфигурацию БД)
- Запусти curl-тест (см. ниже)
- Проверь печатную форму

---

## 🔑 Ключевые изменения

### ❌ УДАЛЕНО (не работало):
```1c
// Вся эта форменная механика УДАЛЕНА:
ТаблицаПараметров = Новый ТаблицаЗначений;
Прием.ДополнительныеСвойства.Вставить("ПараметрыПриема", ...);
РаботаСШаблонамиHTML.ЗаписатьЗначенияПараметровДокумента(...);
```

### ✅ ДОБАВЛЕНО (работает):
```1c
// Прямая запись в регистр:
Набор = РегистрыСведений.ЗначенияПараметровHTML.СоздатьНаборЗаписей();
Набор.Отбор.Документ.Установить(СсылкаДокумента);
Набор.Прочитать();
Набор.Очистить();

// Заполняем параметры по GUID:
НоваяЗапись = Набор.Добавить();
НоваяЗапись.Документ = СсылкаДокумента;
НоваяЗапись.ИндексШаблона = 0;
НоваяЗапись.Параметр = НайтиПараметрПоGUID(guid);  // ← Поиск по GUID!
НоваяЗапись.Значение = "25.5";

Набор.Записать();  // ← Записываем в регистр!
```

---

## 🧪 Тест (скопируй и запусти):

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/createAppointment \
  -u "Администратор:1234" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Тестов Тест Тестович",
    "age": "35",
    "gender": "Мужской",
    "sample_date": "20.11.2025",
    "result_date": "20.11.2025",
    "test_results": [
      {"parameter_id": "ae584a2f-957f-11f0-a7be-eca0f0014d7d", "value": "25.5"},
      {"parameter_id": "8bf99265-9580-11f0-a7be-eca0f0014d7d", "value": "5.2"}
    ]
  }'
```

**Что должно быть в ответе:**
```json
{
  "parameters_filled": 2  ← НЕ 0, А 2!
}
```

---

## ✅ Проверка результата

### 1. Проверь регистр (в консоли запросов 1С):
```1c
ВЫБРАТЬ
    ЗначенияПараметровHTML.Документ,
    ЗначенияПараметровHTML.Параметр.Наименование КАК ИмяПараметра,
    ЗначенияПараметровHTML.Значение
ИЗ
    РегистрСведений.ЗначенияПараметровHTML КАК ЗначенияПараметровHTML
УПОРЯДОЧИТЬ ПО
    ЗначенияПараметровHTML.Документ.Дата УБЫВ
```

**Должны увидеть:**
```
Документ          | ИмяПараметра | Значение
Прием.000000001  | АЛАТ         | 25.5
Прием.000000001  | Глюкоза      | 5.2
```

### 2. Проверь печатную форму:
- Открой созданный документ
- Печать → выберите шаблон
- **Параметры должны подставиться!** 🎉

---

## 🚨 Если не работает

### parameters_filled = 0
**Проблема:** GUID не совпадают

**Решение:** Выполни запрос и сравни GUID:
```1c
ВЫБРАТЬ
    ПараметрыHTML.Наименование,
    ПараметрыHTML.Ссылка.УникальныйИдентификатор() КАК GUID
ИЗ
    ПланВидовХарактеристик.ПараметрыHTML КАК ПараметрыHTML
```

Сравни эти GUID с теми, что в JSON и в HTML шаблоне!

---

## 📁 Полная документация:
→ `/opt/lis-md/1C-CREATE-APPOINTMENT-CORRECT.md`

---

## 🎯 Итого (что изменилось):

| Старый подход | Новый подход |
|---------------|--------------|
| ❌ ТаблицаПараметров (форма) | ✅ Регистр ЗначенияПараметровHTML (напрямую) |
| ❌ ДополнительныеСвойства | ✅ НаборЗаписей.Добавить() |
| ❌ Поиск по .Код | ✅ Поиск по УникальныйИдентификатор() |
| ❌ РаботаСШаблонамиHTML.Записать... | ✅ Набор.Записать() |

**Результат:** Параметры РЕАЛЬНО попадают в регистр и отображаются в печатной форме! 🚀

