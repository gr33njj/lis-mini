# ЛИС МД - Система управления лабораторными результатами

Автоматизированная система для загрузки и рассылки лабораторных результатов для 1С:Управление медицинским центром (Первый Бит).

## 🚀 Быстрый старт

### 1. Настройка сервера

```bash
# Скопировать файлы проекта на сервер
scp -r /path/to/lis-md root@185.247.185.145:/opt/

# Подключиться к серверу
ssh root@185.247.185.145

# Запустить скрипт настройки
cd /opt/lis-md
chmod +x setup-server.sh
sudo ./setup-server.sh
```

### 2. Настройка монтирования NAS

Добавить в `/etc/fstab`:
```
//192.168.100.177/laba /mnt/nas cifs username=YOUR_USER,password=YOUR_PASSWORD,vers=3.0,iocharset=utf8,file_mode=0777,dir_mode=0777 0 0
```

Монтировать:
```bash
mount -a
```

### 3. Настройка переменных окружения

Создать `.env` файл:
```bash
cp .env.template .env
nano .env
```

Заполнить настройки:
- `API_1C_URL` - URL API 1С сервера (через OpenVPN туннель)
- `API_1C_TOKEN` - токен авторизации для API 1С
- `SMTP_*` - настройки email сервера
- `SECRET_KEY` - секретный ключ для JWT токенов

### 4. Запуск системы

```bash
# Запустить контейнеры
sudo systemctl start lis-md

# Проверить статус
sudo systemctl status lis-md

# Посмотреть логи
sudo docker-compose logs -f app
```

### 5. Получение SSL сертификата

```bash
sudo certbot --nginx -d lis.it-mydoc.ru
```

## 📋 Функциональность

### Модули системы

- **Watcher** - мониторинг NAS для новых PDF файлов
- **Integrator** - отправка результатов в 1С через HTTP API
- **Mailer** - рассылка результатов пациентам по email
- **Web GUI** - веб-интерфейс управления и мониторинга
- **Audit** - система логирования всех операций

### Веб-интерфейс

Доступен по адресу: http://185.247.185.145:8000/login (или https://lis.it-mydoc.ru после настройки SSL)

**Для тестирования:**
- **Тест аутентификации**: http://185.247.185.145:8000/test-auth.html
- **API документация**: http://185.247.185.145:8000/docs

Функции:
- Просмотр статистики обработки файлов
- Список всех записей с фильтрацией по статусу
- Журнал аудита операций
- Повторная обработка неудачных файлов
- Повторная отправка email уведомлений

## 🔧 Структура проекта

```
/opt/lis-md/
├── app/                    # FastAPI приложение
│   ├── main.py            # Основное приложение
│   ├── watcher.py         # Мониторинг файлов
│   ├── integrator.py      # Интеграция с 1С
│   ├── mailer.py          # Отправка email
│   ├── auth.py            # Аутентификация
│   ├── database.py        # Модели данных
│   ├── config.py          # Конфигурация
│   ├── requirements.txt    # Зависимости Python
│   ├── Dockerfile         # Контейнер приложения
│   └── templates/         # HTML шаблоны
├── nginx/                 # Конфигурация Nginx
│   ├── nginx.conf
│   └── conf.d/
├── docker-compose.yml     # Оркестрация контейнеров
├── setup-server.sh        # Скрипт настройки сервера
└── .env.template         # Шаблон настроек
```

## 🔒 Безопасность

- HTTPS с сертификатами Let's Encrypt
- Аутентификация пользователей с ролями (администратор/оператор)
- JWT токены с ограниченным временем жизни
- Логирование без хранения персональных данных
- Контейнеризация с непривилегированным пользователем

## 📊 Мониторинг

Система автоматически:
- Мониторит появление новых PDF файлов на NAS
- Отправляет результаты в 1С через HTTP API
- Перемещает обработанные файлы в архив
- Сохраняет неудачные файлы в карантин с логами ошибок
- Ведет полный аудит всех операций
- Автоматически удаляет архивы старше периода хранения (90 дней по умолчанию)

## 🛠 Технический стек

- **Backend**: Python 3.12 + FastAPI
- **Frontend**: Tailwind CSS + HTMX
- **База данных**: SQLite
- **Контейнеризация**: Docker + Docker Compose
- **Веб-сервер**: Nginx с SSL
- **Мониторинг**: Встроенная система логирования

## 📞 Поддержка

По вопросам настройки и эксплуатации обращайтесь к документации или администратору системы.
