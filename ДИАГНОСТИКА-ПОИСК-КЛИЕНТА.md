# 🔍 Диагностика: Почему не находит клиента

## 🚨 Возможные проблемы

### Проблема 1: Неправильное название поля ДатаРождения

**В коде используется:**
```1c
Клиенты.ДатаРождения = &ДатаРождения
```

**Возможные варианты в вашей конфигурации:**
- `ДатаРождения`
- `ДатРождения`
- `Дата`
- `БирthDate`

**КАК ПРОВЕРИТЬ:**
1. Открыть 1С Конфигуратор
2. Справочники → Клиенты → Реквизиты
3. Посмотреть ТОЧНОЕ название поля с датой рождения

**КАК ИСПРАВИТЬ:**
```1c
// Замените в запросе:
Клиенты.ДатаРождения = &ДатаРождения
// На правильное название из вашей конфигурации
```

---

### Проблема 2: Точное совпадение ФИО (пробелы, регистр)

**В коде:**
```1c
Клиенты.Наименование = &Наименование  // Точное совпадение!
```

**Проблема:**
- `"Иванов Иван  Иванович"` (2 пробела) ≠ `"Иванов Иван Иванович"` (1 пробел)
- `"Иванов Иван Иванович "` (пробел в конце) ≠ `"Иванов Иван Иванович"`

**РЕШЕНИЕ:** Улучшенный код с ПОДОБНО и очисткой пробелов

```1c
Функция НайтиПациента(ИмяПолное, ДатаРождения)
    
    Попытка
        // ❗ ОЧИЩАЕМ ФИО от лишних пробелов и приводим к нормальному виду
        ИмяОчищенное = СокрЛП(ИмяПолное);  // Убираем пробелы с краёв
        
        // Убираем двойные пробелы
        Пока СтрНайти(ИмяОчищенное, "  ") > 0 Цикл
            ИмяОчищенное = СтрЗаменить(ИмяОчищенное, "  ", " ");
        КонецЦикла;
        
        // Конвертируем строку даты в формат даты 1С
        ДатаРожденияДата = Неопределено;
        
        Если НЕ ПустаяСтрока(ДатаРождения) Тогда
            // Парсим дату "21.05.1982"
            ЧастиДаты = СтрРазделить(ДатаРождения, ".", Ложь);
            Если ЧастиДаты.Количество() = 3 Тогда
                Попытка
                    День = Число(ЧастиДаты[0]);
                    Месяц = Число(ЧастиДаты[1]);
                    Год = Число(ЧастиДаты[2]);
                    ДатаРожденияДата = Дата(Год, Месяц, День);
                Исключение
                    // Неверный формат даты
                    ДатаРожденияДата = Неопределено;
                КонецПопытки;
            КонецЕсли;
        КонецЕсли;
        
        // Поиск по ФИО + Дата рождения
        Запрос = Новый Запрос;
        
        Если ДатаРожденияДата = Неопределено Тогда
            // Поиск только по ФИО (менее надёжно!)
            Запрос.Текст = 
            "ВЫБРАТЬ ПЕРВЫЕ 1
            |    Клиенты.Ссылка КАК Ссылка,
            |    Клиенты.Наименование КАК Наименование
            |ИЗ
            |    Справочник.Клиенты КАК Клиенты
            |ГДЕ
            |    Клиенты.Наименование ПОДОБНО &Наименование
            |    И НЕ Клиенты.ПометкаУдаления";
            
            Запрос.УстановитьПараметр("Наименование", "%" + ИмяОчищенное + "%");
        Иначе
            // ❗ ВАЖНО: Замените ДатаРождения на правильное название поля!
            Запрос.Текст = 
            "ВЫБРАТЬ ПЕРВЫЕ 1
            |    Клиенты.Ссылка КАК Ссылка,
            |    Клиенты.Наименование КАК Наименование,
            |    Клиенты.ДатаРождения КАК ДатаРождения
            |ИЗ
            |    Справочник.Клиенты КАК Клиенты
            |ГДЕ
            |    Клиенты.Наименование ПОДОБНО &Наименование
            |    И Клиенты.ДатаРождения = &ДатаРождения
            |    И НЕ Клиенты.ПометкаУдаления";
            
            Запрос.УстановитьПараметр("Наименование", "%" + ИмяОчищенное + "%");
            Запрос.УстановитьПараметр("ДатаРождения", ДатаРожденияДата);
        КонецЕсли;
        
        Результат = Запрос.Выполнить();
        
        Если НЕ Результат.Пустой() Тогда
            Выборка = Результат.Выбрать();
            Выборка.Следующий();
            Возврат Выборка.Ссылка;
        Иначе
            // ❗ НЕ создаём пациента, возвращаем Неопределено!
            Возврат Неопределено;
        КонецЕсли;
        
    Исключение
        // Логируем ошибку для отладки
        ЗаписьЖурналаРегистрации(
            "ЛИС.ПоискПациента",
            УровеньЛогаСобытий.Ошибка,
            ,
            ,
            "Ошибка поиска: " + ИмяПолное + " / " + ДатаРождения + ": " + ОписаниеОшибки()
        );
        Возврат Неопределено;
    КонецПопытки;
    
КонецФункции
```

---

### Проблема 3: Отправка лишних полей из Python

**БЫЛО:**
```json
{
  "patient_name": "...",
  "birth_date": "...",
  "age": "...",
  "gender": "...",          ← ИЗ PDF (не нужно!)
  "sample_date": "...",     ← ИЗ PDF (не нужно!)
  "result_date": "...",     ← ИЗ PDF (не нужно!)
  "test_results": [...]
}
```

**СТАЛО (исправлено):**
```json
{
  "patient_name": "...",    ← ИЗ ИМЕНИ ФАЙЛА
  "birth_date": "...",      ← ИЗ ИМЕНИ ФАЙЛА
  "age": "...",             ← ВЫЧИСЛЕНО
  "test_results": [...]     ← ИЗ PDF
}
```

---

## 🧪 Как диагностировать

### Шаг 1: Проверить данные в 1С

Выполните запрос в консоли 1С:

```1c
ВЫБРАТЬ
    Клиенты.Ссылка,
    Клиенты.Наименование,
    Клиенты.ДатаРождения  // ← Может называться по-другому!
ИЗ
    Справочник.Клиенты КАК Клиенты
ГДЕ
    Клиенты.Наименование ПОДОБНО "%Иванов%"
    И НЕ Клиенты.ПометкаУдаления
```

**Смотрим:**
1. Как точно записано ФИО в базе
2. Какая дата рождения (формат!)
3. Есть ли лишние пробелы

---

### Шаг 2: Добавить отладочный вывод в 1С

```1c
Функция createAppointmentPOST(Запрос)
    
    Попытка
        // ... читаем JSON
        
        ИмяПациента = ДанныеJSON["patient_name"];
        ДатаРождения = ДанныеJSON["birth_date"];
        
        // ❗ ОТЛАДКА: Логируем что пришло
        ЗаписьЖурналаРегистрации(
            "ЛИС.Отладка",
            УровеньЛогаСобытий.Информация,
            ,
            ,
            "Поиск: ФИО=""" + ИмяПациента + """, Дата=""" + ДатаРождения + """"
        );
        
        Пациент = НайтиПациента(ИмяПациента, ДатаРождения);
        
        // ❗ ОТЛАДКА: Логируем результат поиска
        Если Пациент = Неопределено Тогда
            ЗаписьЖурналаРегистрации(
                "ЛИС.Отладка",
                УровеньЛогаСобытий.Предупреждение,
                ,
                ,
                "Пациент НЕ НАЙДЕН: """ + ИмяПациента + """ / """ + ДатаРождения + """"
            );
        Иначе
            ЗаписьЖурналаРегистрации(
                "ЛИС.Отладка",
                УровеньЛогаСобытий.Информация,
                ,
                Пациент,
                "Пациент НАЙДЕН: " + Строка(Пациент)
            );
        КонецЕсли;
        
        // ... остальной код
```

---

### Шаг 3: Проверить что приходит из Python

В логах Docker посмотрите что отправляется:

```bash
docker compose logs -f app | grep -A 20 "Отправка в 1С"
```

Должно быть:
```json
{
  "patient_name": "Иванов Иван Иванович",
  "birth_date": "21.05.1982",
  "age": "42",
  "test_results": {...}
}
```

**НЕ должно быть:**
```json
{
  "gender": "...",
  "sample_date": "...",
  "result_date": "..."
}
```

---

## ✅ Чеклист диагностики

- [ ] Проверено название поля даты рождения в справочнике Клиенты
- [ ] ФИО в файле точно совпадает с ФИО в 1С (без лишних пробелов)
- [ ] Дата рождения в формате ДД.ММ.ГГГГ
- [ ] В 1С добавлено логирование для отладки
- [ ] Проверены логи журнала регистрации 1С
- [ ] Проверены логи Python (docker compose logs)
- [ ] Код 1С обновлён с улучшенным поиском (ПОДОБНО вместо =)
- [ ] Из Python НЕ отправляются лишние поля (gender, sample_date, result_date)

---

## 📋 Возможные названия поля даты в справочнике

В разных конфигурациях может быть:

| Название в коде | Описание |
|----------------|----------|
| `ДатаРождения` | Стандартное |
| `ДатРождения` | Сокращённое |
| `Дата` | Может конфликтовать! |
| `ДатаРожд` | Сокращённое |
| `BirthDate` | Английское |

**Как узнать точно:**
1. Конфигуратор → Справочники → Клиенты → Реквизиты
2. Или выполнить запрос:
```1c
ВЫБРАТЬ
    *
ИЗ
    Справочник.Клиенты
ГДЕ
    Ссылка = &Ссылка  // Подставьте ссылку на любого клиента
```

---

## 🔧 Быстрое решение

Если не хотите разбираться - используйте поиск **только по ФИО без даты**:

```1c
Функция НайтиПациентаТолькоПоФИО(ИмяПолное)
    
    Попытка
        // Очищаем ФИО
        ИмяОчищенное = СокрЛП(ИмяПолное);
        Пока СтрНайти(ИмяОчищенное, "  ") > 0 Цикл
            ИмяОчищенное = СтрЗаменить(ИмяОчищенное, "  ", " ");
        КонецЦикла;
        
        Запрос = Новый Запрос;
        Запрос.Текст = 
        "ВЫБРАТЬ ПЕРВЫЕ 1
        |    Клиенты.Ссылка КАК Ссылка
        |ИЗ
        |    Справочник.Клиенты КАК Клиенты
        |ГДЕ
        |    Клиенты.Наименование ПОДОБНО &Наименование
        |    И НЕ Клиенты.ПометкаУдаления
        |УПОРЯДОЧИТЬ ПО
        |    Клиенты.Наименование";
        
        Запрос.УстановитьПараметр("Наименование", "%" + ИмяОчищенное + "%");
        
        Результат = Запрос.Выполнить();
        
        Если НЕ Результат.Пустой() Тогда
            Выборка = Результат.Выбрать();
            Выборка.Следующий();
            Возврат Выборка.Ссылка;
        Иначе
            Возврат Неопределено;
        КонецЕсли;
        
    Исключение
        Возврат Неопределено;
    КонецПопытки;
    
КонецФункции
```

**Минусы:**
- ❌ Не защищает от дубликатов полностью
- ❌ Если 2 человека с одинаковым ФИО - найдёт первого

**Плюсы:**
- ✅ Работает всегда
- ✅ Не зависит от названия поля даты
- ✅ Устойчив к лишним пробелам

---

**Версия:** 2.0  
**Дата:** 21.11.2025  
**Статус:** Диагностический документ

