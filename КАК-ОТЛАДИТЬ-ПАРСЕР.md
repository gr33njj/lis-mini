# 🔍 КАК ОТЛАДИТЬ ПАРСЕР PDF

## 🎯 Проблема

PDF файл не парсится, не извлекаются результаты анализов.

## ✅ Решение

Добавлено **детальное логирование** в парсер для отладки.

---

## 🧪 ШАГ 1: Найдите ваш PDF файл

```bash
# Поиск на NAS
find /mnt/nas -name "*Глухоньчук*" -type f

# Или посмотрите содержимое папки
ls -lah /mnt/nas/lab_results/
```

**Ваш файл:** `Глухоньчук Данил Игоревич 04.11.1999.pdf`

---

## 🧪 ШАГ 2: Запустите тестовый скрипт

```bash
cd /root/.cursor/worktrees/lis-md__SSH__lis-md_/UC0hJ

# Запустите тест (замените путь на реальный!)
python test_pdf_parser.py "/mnt/nas/lab_results/Глухоньчук Данил Игоревич 04.11.1999.pdf"
```

---

## 📊 ЧТО ПОКАЖЕТ СКРИПТ

### 1. Извлечение данных из имени файла

```
📝 Шаг 1: Извлечение данных из имени файла
✅ ФИО: Глухоньчук Данил Игоревич
✅ Дата рождения: 04.11.1999
✅ Возраст: 25
```

### 2. Извлечение текста из PDF

```
📊 Шаг 2: Извлечение результатов анализов из PDF
Страниц в PDF: 1

--- Страница 1 ---
Текст (первые 500 символов):
ООО "Мой Доктор" Клинико - диагностическая лаборатория
...
```

### 3. Парсинг таблиц

```
DEBUG: Найдено таблиц: 1

DEBUG: Таблица 1, строк: 19

DEBUG: Строка 0: ['Хим', 'Резул', 'Ед из', 'Реф диап']
DEBUG: Пропуск заголовка

DEBUG: Строка 1: ['1.', 'АЛАТ', '', 'Ед/л', '0-31']
DEBUG: Найдено название: 'АЛАТ' в колонке 1
⚠️ DEBUG: Не удалось извлечь данные из строки

DEBUG: Строка 2: ['2.', 'АСАТ', '', 'Ед/л', '0-31']
DEBUG: Найдено название: 'АСАТ' в колонке 1
⚠️ DEBUG: Не удалось извлечь данные из строки

DEBUG: Строка 3: ['3.', 'Холестерин', '0.31', 'ммоль/л', '0-5.2']
DEBUG: Найдено название: 'Холестерин' в колонке 1
DEBUG: Найдено значение: '0.31' в колонке 2
DEBUG: Нормализованное название: 'Холестерин'
✅ DEBUG: Сохранено: Холестерин = 0.31 (ID: e5e808bc-957f-11f0-a7be-eca0f0014d7d)

...
```

### 4. Итоговая статистика

```
📈 ИТОГО: Извлечено 15 показателей

📊 РЕЗУЛЬТАТ ПАРСИНГА:
{
  "patient_name": "Глухоньчук Данил Игоревич",
  "birth_date": "04.11.1999",
  "age": "25",
  "test_results": {
    "e5e808bc-957f-11f0-a7be-eca0f0014d7d": "0.31",
    "ef9d787d-957f-11f0-a7be-eca0f0014d7d": "0.0",
    ...
  }
}
```

---

## 🔍 ЧТО СМОТРЕТЬ В ЛОГАХ

### ✅ ХОРОШО (всё работает):

```
DEBUG: Найдено название: 'Холестерин' в колонке 1
DEBUG: Найдено значение: '0.31' в колонке 2
✅ DEBUG: Сохранено: Холестерин = 0.31
```

### ⚠️ ПРОБЛЕМА 1: Название найдено, но НЕТ значения

```
DEBUG: Найдено название: 'АЛАТ' в колонке 1
⚠️ DEBUG: Не удалось извлечь данные из строки
```

**Причина:** В этой строке нет значения (пустая ячейка в PDF).

**Решение:** Это нормально, анализ не сделан.

---

### ⚠️ ПРОБЛЕМА 2: Показатель НЕ найден в маппинге

```
DEBUG: Найдено название: 'Какой-то показатель'
DEBUG: Найдено значение: '123.45'
⚠️ DEBUG: Показатель 'Какой-то показатель' НЕ найден в маппинге
   Доступные ключи: ['АЛАТ', 'АСАТ', 'Холестерин', ...]
```

**Причина:** Название показателя в PDF написано по-другому, чем в маппинге.

**Решение:** Добавьте этот вариант названия в `FIELD_MAPPING` в `app/pdf_parser.py`:

```python
FIELD_MAPPING = {
    # ...
    "Какой-то показатель": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
    # ...
}
```

---

### ⚠️ ПРОБЛЕМА 3: Таблицы НЕ НАЙДЕНО

```
Таблицы: НЕ НАЙДЕНО
```

**Причина:** PDF не содержит таблиц (возможно, это скан или изображение).

**Решение:** Нужно использовать OCR или другой метод парсинга.

---

## 🛠️ КАК ИСПРАВИТЬ ПАРСЕР

### Если нужно добавить новый показатель:

1. Откройте: `app/pdf_parser.py`
2. Найдите: `FIELD_MAPPING = {`
3. Добавьте новый показатель:

```python
FIELD_MAPPING = {
    # Существующие...
    "АЛАТ": "ae584a2f-957f-11f0-a7be-eca0f0014d7d",
    
    # НОВЫЙ ПОКАЗАТЕЛЬ:
    "Новое название": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
}
```

4. Сохраните файл
5. Перезапустите тест

---

### Если структура таблицы другая:

Смотрите логи `DEBUG: Строка N: [...]`

Например:
```
DEBUG: Строка 3: ['3.', 'Холестерин', '0.31', 'ммоль/л', '0-5.2']
```

Это означает:
- Колонка 0: `'3.'` (номер строки)
- Колонка 1: `'Холестерин'` (название)
- Колонка 2: `'0.31'` (значение) ← МЫ ЭТО БЕРЁМ
- Колонка 3: `'ммоль/л'` (единицы)
- Колонка 4: `'0-5.2'` (референс)

Парсер автоматически ищет **название** (кириллица) и **значение** (число).

---

## 📞 ЧТО ДЕЛАТЬ ДАЛЬШЕ

### Если всё работает (извлечено > 0 показателей):

```bash
# Перезапустите Docker контейнеры
cd /root/.cursor/worktrees/lis-md__SSH__lis-md_/UC0hJ
docker compose down
docker compose build --no-cache app
docker compose up -d

# Проверьте логи
docker compose logs -f app
```

Теперь скопируйте файл на NAS и смотрите, как он обработается.

---

### Если НЕ работает (0 показателей):

1. **Пришлите логи из теста** (всю выдачу скрипта)
2. **Укажите:**
   - Сколько показателей должно быть извлечено
   - Какие именно показатели не извлеклись
3. **Если возможно:**
   - Скриншот таблицы из PDF (уже есть ✅)
   - Или текст таблицы

---

## 🧪 БЫСТРЫЙ ТЕСТ (если файла нет)

Можете создать тестовый PDF с текстом:

```bash
cd /root/.cursor/worktrees/lis-md__SSH__lis-md_/UC0hJ

# Создайте простой текстовый файл
cat > test_table.txt <<'EOF'
1. АЛАТ 25.5 Ед/л 0-31
2. АСАТ 44.1 Ед/л 0-31
3. Холестерин 0.31 ммоль/л 0-5.2
4. Глюкоза 2.13 ммоль/л 3.9-6.4
EOF

# (Это не PDF, но можно попробовать с реальным PDF)
```

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После исправления парсера:

```json
{
  "patient_name": "Глухоньчук Данил Игоревич",
  "birth_date": "04.11.1999",
  "age": "25",
  "test_results": {
    "e5e808bc-957f-11f0-a7be-eca0f0014d7d": "0.31",   // Холестерин
    "ef9d787d-957f-11f0-a7be-eca0f0014d7d": "0.0",    // Гамма-ГТ
    "0190f459-9580-11f0-a7be-eca0f0014d7d": "0.0",    // Щелочн. фосф-за
    "0bf4b4f9-9580-11f0-a7be-eca0f0014d7d": "0.04",   // Триглицериды
    "19dd5baa-9580-11f0-a7be-eca0f0014d7d": "0.00",   // ЛПВП
    "3b7519c0-9580-11f0-a7be-eca0f0014d7d": "0.00",   // ЛПНП
    "562a56ba-9580-11f0-a7be-eca0f0014d7d": "0.00",   // альфа-Амилаза
    "62c0a29c-9580-11f0-a7be-eca0f0014d7d": "0.00",   // Билирубин общий
    "681a2d53-9580-11f0-a7be-eca0f0014d7d": "0.77",   // Билирубин прямой
    "740e37da-9580-11f0-a7be-eca0f0014d7d": "0.11",   // Кальций
    "8007eafe-9580-11f0-a7be-eca0f0014d7d": "0.0",    // Креатинин
    "8bf99265-9580-11f0-a7be-eca0f0014d7d": "2.13",   // Глюкоза
    "97ee2791-9580-11f0-a7be-eca0f0014d7d": "71.6"    // Общий белок
  }
}
```

Всего: **13 показателей** (не все анализы были сделаны, АЛАТ и АСАТ пустые).

---

**Запустите тест и пришлите результат!** 🚀

