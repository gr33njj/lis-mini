# 🔧 Исправление DNS для lis.it-mydoc.ru

## 🔍 Проблема

Домен `lis.it-mydoc.ru` не резолвится (NXDOMAIN).

**Обнаружено:**
- Корневой домен: it-mydoc.ru → 85.92.110.173 (работает)
- NS серверы: Beget (ns1.beget.pro, ns2.beget.com и т.д.)
- Поддомен: lis.it-mydoc.ru → не найден ❌

## ✅ Решение

### Вариант 1: Проверить настройки в панели Beget

1. **Войдите в панель управления Beget:**
   - https://cp.beget.com/

2. **Перейдите в раздел "Домены" → "DNS-записи"**

3. **Найдите домен it-mydoc.ru**

4. **Проверьте A-запись для поддомена:**
   ```
   Тип:  A
   Имя:  lis (или lis.it-mydoc.ru)
   Значение: 185.247.185.145
   TTL: 600
   ```

5. **Если записи нет - создайте её:**
   - Нажмите "Добавить запись"
   - Тип: A
   - Имя: lis
   - Значение: 185.247.185.145
   - TTL: 600
   - Сохраните

6. **Подождите 5-10 минут для распространения DNS**

### Вариант 2: Если нет доступа к Beget

Если домен управляется в другом месте:

1. Войдите в панель управления доменом (где покупали it-mydoc.ru)
2. Найдите раздел DNS или "Управление записями"
3. Добавьте A-запись как выше

### Проверка после изменений

Подождите 5-10 минут, затем проверьте:

```bash
# На сервере
dig lis.it-mydoc.ru A +short

# Должно вернуть: 185.247.185.145
```

Или в браузере:
```bash
# В командной строке Windows/Mac
nslookup lis.it-mydoc.ru
```

## 📋 Текущие настройки (которые вы указали)

✅ **A-запись:**
```
Имя:     lis.it-mydoc.ru
Значение: 185.247.185.145
TTL:     600
```

✅ **TXT-запись (SPF):**
```
Имя:     lis.it-mydoc.ru
Значение: v=spf1 include:_spf.timeweb.ru ~all
TTL:     600
```

**Возможная проблема:** 
Если настройки были добавлены 3 недели назад и работали, но сейчас не работают - возможно:
1. DNS запись была случайно удалена
2. Изменились NS-серверы
3. Истёк срок оплаты хостинга/домена

## 🚀 Временное решение

Пока DNS не работает, система доступна по IP:

```
http://185.247.185.145/
```

После исправления DNS сможем получить SSL сертификат:

```bash
cd /opt/lis-md
./setup-ssl.sh
```

## 📞 Поддержка Beget

Если проблема не решается:
- Email: support@beget.ru
- Онлайн-чат в панели управления
- Телефон: +7 (495) 137-73-70

## ✅ После исправления DNS

Когда DNS заработает, выполните:

```bash
cd /opt/lis-md

# Проверка DNS
dig lis.it-mydoc.ru A +short

# Если вернёт 185.247.185.145, можно получать SSL
sudo certbot certonly --standalone -d lis.it-mydoc.ru

# Перезапустить Nginx
docker compose restart nginx
```
