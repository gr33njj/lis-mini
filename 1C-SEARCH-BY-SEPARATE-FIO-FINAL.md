# 🎯 ФИНАЛЬНОЕ РЕШЕНИЕ: Поиск по раздельным полям ФИО

## ✅ Для конфигураций, где НЕТ реквизита Представление

В вашей конфигурации справочник `Клиенты` имеет **раздельные реквизиты**:
- `Фамилия` (строка)
- `Имя` (строка)
- `Отчество` (строка, может быть пустым)
- `ДатаРождения` (дата)

**JSON от ЛИС приходит:**
```json
{
  "patient_name": "Иванов Иван Иванович",
  "birth_date": "21.05.1982"
}
```

**Задача:** Разобрать `patient_name` на части и найти клиента по раздельным полям.

---

## 🔧 Правильная функция НайтиПациента

### Полная реализация:

```1c
Функция НайтиПациента(ИмяПолное, ДатаРождения)
    
    Попытка
        // 1. Нормализуем ФИО (убираем лишние пробелы)
        ФИО = СокрЛП(ИмяПолное);
        
        // Убираем двойные пробелы
        Пока СтрНайти(ФИО, "  ") > 0 Цикл
            ФИО = СтрЗаменить(ФИО, "  ", " ");
        КонецЦикла;
        
        // 2. Разбираем ФИО на части
        Фамилия = "";
        Имя = "";
        Отчество = "";
        
        СписокФИО = СтрРазделить(ФИО, " ", Ложь);
        
        Если СписокФИО.Количество() >= 1 Тогда
            Фамилия = СписокФИО[0];
        КонецЕсли;
        
        Если СписокФИО.Количество() >= 2 Тогда
            Имя = СписокФИО[1];
        КонецЕсли;
        
        Если СписокФИО.Количество() >= 3 Тогда
            Отчество = СписокФИО[2];
        КонецЕсли;
        
        // Логируем разбор ФИО
        ЗаписьЖурналаРегистрации(
            "ЛИС.РазборФИО",
            ,
            ,
            ,
            "Фамилия: '" + Фамилия + "', Имя: '" + Имя + "', Отчество: '" + Отчество + "'"
        );
        
        // 3. Парсим дату рождения
        ДатаРожденияДата = Неопределено;
        
        Если НЕ ПустаяСтрока(ДатаРождения) Тогда
            Попытка
                // Формат даты: "21.05.1982" → Дата(1982, 05, 21)
                Части = СтрРазделить(ДатаРождения, ".", Ложь);
                
                Если Части.Количество() = 3 Тогда
                    День = Число(Части[0]);
                    Месяц = Число(Части[1]);
                    Год = Число(Части[2]);
                    ДатаРожденияДата = Дата(Год, Месяц, День);
                КонецЕсли;
            Исключение
                // Не удалось распарсить дату - логируем, но продолжаем без даты
                ЗаписьЖурналаРегистрации(
                    "ЛИС.ПарсингДаты",
                    ,
                    ,
                    ,
                    "Не удалось распарсить дату: '" + ДатаРождения + "'. Ошибка: " + ОписаниеОшибки()
                );
            КонецПопытки;
        КонецЕсли;
        
        // 4. Формируем запрос
        Запрос = Новый Запрос;
        
        // Базовый текст запроса
        ТекстЗапроса = 
        "ВЫБРАТЬ ПЕРВЫЕ 1
        |    Клиенты.Ссылка КАК Ссылка
        |ИЗ
        |    Справочник.Клиенты КАК Клиенты
        |ГДЕ
        |    Клиенты.Фамилия = &Фамилия
        |    И Клиенты.Имя = &Имя";
        
        Запрос.УстановитьПараметр("Фамилия", Фамилия);
        Запрос.УстановитьПараметр("Имя", Имя);
        
        // Добавляем условие по отчеству (если есть)
        Если НЕ ПустаяСтрока(Отчество) Тогда
            ТекстЗапроса = ТекстЗапроса + "
            |    И Клиенты.Отчество = &Отчество";
            Запрос.УстановитьПараметр("Отчество", Отчество);
        КонецЕсли;
        
        // Добавляем условие по дате рождения (если есть)
        Если ДатаРожденияДата <> Неопределено Тогда
            ТекстЗапроса = ТекстЗапроса + "
            |    И Клиенты.ДатаРождения = &ДатаРождения";
            Запрос.УстановитьПараметр("ДатаРождения", ДатаРожденияДата);
        КонецЕсли;
        
        // Исключаем удалённых
        ТекстЗапроса = ТекстЗапроса + "
        |    И НЕ Клиенты.ПометкаУдаления";
        
        Запрос.Текст = ТекстЗапроса;
        
        // Логируем параметры поиска
        ПараметрыПоиска = "Фамилия: '" + Фамилия + "', Имя: '" + Имя + "'";
        Если НЕ ПустаяСтрока(Отчество) Тогда
            ПараметрыПоиска = ПараметрыПоиска + ", Отчество: '" + Отчество + "'";
        КонецЕсли;
        Если ДатаРожденияДата <> Неопределено Тогда
            ПараметрыПоиска = ПараметрыПоиска + ", Дата рождения: " + Формат(ДатаРожденияДата, "ДФ=dd.MM.yyyy");
        КонецЕсли;
        
        ЗаписьЖурналаРегистрации(
            "ЛИС.ПоискПациента",
            ,
            ,
            ,
            "Начат поиск. " + ПараметрыПоиска
        );
        
        // 5. Выполняем запрос
        Результат = Запрос.Выполнить();
        
        Если НЕ Результат.Пустой() Тогда
            Выборка = Результат.Выбрать();
            Выборка.Следующий();
            
            // Логируем успех
            ЗаписьЖурналаРегистрации(
                "ЛИС.ПоискПациента",
                ,
                ,
                Выборка.Ссылка,
                "✅ НАЙДЕН! " + ПараметрыПоиска
            );
            
            Возврат Выборка.Ссылка;
        Иначе
            // Логируем что не найден
            ЗаписьЖурналаРегистрации(
                "ЛИС.ПоискПациента",
                ,
                ,
                ,
                "❌ НЕ НАЙДЕН! " + ПараметрыПоиска
            );
            
            Возврат Неопределено;
        КонецЕсли;
        
    Исключение
        // Логируем ошибку
        ЗаписьЖурналаРегистрации(
            "ЛИС.ПоискПациента",
            ,
            ,
            ,
            "⚠️ ОШИБКА поиска для '" + ИмяПолное + "' / '" + ДатаРождения + "': " + ОписаниеОшибки()
        );
        
        Возврат Неопределено;
    КонецПопытки;
    
КонецФункции
```

---

## 📊 Как это работает

### Алгоритм (пошагово):

```
1. Входные данные:
   ИмяПолное = "Иванов Иван Иванович"
   ДатаРождения = "21.05.1982"

2. Нормализация:
   ФИО = "Иванов Иван Иванович" (без двойных пробелов)

3. Разбор ФИО:
   Фамилия = "Иванов"
   Имя = "Иван"
   Отчество = "Иванович"

4. Парсинг даты:
   "21.05.1982" → Дата(1982, 05, 21)

5. Формирование запроса:
   SELECT Клиенты.Ссылка
   WHERE Клиенты.Фамилия = "Иванов"
     AND Клиенты.Имя = "Иван"
     AND Клиенты.Отчество = "Иванович"
     AND Клиенты.ДатаРождения = Дата(1982, 05, 21)
     AND НЕ Клиенты.ПометкаУдаления

6. Результат:
   ✅ НАЙДЕН → Возврат Ссылка
   ❌ НЕ НАЙДЕН → Возврат Неопределено
```

---

## 🧪 Тестирование

### Тест 1: Поиск с отчеством и датой (максимальная точность)

```1c
ФИО = "Тестов Тест Тестович";
ДатаРождения = "18.11.1988";

Пациент = НайтиПациента(ФИО, ДатаРождения);

Если Пациент <> Неопределено Тогда
    Сообщить("✅ Пациент найден: " + Строка(Пациент));
Иначе
    Сообщить("❌ Пациент НЕ найден!");
КонецЕсли;
```

**Ожидаемый результат:** `✅ Пациент найден: Клиенты.Тестов Тест Тестович`

**Проверьте журнал регистрации:**
```
ЛИС.РазборФИО: Фамилия: 'Тестов', Имя: 'Тест', Отчество: 'Тестович'
ЛИС.ПоискПациента: Начат поиск. Фамилия: 'Тестов', Имя: 'Тест', Отчество: 'Тестович', Дата рождения: 18.11.1988
ЛИС.ПоискПациента: ✅ НАЙДЕН! ...
```

---

### Тест 2: Поиск БЕЗ отчества

```1c
ФИО = "Петров Петр";  // Без отчества
ДатаРождения = "10.05.1990";

Пациент = НайтиПациента(ФИО, ДатаРождения);

Если Пациент <> Неопределено Тогда
    Сообщить("✅ Пациент найден: " + Строка(Пациент));
Иначе
    Сообщить("❌ Пациент НЕ найден!");
КонецЕсли;
```

**Запрос будет:**
```sql
WHERE Клиенты.Фамилия = "Петров"
  AND Клиенты.Имя = "Петр"
  -- Отчество НЕ проверяется!
  AND Клиенты.ДатаРождения = Дата(1990, 05, 10)
```

---

### Тест 3: Поиск БЕЗ даты рождения

```1c
ФИО = "Сидоров Сидор Сидорович";
ДатаРождения = "";  // Пустая строка

Пациент = НайтиПациента(ФИО, ДатаРождения);

Если Пациент <> Неопределено Тогда
    Сообщить("✅ Пациент найден: " + Строка(Пациент));
Иначе
    Сообщить("❌ Пациент НЕ найден (возможны однофамильцы)!");
КонецЕсли;
```

**Запрос будет:**
```sql
WHERE Клиенты.Фамилия = "Сидоров"
  AND Клиенты.Имя = "Сидор"
  AND Клиенты.Отчество = "Сидорович"
  -- Дата рождения НЕ проверяется!
```

⚠️ **Внимание:** Без даты рождения может найтись не тот пациент, если есть однофамильцы!

---

### Тест 4: Проверка разбора ФИО (в консоли)

```1c
ФИО = "Иванов  Иван   Иванович";  // С лишними пробелами

// Нормализация
ФИО = СокрЛП(ФИО);
Пока СтрНайти(ФИО, "  ") > 0 Цикл
    ФИО = СтрЗаменить(ФИО, "  ", " ");
КонецЦикла;

// Разбор
СписокФИО = СтрРазделить(ФИО, " ", Ложь);

Сообщить("Количество частей: " + Строка(СписокФИО.Количество()));
Сообщить("Фамилия: '" + СписокФИО[0] + "'");
Сообщить("Имя: '" + СписокФИО[1] + "'");
Сообщить("Отчество: '" + СписокФИО[2] + "'");
```

**Ожидаемый результат:**
```
Количество частей: 3
Фамилия: 'Иванов'
Имя: 'Иван'
Отчество: 'Иванович'
```

---

## 🧪 Тестирование через curl (Windows CMD)

### Предварительные требования:

1. В базе 1С должен существовать клиент:
   - **Фамилия:** Тестов
   - **Имя:** Тест
   - **Отчество:** Тестович
   - **Дата рождения:** 18.11.1988

2. Замените в команде:
   - `YOUR_TOKEN` → ваш реальный токен
   - GUID параметров → реальные GUID из базы

---

### Пример 1: Полный запрос (с отчеством и датой)

```cmd
curl -X POST "http://192.168.100.234/med/hs/lis/createAppointment" -H "Authorization: Bearer YOUR_TOKEN" -H "Content-Type: application/json" -d "{\"patient_name\":\"Тестов Тест Тестович\",\"birth_date\":\"18.11.1988\",\"age\":\"36\",\"test_results\":{\"ae584a2f-1234-5678-90ab-cdef12345678\":\"25.5\",\"d284601d-1234-5678-90ab-cdef87654321\":\"44.1\"}}"
```

**Ожидаемый ответ:**
```json
{
  "status": "success",
  "appointment_id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
}
```

---

### Пример 2: Без отчества

```cmd
curl -X POST "http://192.168.100.234/med/hs/lis/createAppointment" -H "Authorization: Bearer YOUR_TOKEN" -H "Content-Type: application/json" -d "{\"patient_name\":\"Петров Петр\",\"birth_date\":\"10.05.1990\",\"age\":\"34\",\"test_results\":{\"ae584a2f-1234-5678-90ab-cdef12345678\":\"30.1\"}}"
```

---

### Пример 3: Basic Auth

```cmd
curl -X POST "http://192.168.100.234/med/hs/lis/createAppointment" -u "username:password" -H "Content-Type: application/json" -d "{\"patient_name\":\"Тестов Тест Тестович\",\"birth_date\":\"18.11.1988\",\"age\":\"36\",\"test_results\":{\"ae584a2f-1234-5678-90ab-cdef12345678\":\"25.5\"}}"
```

---

## ❗ Возможные проблемы и решения

### Проблема 1: Пациент НЕ найден (хотя есть в базе)

**Симптомы:**
```
❌ НЕ НАЙДЕН! Фамилия: 'Тестов', Имя: 'Тест', Отчество: 'Тестович', Дата рождения: 18.11.1988
```

**Причины:**

#### 1. ФИО не совпадает ТОЧНО

Проверьте в базе:
```1c
Запрос = Новый Запрос;
Запрос.Текст = 
"ВЫБРАТЬ
|    Клиенты.Фамилия,
|    Клиенты.Имя,
|    Клиенты.Отчество,
|    Клиенты.ДатаРождения
|ИЗ
|    Справочник.Клиенты КАК Клиенты
|ГДЕ
|    Клиенты.Фамилия ПОДОБНО ""%Тест%""
|    И НЕ Клиенты.ПометкаУдаления";

Результат = Запрос.Выполнить();
Выборка = Результат.Выбрать();

Пока Выборка.Следующий() Цикл
    Сообщить("Фамилия: '" + Выборка.Фамилия + "'");
    Сообщить("Имя: '" + Выборка.Имя + "'");
    Сообщить("Отчество: '" + Выборка.Отчество + "'");
    Сообщить("Дата: " + Формат(Выборка.ДатаРождения, "ДФ=dd.MM.yyyy"));
    Сообщить("---");
КонецЦикла;
```

**Возможные расхождения:**
- Лишние пробелы: `"Тест "` вместо `"Тест"`
- Регистр: `"тест"` вместо `"Тест"` (если поиск регистрозависимый)
- Опечатки: `"Тестовв"` вместо `"Тестов"`

#### 2. Дата рождения не совпадает

**В файле:** `18.11.1988`  
**В базе:** `18.11.1987` (опечатка!)

**Решение:** Исправьте имя файла или дату в базе.

#### 3. Клиент удалён (ПометкаУдаления = Истина)

**Решение:** Снимите пометку удаления в 1С.

---

### Проблема 2: Найден НЕ ТОТ пациент

**Симптомы:**
```
✅ НАЙДЕН! Но это другой человек!
```

**Причина:** Поиск без даты рождения или без отчества нашёл однофамильца.

**Решение:**
- **ВСЕГДА** передавайте дату рождения в имени файла!
- Формат: `Тестов Тест Тестович 18.11.1988.pdf`

---

### Проблема 3: Ошибка парсинга даты

**Симптомы:**
```
ЛИС.ПарсингДаты: Не удалось распарсить дату: '18/11/1988'
```

**Причина:** Неправильный формат даты.

**Правильный формат:** `ДД.ММ.ГГГГ` (точки!)  
**Примеры:**
- ✅ `18.11.1988`
- ✅ `01.01.2000`
- ❌ `18/11/1988` (слэши)
- ❌ `1988-11-18` (ISO формат)

---

### Проблема 4: Двойная фамилия или сложное ФИО

**Пример:** `Петров-Водкин Иван`

**Текущее поведение:**
```
Фамилия = "Петров-Водкин"
Имя = "Иван"
Отчество = ""
```

✅ **Работает корректно!** Функция разбивает только по пробелам, поэтому дефис остаётся в фамилии.

---

## 📋 Важные правила

### 1. Формат имени файла

**ОБЯЗАТЕЛЬНЫЙ:**
```
Фамилия Имя Отчество ДД.ММ.ГГГГ.pdf
```

**Примеры:**
- ✅ `Тестов Тест Тестович 18.11.1988.pdf`
- ✅ `Иванов Иван Иванович 21.05.1982.pdf`
- ✅ `Петров Петр 10.05.1990.pdf` (без отчества - допустимо, но менее точно)
- ❌ `123456.pdf` (нет ФИО!)
- ❌ `Тестов_Тест_Тестович_18.11.1988.pdf` (нижние подчёркивания!)

### 2. Точность поиска

| Условия поиска | Точность | Риск ошибки |
|----------------|----------|-------------|
| Фамилия + Имя + Отчество + Дата | ✅ Максимальная | Минимальный |
| Фамилия + Имя + Дата (без отчества) | ⚠️ Высокая | Низкий |
| Фамилия + Имя + Отчество (без даты) | ⚠️ Средняя | Средний |
| Фамилия + Имя (без отчества и даты) | ❌ Низкая | Высокий |

**Рекомендация:** ВСЕГДА указывайте полное ФИО + дату рождения!

### 3. Регистр букв

Поиск в 1С **НЕ** чувствителен к регистру (по умолчанию).

**Все варианты найдут одно и то же:**
- `Тестов Тест Тестович`
- `ТЕСТОВ ТЕСТ ТЕСТОВИЧ`
- `тестов тест тестович`

---

## 🎯 Пошаговая инструкция внедрения

### Шаг 1: Откройте 1С Конфигуратор

Запустите конфигуратор и перейдите к HTTP-сервису.

### Шаг 2: Найдите функцию `НайтиПациента`

Откройте модуль HTTP-сервиса (или общий модуль) и найдите функцию.

### Шаг 3: Замените код

1. **УДАЛИТЕ** весь старый код функции `НайтиПациента`
2. **СКОПИРУЙТЕ** новый код из этого документа (раздел "Полная реализация")
3. **ВСТАВЬТЕ** в модуль

### Шаг 4: Сохраните и опубликуйте

1. Сохраните изменения в конфигураторе (Ctrl+S)
2. Опубликуйте HTTP-сервис
3. Перезапустите публикацию (если требуется)

### Шаг 5: Создайте тестового пациента

Создайте в справочнике `Клиенты`:
- **Фамилия:** Тестов
- **Имя:** Тест
- **Отчество:** Тестович
- **Дата рождения:** 18.11.1988

### Шаг 6: Протестируйте в консоли 1С

Выполните Тест 1 из раздела "Тестирование".

### Шаг 7: Протестируйте через curl

Выполните команду из раздела "Тестирование через curl".

### Шаг 8: E2E тест с реальным файлом

1. Создайте PDF файл (или просто текстовый):
   ```bash
   echo "Test content" > "Тестов Тест Тестович 18.11.1988.pdf"
   ```

2. Скопируйте на NAS:
   ```bash
   cp "Тестов Тест Тестович 18.11.1988.pdf" /mnt/nas/lab_results/
   ```

3. Смотрите логи:
   ```bash
   docker compose logs -f app
   ```

4. Проверьте журнал 1С:
   - Администрирование → Журнал регистрации
   - Фильтр: `ЛИС`
   - Должны быть записи:
     - `ЛИС.РазборФИО`
     - `ЛИС.ПоискПациента`
     - `✅ НАЙДЕН!`

5. Проверьте приём:
   - Медицинская организация → Документы → Приёмы
   - Должен быть новый приём с врачом "МД Lab"

---

## ✅ Checklist внедрения

- [ ] Конфигуратор 1С открыт
- [ ] Функция `НайтиПациента` найдена
- [ ] Старый код удалён
- [ ] Новый код вставлен
- [ ] Конфигурация сохранена
- [ ] HTTP-сервис опубликован
- [ ] Тестовый пациент создан (Тестов Тест Тестович, 18.11.1988)
- [ ] Тест 1: Консоль 1С ✅
- [ ] Тест 2: curl ✅
- [ ] Тест 3: E2E с файлом ✅
- [ ] Журнал 1С проверен ✅
- [ ] Приём создан ✅

---

## 🚀 Результат

### Полный цикл обработки:

```
1. Файл на NAS:
   "Тестов Тест Тестович 18.11.1988.pdf"

2. Python парсит имя:
   patient_name = "Тестов Тест Тестович"
   birth_date = "18.11.1988"

3. Python отправляет в 1С:
   POST /createAppointment
   {
     "patient_name": "Тестов Тест Тестович",
     "birth_date": "18.11.1988",
     "test_results": {...}
   }

4. 1С разбирает ФИО:
   Фамилия = "Тестов"
   Имя = "Тест"
   Отчество = "Тестович"

5. 1С ищет:
   WHERE Клиенты.Фамилия = "Тестов"
     AND Клиенты.Имя = "Тест"
     AND Клиенты.Отчество = "Тестович"
     AND Клиенты.ДатаРождения = Дата(1988, 11, 18)

6. ✅ НАЙДЕН!
   → Создаёт приём
   → Заполняет HTML параметры
   → Отправляет email
   → Файл в архив

7. ❌ НЕ НАЙДЕН
   → Ошибка 400
   → Файл в карантин
   → Подробное сообщение в логах
```

---

**Версия:** 3.1 FINAL (поиск по раздельным полям)  
**Дата:** 24.11.2025  
**Статус:** ✅ ГОТОВО К ПРОДАКШЕНУ  
**Тестовые данные:** Тестов Тест Тестович, 18.11.1988

