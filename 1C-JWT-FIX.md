# 🔐 Решение проблемы "Цифровая подпись токена доступа недействительна"

## ❌ Ошибка

```xml
<descr>Цифровая подпись токена доступа недействительна</descr>
```

## 🔍 Причина

1С пытается проверить токен как **JWT (JSON Web Token)** с цифровой подписью.

Это происходит потому что в настройках **HTTP-сервиса** в конфигураторе включена встроенная проверка JWT.

Наша простая строка-токен не подходит, нужен настоящий JWT с подписью.

---

## ✅ Решение: Отключить встроенную проверку JWT

### Вариант 1: Изменить настройки HTTP-сервиса (Рекомендуется)

**Шаг 1: Откройте конфигуратор 1С**

1. Запустите 1С в режиме Конфигуратора
2. Конфигурация → Общие → HTTP-сервисы
3. Найдите ваш HTTP-сервис (например, `LabIntegration`)
4. Откройте его

**Шаг 2: Проверьте настройки аутентификации**

В свойствах HTTP-сервиса найдите:

```
Аутентификация: <Не использовать>
```

Или:

```
Аутентификация: OAuth 2.0  ← ПРОБЛЕМА ЗДЕСЬ!
```

**ВАЖНО:** Установите `<Не использовать>` или снимите галочку с JWT/OAuth

**Шаг 3: Проверьте настройки URL-шаблонов**

Для каждого метода (attachResult, test и т.д.):

1. Откройте метод
2. Найдите вкладку "Аутентификация"
3. Убедитесь что стоит: **Без аутентификации**

**Шаг 4: Опубликуйте изменения**

Конфигурация → Конфигурация базы данных → Обновить конфигурацию базы данных

---

### Вариант 2: Убрать проверку токена в коде (Быстрый обход)

Если не можете изменить настройки HTTP-сервиса, временно уберите проверку в коде:

**В каждом методе (attachResultPOST, testPOST и т.д.) закомментируйте проверку:**

```1c
Функция attachResultPOST(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
    
    Попытка
        // ВРЕМЕННО: закомментируем проверку токена
        /*
        ТокенАвторизации = Запрос.Заголовки.Получить("Authorization");
        Если Не ПроверитьТокен(ТокенАвторизации) Тогда
            Ответ.КодСостояния = 401;
            Ответ.УстановитьТелоИзСтроки("{""error"":""Unauthorized""}", "UTF-8");
            Возврат Ответ;
        КонецЕсли;
        */
        
        // Остальной код без изменений...
```

---

### Вариант 3: Использовать базовую аутентификацию Apache

Можно настроить аутентификацию на уровне Apache (веб-сервер 1С), а не в самом HTTP-сервисе.

**НО** это сложнее и не рекомендуется для начала.

---

## 🧪 Тестирование после исправления

После отключения встроенной проверки JWT:

```bash
# Тест БЕЗ токена (должен работать)
curl -X POST http://192.168.100.234/BITtest/hs/lab/attachResult \
  -H "Content-Type: application/json" \
  -d '{"orderNo":"12345","fileName":"test.pdf","fileBase64":"dGVzdA==","sendEmail":false}'
```

**Ожидаемый результат:**

- ✅ `{"error":"Order not found","orderNo":"12345"}` (404) - отлично! Метод работает!
- ❌ `"Ошибка формата потока"` (500) - нужно исправить парсинг JSON
- ❌ `"Цифровая подпись..."` - проблема не решена

---

## 🔧 Пошаговая инструкция

### ШАГ 1: Найти HTTP-сервис в конфигураторе

1. Откройте конфигуратор 1С
2. В дереве объектов найдите:
   ```
   Общие
     └─ HTTP-сервисы
        └─ LabIntegration (или ваше название)
   ```

### ШАГ 2: Проверить свойства HTTP-сервиса

**Правая кнопка мыши → Свойства**

Должно быть:
- ✅ Корневой URL: `/lab` (или как у вас)
- ✅ Повторное использование сеансов: `Автоматически`
- ✅ **Аутентификация: `<Не использовать>`** ← ВАЖНО!
- ✅ Имя пользователя: (пусто)
- ✅ Пароль: (пусто)

### ШАГ 3: Проверить методы

Откройте каждый URL-шаблон (attachResult, test и т.д.):

В свойствах метода:
- ✅ HTTP-метод: `POST`
- ✅ Обработчик: `attachResultPOST` (или имя вашей функции)
- ✅ **Аутентификация: (без галочки)**

### ШАГ 4: Опубликовать

Конфигурация → Обновить конфигурацию базы данных

### ШАГ 5: Перезапустить 1С (опционально)

Если не помогло - перезапустите службу 1С:

```
Администрирование → Поддержка и обслуживание → 
  Управление службами → Перезапустить
```

---

## 📊 Диагностика

### Если ошибка сохраняется:

**1. Проверьте настройки публикации на веб-сервере:**

Конфигурация → Публикация на веб-сервере

Убедитесь что:
- Публикация HTTP-сервисов включена
- В списке есть ваш HTTP-сервис
- Аутентификация отключена

**2. Проверьте логи 1С:**

Путь к логам (обычно):
```
C:\Program Files\1cv8\srvinfo\reg_1541\
```

Найдите файл с ошибками и проверьте детали.

**3. Проверьте веб-сервер Apache:**

На сервере 1С откройте:
```
C:\Program Files\Apache24\conf\httpd.conf
или
C:\Program Files (x86)\Apache24\conf\httpd.conf
```

Найдите секцию с 1С и убедитесь что нет дополнительных директив аутентификации.

---

## 🎯 Рекомендуемый путь

**Самый простой способ:**

1. Откройте конфигуратор
2. HTTP-сервисы → LabIntegration → Свойства
3. Аутентификация: **`<Не использовать>`**
4. Для каждого метода: **Без аутентификации**
5. Опубликуйте
6. Тест:

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/attachResult \
  -H "Content-Type: application/json" \
  -d '{"orderNo":"12345","fileName":"test.pdf","fileBase64":"dGVzdA==","sendEmail":false}'
```

Должен вернуть `404 Order not found` - это ХОРОШО! ✅

---

## 💡 Важно

После отключения встроенной аутентификации 1С:

- Можно добавить свою проверку токена в коде метода
- Или оставить без защиты (если HTTP-сервис доступен только через VPN)
- Или настроить аутентификацию на уровне Apache

Для начала рекомендую оставить без проверки, чтобы протестировать основную функциональность.

После того как всё заработает - добавим нормальную проверку токена в коде.

---

**После исправления запустите тест и пришлите результат!**

