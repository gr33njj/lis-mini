# 🔄 Рабочий процесс ЛИС МД

## ✅ Текущий статус

- ✅ Система развернута и работает
- ✅ OpenVPN подключен к офисной сети
- ✅ NAS смонтирован: `\\192.168.100.177\laba` → `/mnt/nas`
- ✅ Watcher следит за папкой: `/mnt/nas/lab_results/`
- ⚠️ **Требуется настройка:** Формат имен файлов

## 📋 Как работает система

### 1. Лаборатория создает PDF с результатами

Лаборатория должна сохранять PDF файлы **по номеру исследования** в папку:

```
\\192.168.100.177\laba\lab_results\
```

### 2. Формат имен файлов

**КРИТИЧНО:** Имя файла должно быть **номером исследования** из 1С!

✅ **Правильные примеры:**
```
12345.pdf
МИД-001234.pdf
2024110712345.pdf
ЛАБ-12345.pdf
```

❌ **Неправильные примеры:**
```
Иванов И.И..pdf          ← ФИО вместо номера
Анализ крови 12345.pdf   ← Лишний текст
результат.pdf            ← Нет номера
```

### 3. Автоматическая обработка

Когда файл появляется в `lab_results/`, система **автоматически**:

1. **Обнаруживает** новый PDF файл
2. **Извлекает** номер исследования из имени файла
3. **Отправляет** PDF в 1С через HTTP API:
   ```
   POST https://192.168.100.234/ваш_1с_путь/attachResult
   Body: {
     "research_number": "12345",
     "pdf_base64": "..."
   }
   ```
4. **Получает** от 1С информацию о пациенте (включая email)
5. **Отправляет** PDF пациенту на email
6. **Перемещает** файл в `/mnt/nas/archive/`

### 4. Обработка ошибок

Если что-то идет не так:
- Файл перемещается в `/mnt/nas/quarantine/`
- В веб-интерфейсе появляется запись об ошибке
- Можно вручную повторить обработку

## 🎯 Структура папок на NAS

```
\\192.168.100.177\laba\
│
├── lab_results/          ← СЮДА кладут новые PDF (по номеру!)
│   ├── 12345.pdf
│   ├── 12346.pdf
│   └── МИД-001234.pdf
│
├── archive/              ← Сюда попадают обработанные файлы
│   ├── 2024/
│   │   └── 11/
│   │       └── 12345.pdf
│   └── ...
│
└── quarantine/           ← Сюда попадают файлы с ошибками
    └── 12347.pdf_error
```

## ⚙️ Текущая ситуация на вашем NAS

На данный момент в корне `/mnt/nas/` есть файлы:
```
Аханова Л.П..pdf
Герман Т.Ф..pdf
Горбенко А.С..pdf
... (и другие по ФИО)
```

**Проблема:** Система не может обработать эти файлы, т.к.:
1. Они в корне, а не в `lab_results/`
2. Они названы по ФИО, а не по номерам исследований

### Решение 1: Переименовать и переместить

Если вы знаете номера исследований для этих файлов:

```bash
# Пример переименования и перемещения
mv /mnt/nas/"Аханова Л.П..pdf" /mnt/nas/lab_results/12345.pdf
mv /mnt/nas/"Герман Т.Ф..pdf" /mnt/nas/lab_results/12346.pdf
# и т.д.
```

### Решение 2: Настроить лабораторию

Попросите лабораторию **изменить процесс** и сохранять файлы:
- В папку `lab_results/`
- С именем = номер исследования из 1С

## 📊 Мониторинг в веб-интерфейсе

Откройте: **http://185.247.185.145/dashboard**

Там вы увидите:
- **Всего файлов:** Общее количество обработанных
- **Обработано:** Успешно отправлено в 1С и на email
- **В обработке:** Находятся в очереди
- **Ошибок:** Не удалось обработать

### Вкладка "Записи"
- Список всех файлов с статусами
- Можно повторить обработку
- Можно скачать PDF

### Вкладка "Журнал"
- Полный лог всех операций
- Детали ошибок
- Кто и когда выполнял действия

## 🧪 Тестирование системы

### Шаг 1: Положите тестовый файл

```bash
# На NAS или с сервера
cp /путь/к/тестовому.pdf /mnt/nas/lab_results/TEST-12345.pdf
```

### Шаг 2: Следите за логами

```bash
# В реальном времени
docker logs -f lis-md-app
```

Вы увидите:
```
[Watcher] New file detected: TEST-12345.pdf
[Watcher] Research number extracted: TEST-12345
[Integrator] Sending to 1C: TEST-12345
[Integrator] Sending email to patient...
[Watcher] File moved to archive: TEST-12345.pdf
```

### Шаг 3: Проверьте веб-интерфейс

Обновите dashboard - должна появиться новая запись.

## ⚠️ Важные замечания

### 1. Интеграция с 1С
Пока не настроена! Нужно:
- Создать HTTP-сервис в 1С (см. `docs/1c-integration.md`)
- Получить токен авторизации
- Обновить `.env` файл

Пока 1С не настроена, файлы будут попадать в **quarantine** с ошибкой подключения.

### 2. Email уведомления
Пока не настроены! Нужно:
- Настроить SMTP в `.env`
- Указать реквизиты почтового сервера

Пока email не настроен, отправка писем будет пропускаться.

### 3. Формат номеров

Система извлекает номер из имени файла (без расширения):
```
"12345.pdf"           → номер: "12345"
"МИД-001234.pdf"      → номер: "МИД-001234"
"test 123.pdf"        → номер: "test 123"
```

Убедитесь, что номера в файлах **точно совпадают** с номерами в 1С!

## 🔧 Следующие шаги

1. ✅ NAS настроен
2. ⏳ Настроить формат файлов (переименовать или настроить лабораторию)
3. ⏳ Настроить интеграцию с 1С
4. ⏳ Настроить email отправку
5. ⏳ Протестировать полный цикл

## 📞 Поддержка

Если возникли вопросы или проблемы:
- Смотрите логи: `docker logs -f lis-md-app`
- Проверяйте статус в веб-интерфейсе
- Читайте документацию в `/opt/lis-md/docs/`

