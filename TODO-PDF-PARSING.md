# TODO: Парсинг PDF и заполнение HTML шаблона

## ✅ ЧТО УЖЕ РАБОТАЕТ:
- HTTP-сервис 1С работает с Basic Auth (`Администратор:1234`)
- JSON парсится правильно
- Код обработки запросов работает

## 🎯 НОВАЯ ЗАДАЧА:
Парсить PDF с результатами анализов → Заполнять HTML шаблон в 1С

## 📋 ЧТО НУЖНО СДЕЛАТЬ:

### 1. Python (ЛИС-МД): Парсинг PDF
- [ ] Добавить `pdfplumber` в `app/requirements.txt`
- [ ] Создать `app/services/pdf_parser.py` для извлечения данных из PDF
- [ ] Парсить таблицу с показателями (Название, Результат, Ед.изм, Референс)
- [ ] Извлекать данные пациента (ФИО, возраст, пол)

### 2. Python (ЛИС-МД): Интеграция
- [ ] Обновить `app/services/onec_integrator.py` на Basic Auth
- [ ] Отправлять структурированные данные анализа в 1С
- [ ] Формат данных: `{"patient": {...}, "indicators": [{name, result, unit, ref}, ...]}`

### 3. 1С: HTTP-сервис
- [ ] Создать метод `saveAnalysisResults`POST
- [ ] Принимать JSON с данными анализа
- [ ] Найти пациента по ФИО/дате рождения
- [ ] Создать/найти прием пациента
- [ ] Заполнить HTML шаблон значениями из JSON
- [ ] Сохранить заполненный HTML

### 4. 1С: Заполнение HTML шаблона
- [ ] Получить HTML шаблон биохимии
- [ ] Найти все `<a id="UUID">[...]</a>` элементы
- [ ] Заменить `[...]` на значения из JSON по mapping ID → показатель
- [ ] Вернуть заполненный HTML

## 🔗 MAPPING (ID → Показатель):

```
ae584a2f-957f-11f0-a7be-eca0f0014d7d → АЛАТ
d284601d-957f-11f0-a7be-eca0f0014d7d → АСАТ
e5e808bc-957f-11f0-a7be-eca0f0014d7d → Холестерин
ef9d787d-957f-11f0-a7be-eca0f0014d7d → Гамма-ГТ
...и т.д.
```

## 📂 СТРУКТУРА ДАННЫХ:

### Из PDF извлекаем:
```json
{
  "patient": {
    "fullName": "Пастухова Н",
    "birthDate": null,
    "age": 43,
    "gender": "Женский"
  },
  "analysis": {
    "type": "Биохимический анализ крови",
    "date": "17.11.2024",
    "orderNo": "2"
  },
  "indicators": [
    {
      "name": "АЛАТ",
      "result": "",
      "unit": "Ед/л",
      "reference": "0-31"
    },
    {
      "name": "АСАТ",
      "result": "",
      "unit": "Ед/л",
      "reference": "0-31"
    },
    ...
  ]
}
```

### Отправляем в 1С:
```
POST /hs/lab/saveAnalysisResults
Authorization: Basic Администратор:1234
Content-Type: application/json

{полная структура выше}
```

### 1С возвращает:
```json
{
  "status": "ok",
  "patientRef": "uuid",
  "visitRef": "uuid",
  "htmlFilled": true
}
```

## ⚡ БЫСТРЫЙ СТАРТ:

### Шаг 1: Установка зависимостей
```bash
cd /opt/lis-md
echo "pdfplumber==0.11.0" >> app/requirements.txt
docker compose down
docker compose build app
docker compose up -d
```

### Шаг 2: Тестовый парсинг PDF
```bash
# Монтируем NAS
mount -t cifs //192.168.100.177/laba /mnt/nas \
  -o username=lis-md,password=GmLnxc41

# Парсим тестовый PDF
python3 app/services/pdf_parser.py /mnt/nas/lab_results/2.pdf
```

### Шаг 3: Доработка 1С
- Добавить метод `saveAnalysisResultsPOST` в HTTP-сервис
- Реализовать логику заполнения HTML шаблона

## 📝 ВОПРОСЫ:

1. **Как идентифицировать пациента?**
   - В PDF есть ФИО: "Пастухова Н"
   - Как найти пациента в 1С? По полному ФИО? По номеру заказа?

2. **Где хранится HTML шаблон?**
   - Справочник?
   - Документ?
   - Обработка?

3. **Нужно ли создавать новый прием или использовать существующий?**

4. **Что делать если пациент не найден?**
   - Создать нового?
   - Вернуть ошибку?

5. **Как связать PDF файл с приемом?**
   - По номеру в имени файла (2.pdf)?
   - По другому полю?

