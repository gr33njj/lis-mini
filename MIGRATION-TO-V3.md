# 🔄 Миграция на версию 3.0 (поиск по Представление)

## 🎯 Что изменилось

### Версия 2.x → Версия 3.0

**КРИТИЧЕСКОЕ ИЗМЕНЕНИЕ:** Метод поиска пациента в 1С

| Версия | Метод поиска | Статус |
|--------|-------------|--------|
| 1.x | По Наименование | ❌ Не работало |
| 2.x | По Фамилия + Имя + Отчество | ❌ Не работало |
| **3.0** | **По Представление + ДатаРождения** | ✅ **РАБОТАЕТ!** |

---

## 📋 Что нужно изменить

### 1️⃣ В 1С (ОБЯЗАТЕЛЬНО!)

**Файл с кодом:** `1C-SEARCH-BY-PREDSTAVLENIE.md`

#### Шаг 1: Открыть конфигуратор

Запустите 1С в режиме конфигуратора.

#### Шаг 2: Найти HTTP-сервис

Перейдите к HTTP-сервису `ЛИС` или тому, где реализована интеграция.

#### Шаг 3: Найти функцию `НайтиПациента`

Функция может находиться:
- В модуле HTTP-сервиса
- В общем модуле

#### Шаг 4: Заменить код функции

**УДАЛИТЕ** весь старый код функции `НайтиПациента` и **ЗАМЕНИТЕ** его на новый из файла `1C-SEARCH-BY-PREDSTAVLENIE.md`.

#### Шаг 5: Сохранить и опубликовать

1. Сохраните изменения в конфигураторе
2. Опубликуйте HTTP-сервис
3. Перезапустите публикацию (если требуется)

---

### 2️⃣ Проверка конфигурации

**ВАЖНО:** Убедитесь, что в справочнике `Клиенты` есть реквизит `Представление`.

#### Как проверить

Выполните в консоли 1С:

```1c
Запрос = Новый Запрос;
Запрос.Текст = 
"ВЫБРАТЬ ПЕРВЫЕ 1
|    Клиенты.Ссылка,
|    Клиенты.Представление,
|    Клиенты.ДатаРождения
|ИЗ
|    Справочник.Клиенты КАК Клиенты
|ГДЕ
|    НЕ Клиенты.ПометкаУдаления";

Результат = Запрос.Выполнить();

Если НЕ Результат.Пустой() Тогда
    Выборка = Результат.Выбрать();
    Выборка.Следующий();
    Сообщить("✅ Представление: " + Выборка.Представление);
    Сообщить("✅ Дата рождения: " + Формат(Выборка.ДатаРождения, "ДФ=dd.MM.yyyy"));
Иначе
    Сообщить("❌ Нет клиентов в базе!");
КонецЕсли;
```

**Ожидаемый результат:**
```
✅ Представление: Иванов Иван Иванович
✅ Дата рождения: 21.05.1982
```

**Если получили ошибку:**
```
❌ Поле "Представление" не найдено
```

**Решение:** Возможно, в вашей конфигурации поле называется по-другому. Проверьте в конфигураторе структуру справочника `Клиенты`.

---

### 3️⃣ В Python (НЕ ТРЕБУЕТСЯ!)

**Хорошие новости:** Python код **НЕ НУЖНО** менять!

Приложение уже отправляет правильные данные:
```json
{
  "patient_name": "Иванов Иван Иванович",
  "birth_date": "21.05.1982",
  "age": "42",
  "test_results": { ... }
}
```

1С просто теперь ищет по другому полю (`Представление` вместо `Наименование` или раздельных ФИО).

---

## 🧪 Тестирование после миграции

### Тест 1: Проверить поиск в консоли 1С

```1c
// Вставьте реальные данные из базы
ФИО = "Иванов Иван Иванович";
ДатаРождения = "21.05.1982";

Пациент = НайтиПациента(ФИО, ДатаРождения);

Если Пациент <> Неопределено Тогда
    Сообщить("✅ Пациент найден: " + Строка(Пациент));
Иначе
    Сообщить("❌ Пациент НЕ найден!");
КонецЕсли;
```

---

### Тест 2: Проверить через curl

Замените `YOUR_TOKEN` и данные пациента:

```cmd
curl -X POST "http://192.168.100.234/med/hs/lis/createAppointment" -H "Authorization: Bearer YOUR_TOKEN" -H "Content-Type: application/json" -d "{\"patient_name\":\"Иванов Иван Иванович\",\"birth_date\":\"21.05.1982\",\"age\":\"42\",\"test_results\":{\"ae584a2f-1234-5678-90ab-cdef12345678\":\"25.5\"}}"
```

**Ожидаемый ответ:**
```json
{
  "status": "success",
  "appointment_id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
}
```

---

### Тест 3: Полный E2E тест

1. **Создать тестовый PDF:**
   ```bash
   echo "Test content" > "Иванов Иван Иванович 21.05.1982.pdf"
   ```

2. **Скопировать на NAS** (пациент ДОЛЖЕН существовать в 1С!):
   ```bash
   cp "Иванов Иван Иванович 21.05.1982.pdf" /mnt/nas/lab_results/
   ```

3. **Смотреть логи приложения:**
   ```bash
   docker compose logs -f app
   ```

4. **Проверить журнал регистрации в 1С:**
   - Администрирование → Журнал регистрации
   - Фильтр по событию: `ЛИС.ПоискПациента`
   - Должна быть запись: `"✅ Найден: Иванов Иван Иванович / 21.05.1982"`

5. **Проверить создание приёма:**
   - Медицинская организация → Документы → Приёмы
   - Должен быть новый приём с врачом "МД Lab"

---

## ❗ Возможные проблемы

### Проблема 1: Пациент не найден

**Симптомы:**
```
❌ НЕ найден: Иванов Иван Иванович / 21.05.1982
```

**Причины:**

1. **ФИО не совпадает точно**
   - В базе: `Иванов  Иван  Иванович` (лишние пробелы)
   - В файле: `Иванов Иван Иванович`
   - **Решение:** Функция автоматически убирает лишние пробелы через `СокрЛП()`

2. **Дата рождения не совпадает**
   - В базе: `21.05.1982`
   - В файле: `21.05.1983` (опечатка!)
   - **Решение:** Проверьте имя файла

3. **Клиент удалён (ПометкаУдаления = Истина)**
   - **Решение:** Снимите пометку удаления в 1С

4. **Клиента нет в базе**
   - **Решение:** Создайте клиента вручную в 1С

---

### Проблема 2: Ошибка "Поле Представление не найдено"

**Симптомы:**
```
⚠️ Ошибка поиска: ... Поле "Представление" не найдено
```

**Причина:** В вашей конфигурации поле называется по-другому.

**Решение:**

1. Откройте конфигуратор 1С
2. Перейдите к справочнику `Клиенты`
3. Посмотрите, как называется поле с полным ФИО
4. Замените в запросе:

```1c
// Было:
Клиенты.Представление = &ФИО

// Стало (если поле называется, например, ФИОПолное):
Клиенты.ФИОПолное = &ФИО
```

---

### Проблема 3: HTTP-сервис не отвечает

**Симптомы:**
```
Connection timeout
```

**Решение:**

1. Проверьте, что HTTP-сервис опубликован:
   - Администрирование → Интернет → HTTP-сервисы
   - Статус: ✅ Опубликован

2. Проверьте доступность:
   ```bash
   ping 192.168.100.234
   ```

3. Проверьте URL:
   ```bash
   curl http://192.168.100.234/med/hs/lis/createAppointment
   ```

---

## 📋 Checklist миграции

- [ ] Прочитан файл `1C-SEARCH-BY-PREDSTAVLENIE.md`
- [ ] Открыт конфигуратор 1С
- [ ] Найдена функция `НайтиПациента`
- [ ] Старый код заменён на новый
- [ ] Конфигурация сохранена
- [ ] HTTP-сервис опубликован
- [ ] Проверено наличие реквизита `Представление` в справочнике
- [ ] Тест 1: Консоль 1С ✅
- [ ] Тест 2: curl ✅
- [ ] Тест 3: E2E с реальным файлом ✅
- [ ] Проверены логи в журнале регистрации ✅

---

## 🎯 Результат миграции

### ДО миграции:

```
❌ Поиск по Наименование или Фамилия+Имя+Отчество
❌ Пациент не найден (даже если есть в базе)
❌ Файлы в карантине
❌ Ручная обработка
```

### ПОСЛЕ миграции:

```
✅ Поиск по Представление + ДатаРождения
✅ Пациент найден!
✅ Приём создан автоматически
✅ Email отправлен пациенту
✅ Файл в архиве
✅ Полная автоматизация
```

---

## 📞 Поддержка

Если после миграции что-то не работает:

1. **Проверьте логи 1С:**
   - Администрирование → Журнал регистрации
   - Фильтр: "ЛИС"

2. **Проверьте логи приложения:**
   ```bash
   docker compose logs -f app
   ```

3. **Запустите диагностику:**
   ```bash
   ./check-system.sh
   ```

4. **Откройте файл:** `1C-SEARCH-BY-PREDSTAVLENIE.md` и проверьте код функции

---

**Версия:** 3.0  
**Дата:** 24.11.2025  
**Статус:** ✅ ГОТОВО К ПРИМЕНЕНИЮ

