# 📚 История версий - Метод поиска пациента в 1С

## ⚠️ Важно!

Этот документ объясняет **эволюцию** метода поиска пациента в 1С.  
Каждая версия была создана для разных конфигураций 1С с разной структурой справочника `Клиенты`.

**ИСПОЛЬЗУЙТЕ ТОЛЬКО ТУ ВЕРСИЮ, КОТОРАЯ ПОДХОДИТ ДЛЯ ВАШЕЙ КОНФИГУРАЦИИ!**

---

## 📊 Сравнительная таблица версий

| Версия | Метод поиска | Поля в справочнике Клиенты | Статус | Файл документации |
|--------|--------------|----------------------------|--------|-------------------|
| v1.x | `Наименование` | `Наименование` (ФИО одной строкой) | ❌ Устарело | - |
| v2.x | `Фамилия + Имя + Отчество` | Раздельные поля без строкового парсинга | ⚠️ Частично работало | - |
| v3.0 | `Представление` | `Представление` (автореквизит) | ⚠️ Только для конфигураций с Представление | `1C-SEARCH-BY-PREDSTAVLENIE.md` |
| **v3.1** | **`Фамилия + Имя + Отчество`** | **Раздельные поля** | ✅ **РЕКОМЕНДУЕТСЯ** | **`1C-SEARCH-BY-SEPARATE-FIO-FINAL.md`** |

---

## 🔍 Версия 1.x - Поиск по Наименованию (УСТАРЕЛО)

### Метод:
```1c
WHERE Клиенты.Наименование = "Иванов Иван Иванович"
```

### Проблема:
❌ В большинстве конфигураций `Наименование` **НЕ содержит ФИО**, а является автоматически генерируемым полем.

### Вывод:
**НЕ РАБОТАЕТ** в реальных конфигурациях.

---

## 🔍 Версия 2.x - Первая попытка поиска по раздельным полям (УСТАРЕЛО)

### Метод:
```1c
// Разбор ФИО на части (упрощённый)
WHERE Клиенты.Фамилия = "Иванов"
  AND Клиенты.Имя = "Иван"
  AND Клиенты.Отчество = "Иванович"
```

### Проблема:
⚠️ Упрощённый парсинг ФИО не учитывал:
- Двойные пробелы
- Отсутствие отчества
- Неправильную обработку даты рождения

### Вывод:
**ЧАСТИЧНО РАБОТАЛО**, но было много ошибок.

---

## 🔍 Версия 3.0 - Поиск по Представление (ДЛЯ ОПРЕДЕЛЁННЫХ КОНФИГУРАЦИЙ)

### Метод:
```1c
WHERE Клиенты.Представление = "Иванов Иван Иванович"
  AND Клиенты.ДатаРождения = Дата(1982, 05, 21)
```

### Для каких конфигураций:
✅ Конфигурации, где `Представление` является **СТРОКОВЫМ** реквизитом с возможностью сравнения через `=`.

### Проблема:
❌ В большинстве конфигураций `Представление` - это **строка неограниченной длины**, которую нельзя сравнивать через `=` или `ПОДОБНО`.

### Вывод:
**РАБОТАЕТ ТОЛЬКО** если в вашей конфигурации `Представление` можно сравнивать.

**Файл:** `1C-SEARCH-BY-PREDSTAVLENIE.md`

---

## ✅ Версия 3.1 FINAL - Поиск по раздельным полям (РЕКОМЕНДУЕТСЯ)

### Метод:
```1c
// 1. Нормализация ФИО
ФИО = СокрЛП(ИмяПолное);
Пока СтрНайти(ФИО, "  ") > 0 Цикл
    ФИО = СтрЗаменить(ФИО, "  ", " ");
КонецЦикла;

// 2. Разбор на части
СписокФИО = СтрРазделить(ФИО, " ", Ложь);
Фамилия = СписокФИО[0];
Имя = СписокФИО[1];
Отчество = СписокФИО[2];  // Если есть

// 3. Поиск
WHERE Клиенты.Фамилия = "Иванов"
  AND Клиенты.Имя = "Иван"
  AND Клиенты.Отчество = "Иванович"  // Если есть
  AND Клиенты.ДатаРождения = Дата(1982, 05, 21)
```

### Для каких конфигураций:
✅ **УНИВЕРСАЛЬНОЕ РЕШЕНИЕ!**

Работает для всех конфигураций, где справочник `Клиенты` имеет раздельные реквизиты:
- `Фамилия`
- `Имя`
- `Отчество`
- `ДатаРождения`

### Преимущества:
✅ Правильная нормализация пробелов  
✅ Поддержка ФИО без отчества  
✅ Корректный парсинг даты рождения  
✅ Подробное логирование  
✅ Обработка всех ошибок  
✅ Работает в 99% конфигураций  

### Вывод:
**✅ РЕКОМЕНДУЕТСЯ ДЛЯ ВСЕХ!**

**Файл:** `1C-SEARCH-BY-SEPARATE-FIO-FINAL.md`

---

## 🎯 Как выбрать правильную версию?

### Шаг 1: Проверьте структуру справочника Клиенты

Выполните в консоли 1С:

```1c
Запрос = Новый Запрос;
Запрос.Текст = 
"ВЫБРАТЬ ПЕРВЫЕ 1
|    Клиенты.Фамилия,
|    Клиенты.Имя,
|    Клиенты.Отчество,
|    Клиенты.ДатаРождения
|ИЗ
|    Справочник.Клиенты КАК Клиенты
|ГДЕ
|    НЕ Клиенты.ПометкаУдаления";

Результат = Запрос.Выполнить();

Если НЕ Результат.Пустой() Тогда
    Выборка = Результат.Выбрать();
    Выборка.Следующий();
    
    Сообщить("✅ Есть раздельные поля:");
    Сообщить("Фамилия: " + Выборка.Фамилия);
    Сообщить("Имя: " + Выборка.Имя);
    Сообщить("Отчество: " + Выборка.Отчество);
    Сообщить("Дата: " + Формат(Выборка.ДатаРождения, "ДФ=dd.MM.yyyy"));
КонецЕсли;
```

**Если запрос выполнился успешно:**
✅ **ИСПОЛЬЗУЙТЕ ВЕРСИЮ 3.1** (`1C-SEARCH-BY-SEPARATE-FIO-FINAL.md`)

---

### Шаг 2: (Необязательно) Проверьте Представление

```1c
Запрос = Новый Запрос;
Запрос.Текст = 
"ВЫБРАТЬ ПЕРВЫЕ 1
|    Клиенты.Представление
|ИЗ
|    Справочник.Клиенты КАК Клиенты
|ГДЕ
|    Клиенты.Представление = &ФИО
|    И НЕ Клиенты.ПометкаУдаления";

Запрос.УстановитьПараметр("ФИО", "Тестов Тест Тестович");

Попытка
    Результат = Запрос.Выполнить();
    Сообщить("✅ Представление можно использовать для поиска!");
    Сообщить("⚠️ Можете попробовать версию 3.0");
Исключение
    Сообщить("❌ Представление НЕЛЬЗЯ использовать для поиска");
    Сообщить("✅ ИСПОЛЬЗУЙТЕ версию 3.1");
КонецПопытки;
```

---

## 📋 Рекомендации

### Для большинства пользователей:

✅ **ИСПОЛЬЗУЙТЕ ВЕРСИЮ 3.1**

**Файл:** `1C-SEARCH-BY-SEPARATE-FIO-FINAL.md`

**Почему:**
- ✅ Универсальное решение
- ✅ Работает в 99% конфигураций
- ✅ Правильная обработка всех случаев
- ✅ Подробное логирование
- ✅ Полная обработка ошибок

---

### Только если у вас особая конфигурация:

⚠️ **ПОПРОБУЙТЕ ВЕРСИЮ 3.0**

**Файл:** `1C-SEARCH-BY-PREDSTAVLENIE.md`

**Условия:**
- У вас есть реквизит `Представление`
- Его можно сравнивать через `=`
- Он содержит полное ФИО одной строкой

**Как проверить:** Выполните Шаг 2 выше.

---

## 🧪 Тестовые данные

Для всех версий используйте:

**Тестовый пациент:**
- **Фамилия:** Тестов
- **Имя:** Тест
- **Отчество:** Тестович
- **Дата рождения:** 18.11.1988

**Имя файла:**
```
Тестов Тест Тестович 18.11.1988.pdf
```

**JSON запрос:**
```json
{
  "patient_name": "Тестов Тест Тестович",
  "birth_date": "18.11.1988",
  "age": "36",
  "test_results": {
    "ae584a2f-1234-5678-90ab-cdef12345678": "25.5"
  }
}
```

---

## 📞 Что делать, если ничего не работает?

### 1. Проверьте, что пациент есть в базе

```1c
Запрос = Новый Запрос;
Запрос.Текст = 
"ВЫБРАТЬ
|    Клиенты.Фамилия,
|    Клиенты.Имя,
|    Клиенты.Отчество,
|    Клиенты.ДатаРождения,
|    Клиенты.ПометкаУдаления
|ИЗ
|    Справочник.Клиенты КАК Клиенты
|ГДЕ
|    Клиенты.Фамилия = ""Тестов""";

Результат = Запрос.Выполнить();
Выборка = Результат.Выбрать();

Если Выборка.Следующий() Тогда
    Сообщить("✅ Пациент найден:");
    Сообщить("ФИО: " + Выборка.Фамилия + " " + Выборка.Имя + " " + Выборка.Отчество);
    Сообщить("Дата: " + Формат(Выборка.ДатаРождения, "ДФ=dd.MM.yyyy"));
    Сообщить("Удалён: " + ?(Выборка.ПометкаУдаления, "ДА", "НЕТ"));
Иначе
    Сообщить("❌ Пациент НЕ найден в базе!");
    Сообщить("Создайте тестового пациента вручную.");
КонецЕсли;
```

---

### 2. Проверьте журнал регистрации 1С

**Администрирование → Журнал регистрации**

Фильтр по событию: `ЛИС`

Должны быть записи:
- `ЛИС.РазборФИО`
- `ЛИС.ПоискПациента`

---

### 3. Проверьте логи Python приложения

```bash
docker compose logs -f app
```

Ищите строки:
```
Parsed filename: patient_name='Тестов Тест Тестович', birth_date='18.11.1988'
Sending to 1C: POST /createAppointment
```

---

### 4. Проверьте доступность 1С сервера

```bash
ping 192.168.100.234

curl -X POST "http://192.168.100.234/med/hs/lis/createAppointment" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"patient_name":"Тестов Тест Тестович","birth_date":"18.11.1988"}'
```

---

## ✅ Итоговые рекомендации

| Ситуация | Версия | Файл |
|----------|--------|------|
| **Стандартная конфигурация** | ✅ v3.1 | `1C-SEARCH-BY-SEPARATE-FIO-FINAL.md` |
| **Есть поле Представление (можно сравнивать)** | ⚠️ v3.0 | `1C-SEARCH-BY-PREDSTAVLENIE.md` |
| **Не знаю, что у меня** | ✅ v3.1 | `1C-SEARCH-BY-SEPARATE-FIO-FINAL.md` |

---

**Последнее обновление:** 24.11.2025  
**Рекомендуемая версия:** v3.1 FINAL  
**Файл:** `1C-SEARCH-BY-SEPARATE-FIO-FINAL.md`

