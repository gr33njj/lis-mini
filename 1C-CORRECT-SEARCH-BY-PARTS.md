# 🔧 ИСПРАВЛЕНИЕ: Поиск по Фамилия + Имя + Отчество + ДатаРождения

## 🎯 Проблема

В конфигурации справочник `Клиенты` устроен так:

**НЕТ:**
- ❌ `Наименование` (единое поле ФИО)

**ЕСТЬ:**
- ✅ `Фамилия` (реквизит)
- ✅ `Имя` (реквизит)
- ✅ `Отчество` (реквизит)
- ✅ `ДатаРождения` (реквизит)

Поэтому поиск по `Клиенты.Наименование = "Иванов Иван Иванович"` **НЕ РАБОТАЕТ**!

---

## ✅ Решение: Разбор ФИО на части

### Исправленная функция `НайтиПациента`:

```1c
Функция НайтиПациента(ИмяПолное, ДатаРождения)
    
    // ❗ НОВЫЙ ПОДХОД: Разбираем ФИО на Фамилия, Имя, Отчество
    // Формат: "Иванов Иван Иванович"
    
    Попытка
        // 1. ПАРСИМ ФИО
        ИмяОчищенное = СокрЛП(ИмяПолное);
        
        // Убираем двойные пробелы
        Пока СтрНайти(ИмяОчищенное, "  ") > 0 Цикл
            ИмяОчищенное = СтрЗаменить(ИмяОчищенное, "  ", " ");
        КонецЦикла;
        
        // Разбиваем по пробелам
        ЧастиИмени = СтрРазделить(ИмяОчищенное, " ", Ложь);
        
        Если ЧастиИмени.Количество() < 2 Тогда
            // Недостаточно частей для поиска
            Возврат Неопределено;
        КонецЕсли;
        
        // Извлекаем части
        Фамилия = ЧастиИмени[0];
        Имя = ЧастиИмени[1];
        Отчество = "";
        
        Если ЧастиИмени.Количество() >= 3 Тогда
            Отчество = ЧастиИмени[2];
        КонецЕсли;
        
        // 2. ПАРСИМ ДАТУ РОЖДЕНИЯ
        ДатаРожденияДата = Неопределено;
        
        Если НЕ ПустаяСтрока(ДатаРождения) Тогда
            // Парсим дату "21.05.1982"
            ЧастиДаты = СтрРазделить(ДатаРождения, ".", Ложь);
            Если ЧастиДаты.Количество() = 3 Тогда
                Попытка
                    День = Число(ЧастиДаты[0]);
                    Месяц = Число(ЧастиДаты[1]);
                    Год = Число(ЧастиДаты[2]);
                    ДатаРожденияДата = Дата(Год, Месяц, День);
                Исключение
                    ДатаРожденияДата = Неопределено;
                КонецПопытки;
            КонецЕсли;
        КонецЕсли;
        
        // 3. ИЩЕМ В БАЗЕ ПО ОТДЕЛЬНЫМ ПОЛЯМ
        Запрос = Новый Запрос;
        
        Если ДатаРожденияДата = Неопределено Тогда
            // Поиск БЕЗ даты (менее надёжно)
            Если ПустаяСтрока(Отчество) Тогда
                // Без отчества
                Запрос.Текст = 
                "ВЫБРАТЬ ПЕРВЫЕ 1
                |    Клиенты.Ссылка КАК Ссылка
                |ИЗ
                |    Справочник.Клиенты КАК Клиенты
                |ГДЕ
                |    Клиенты.Фамилия = &Фамилия
                |    И Клиенты.Имя = &Имя
                |    И НЕ Клиенты.ПометкаУдаления";
                
                Запрос.УстановитьПараметр("Фамилия", Фамилия);
                Запрос.УстановитьПараметр("Имя", Имя);
            Иначе
                // С отчеством
                Запрос.Текст = 
                "ВЫБРАТЬ ПЕРВЫЕ 1
                |    Клиенты.Ссылка КАК Ссылка
                |ИЗ
                |    Справочник.Клиенты КАК Клиенты
                |ГДЕ
                |    Клиенты.Фамилия = &Фамилия
                |    И Клиенты.Имя = &Имя
                |    И Клиенты.Отчество = &Отчество
                |    И НЕ Клиенты.ПометкаУдаления";
                
                Запрос.УстановитьПараметр("Фамилия", Фамилия);
                Запрос.УстановитьПараметр("Имя", Имя);
                Запрос.УстановитьПараметр("Отчество", Отчество);
            КонецЕсли;
        Иначе
            // ✅ ПОЛНЫЙ ПОИСК: Фамилия + Имя + Отчество + Дата рождения
            Если ПустаяСтрока(Отчество) Тогда
                // Без отчества
                Запрос.Текст = 
                "ВЫБРАТЬ ПЕРВЫЕ 1
                |    Клиенты.Ссылка КАК Ссылка
                |ИЗ
                |    Справочник.Клиенты КАК Клиенты
                |ГДЕ
                |    Клиенты.Фамилия = &Фамилия
                |    И Клиенты.Имя = &Имя
                |    И Клиенты.ДатаРождения = &ДатаРождения
                |    И НЕ Клиенты.ПометкаУдаления";
                
                Запрос.УстановитьПараметр("Фамилия", Фамилия);
                Запрос.УстановитьПараметр("Имя", Имя);
                Запрос.УстановитьПараметр("ДатаРождения", ДатаРожденияДата);
            Иначе
                // ✅ ИДЕАЛЬНЫЙ СЛУЧАЙ: Фамилия + Имя + Отчество + Дата
                Запрос.Текст = 
                "ВЫБРАТЬ ПЕРВЫЕ 1
                |    Клиенты.Ссылка КАК Ссылка
                |ИЗ
                |    Справочник.Клиенты КАК Клиенты
                |ГДЕ
                |    Клиенты.Фамилия = &Фамилия
                |    И Клиенты.Имя = &Имя
                |    И Клиенты.Отчество = &Отчество
                |    И Клиенты.ДатаРождения = &ДатаРождения
                |    И НЕ Клиенты.ПометкаУдаления";
                
                Запрос.УстановитьПараметр("Фамилия", Фамилия);
                Запрос.УстановитьПараметр("Имя", Имя);
                Запрос.УстановитьПараметр("Отчество", Отчество);
                Запрос.УстановитьПараметр("ДатаРождения", ДатаРожденияДата);
            КонецЕсли;
        КонецЕсли;
        
        // 4. ВЫПОЛНЯЕМ ЗАПРОС
        Результат = Запрос.Выполнить();
        
        Если НЕ Результат.Пустой() Тогда
            Выборка = Результат.Выбрать();
            Выборка.Следующий();
            
            // Логируем успех
            ЗаписьЖурналаРегистрации(
                "ЛИС.ПоискПациента",
                ,
                ,
                Выборка.Ссылка,
                "Найден: " + Фамилия + " " + Имя + " " + Отчество + " / " + ДатаРождения
            );
            
            Возврат Выборка.Ссылка;
        Иначе
            // Логируем что не найден
            ЗаписьЖурналаРегистрации(
                "ЛИС.ПоискПациента",
                ,
                ,
                ,
                "НЕ найден: " + Фамилия + " " + Имя + " " + Отчество + " / " + ДатаРождения
            );
            
            Возврат Неопределено;
        КонецЕсли;
        
    Исключение
        // Логируем ошибку
        ЗаписьЖурналаРегистрации(
            "ЛИС.ПоискПациента",
            ,
            ,
            ,
            "Ошибка поиска: " + ИмяПолное + " / " + ДатаРождения + ": " + ОписаниеОшибки()
        );
        Возврат Неопределено;
    КонецПопытки;
    
КонецФункции
```

---

## 📊 Как это работает

### Пример 1: Полное ФИО

**Входные данные:**
```
ИмяПолное = "Иванов Иван Иванович"
ДатаРождения = "21.05.1982"
```

**Парсинг:**
```
Фамилия = "Иванов"
Имя = "Иван"
Отчество = "Иванович"
ДатаРожденияДата = Дата(1982, 05, 21)
```

**Запрос:**
```sql
ВЫБРАТЬ ПЕРВЫЕ 1
    Клиенты.Ссылка
ИЗ
    Справочник.Клиенты КАК Клиенты
ГДЕ
    Клиенты.Фамилия = "Иванов"
    И Клиенты.Имя = "Иван"
    И Клиенты.Отчество = "Иванович"
    И Клиенты.ДатаРождения = Дата(1982, 05, 21)
    И НЕ Клиенты.ПометкаУдаления
```

---

### Пример 2: Без отчества

**Входные данные:**
```
ИмяПолное = "Петров Петр"
ДатаРождения = "15.03.1990"
```

**Парсинг:**
```
Фамилия = "Петров"
Имя = "Петр"
Отчество = ""  ← пусто!
```

**Запрос (без отчества):**
```sql
ВЫБРАТЬ ПЕРВЫЕ 1
    Клиенты.Ссылка
ИЗ
    Справочник.Клиенты КАК Клиенты
ГДЕ
    Клиенты.Фамилия = "Петров"
    И Клиенты.Имя = "Петр"
    И Клиенты.ДатаРождения = Дата(1990, 03, 15)
    И НЕ Клиенты.ПометкаУдаления
```

---

## 🧪 Тестирование

### Тест 1: Проверить разбор ФИО

В консоли 1С выполните:

```1c
ИмяПолное = "Иванов Иван Иванович";
ЧастиИмени = СтрРазделить(ИмяПолное, " ", Ложь);

Сообщить("Фамилия: " + ЧастиИмени[0]);  // Иванов
Сообщить("Имя: " + ЧастиИмени[1]);       // Иван
Сообщить("Отчество: " + ЧастиИмени[2]);  // Иванович
```

---

### Тест 2: Проверить поиск в базе

```1c
// Вставьте реальные данные из вашей базы
Фамилия = "Иванов";
Имя = "Иван";
Отчество = "Иванович";
ДатаРожденияДата = Дата(1982, 05, 21);

Запрос = Новый Запрос;
Запрос.Текст = 
"ВЫБРАТЬ ПЕРВЫЕ 1
|    Клиенты.Ссылка,
|    Клиенты.Фамилия,
|    Клиенты.Имя,
|    Клиенты.Отчество,
|    Клиенты.ДатаРождения
|ИЗ
|    Справочник.Клиенты КАК Клиенты
|ГДЕ
|    Клиенты.Фамилия = &Фамилия
|    И Клиенты.Имя = &Имя
|    И Клиенты.Отчество = &Отчество
|    И Клиенты.ДатаРождения = &ДатаРождения
|    И НЕ Клиенты.ПометкаУдаления";

Запрос.УстановитьПараметр("Фамилия", Фамилия);
Запрос.УстановитьПараметр("Имя", Имя);
Запрос.УстановитьПараметр("Отчество", Отчество);
Запрос.УстановитьПараметр("ДатаРождения", ДатаРожденияДата);

Результат = Запрос.Выполнить();

Если НЕ Результат.Пустой() Тогда
    Сообщить("✅ Клиент НАЙДЕН!");
    Выборка = Результат.Выбрать();
    Выборка.Следующий();
    Сообщить("Ссылка: " + Строка(Выборка.Ссылка));
Иначе
    Сообщить("❌ Клиент НЕ НАЙДЕН!");
КонецЕсли;
```

---

## ✅ Полный рабочий код для HTTP-сервиса

### Метод `createAppointmentPOST` (без изменений)

Основной метод остаётся таким же, только функция `НайтиПациента` изменилась.

```1c
Функция createAppointmentPOST(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    
    Попытка
        // 1. Читаем JSON
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(Запрос.ПолучитьТелоКакСтроку());
        ДанныеJSON = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // 2. Извлекаем данные
        ИмяПациента = ДанныеJSON["patient_name"];        // "Иванов Иван Иванович"
        ДатаРождения = ДанныеJSON["birth_date"];         // "21.05.1982"
        Возраст = ДанныеJSON["age"];                     // "42"
        РезультатыАнализов = ДанныеJSON["test_results"]; // Массив результатов
        
        // 3. Валидация
        Если ПустаяСтрока(ИмяПациента) Тогда
            ВызватьИсключение "Не указано ФИО пациента";
        КонецЕсли;
        
        Если ПустаяСтрока(ДатаРождения) Тогда
            ВызватьИсключение "Не указана дата рождения";
        КонецЕсли;
        
        // 4. ❗ ИЩЕМ пациента (с разбором ФИО на части!)
        Пациент = НайтиПациента(ИмяПациента, ДатаРождения);
        
        // 5. Проверка
        Если Пациент = Неопределено Тогда
            ВызватьИсключение 
                "Пациент """ + ИмяПациента + """ (дата рождения: " + ДатаРождения + 
                ") не найден в базе!";
        КонецЕсли;
        
        // ... остальной код (создание приёма, заполнение параметров и т.д.)
        // Без изменений из предыдущей версии
        
    Исключение
        Ответ.КодСостояния = 400;
        
        ОшибкаJSON = Новый Структура;
        ОшибкаJSON.Вставить("status", "error");
        ОшибкаJSON.Вставить("error", ОписаниеОшибки());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, ОшибкаJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции
```

---

## 🧪 Тестирование через curl (Windows CMD)

### Пример 1: Полное ФИО с отчеством

```cmd
curl -X POST "http://localhost/med/hs/lis/createAppointment" -H "Authorization: Bearer YOUR_TOKEN" -H "Content-Type: application/json" -d "{\"patient_name\":\"Иванов Иван Иванович\",\"birth_date\":\"21.05.1982\",\"age\":\"42\",\"test_results\":{\"ae584a2f-1234-5678-90ab-cdef12345678\":\"25.5\",\"d284601d-1234-5678-90ab-cdef87654321\":\"44.1\"}}"
```

### Пример 2: Без отчества

```cmd
curl -X POST "http://localhost/med/hs/lis/createAppointment" -H "Authorization: Bearer YOUR_TOKEN" -H "Content-Type: application/json" -d "{\"patient_name\":\"Петров Петр\",\"birth_date\":\"15.03.1990\",\"age\":\"35\",\"test_results\":{\"ae584a2f-1234-5678-90ab-cdef12345678\":\"28.2\"}}"
```

### Пример 3: С Basic Auth

```cmd
curl -X POST "http://localhost/med/hs/lis/createAppointment" -u "username:password" -H "Content-Type: application/json" -d "{\"patient_name\":\"Сидоров Сидор Сидорович\",\"birth_date\":\"10.12.1975\",\"age\":\"49\",\"test_results\":{\"ae584a2f-1234-5678-90ab-cdef12345678\":\"30.1\"}}"
```

---

## 📋 Checklist

- [ ] Скопирован новый код функции `НайтиПациента`
- [ ] Проверено, что в справочнике Клиенты есть поля:
  - `Фамилия`
  - `Имя`
  - `Отчество`
  - `ДатаРождения`
- [ ] Протестирован разбор ФИО в консоли 1С
- [ ] Протестирован поиск с реальными данными
- [ ] Опубликован HTTP-сервис
- [ ] Проверены логи в журнале регистрации
- [ ] Протестирован через curl (см. примеры выше)

---

## 🎯 Преимущества нового подхода

| Что | Старый подход | Новый подход |
|-----|--------------|--------------|
| **Поиск** | По `Наименование` | По `Фамилия` + `Имя` + `Отчество` |
| **Работает в конфигурации** | ❌ Нет | ✅ Да |
| **Точность** | - | ✅ Высокая |
| **Дата рождения** | ✅ Да | ✅ Да |
| **Логирование** | - | ✅ Подробное |

---

**Версия:** 2.1  
**Дата:** 21.11.2025  
**Статус:** ✅ Исправлено для конфигурации с раздельными полями ФИО

