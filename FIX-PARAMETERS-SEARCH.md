# 🔧 ИСПРАВЛЕНИЕ: Поиск параметров по UUID

## 🎯 Проблема

Функция `НайтиХарактеристикуПоКоду` ищет параметры **ТОЛЬКО** по полю `.Код`, но:
- В HTML шаблоне используются **UUID** в атрибутах `id="..."`
- В ПВХ `ПараметрыHTML` нет отдельного поля для хранения UUID
- Нужно искать по **УникальномуИдентификатору** самого элемента ПВХ!

## ✅ Решение

Функция должна поддерживать **ОБА варианта**:
1. Сначала искать по `.Код` (для внешних систем, которые знают коды)
2. Если не нашли → искать по `УникальномуИдентификатору` элемента

---

## 📝 Замени функцию НайтиХарактеристикуПоКоду

### ❌ СТАРАЯ ВЕРСИЯ (только .Код):

```1c
Функция НайтиХарактеристикуПоКоду(UUID)
    
    // Ищем характеристику в плане видов характеристик ПараметрыHTML
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    ПараметрыHTML.Ссылка КАК Ссылка
    |ИЗ
    |    ПланВидовХарактеристик.ПараметрыHTML КАК ПараметрыHTML
    |ГДЕ
    |    ПараметрыHTML.Код = &Код";
    
    Запрос.УстановитьПараметр("Код", UUID);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции
```

---

### ✅ НОВАЯ ВЕРСИЯ (гибридная: .Код + UUID):

**Скопируй эту функцию ПОЛНОСТЬЮ:**

```1c
Функция НайтиХарактеристикуПоКоду(КодПараметра)
    
    // Ищем характеристику в плане видов характеристик ПараметрыHTML по полю .Код
    // КодПараметра может быть:
    //   - Числовым кодом (например, "000037900") - это предпочтительный вариант!
    //   - UUID элемента (например, "ae584a2f-...") - для обратной совместимости
    
    // Сначала пытаемся найти по .Код (основной способ)
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    ПараметрыHTML.Ссылка КАК Ссылка
    |ИЗ
    |    ПланВидовХарактеристик.ПараметрыHTML КАК ПараметрыHTML
    |ГДЕ
    |    ПараметрыHTML.Код = &Код";
    
    Запрос.УстановитьПараметр("Код", КодПараметра);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    КонецЕсли;
    
    // Если не нашли по .Код, пробуем найти по УникальномуИдентификатору
    // (для обратной совместимости, если в HTML используются UUID)
    Попытка
        СсылкаПоUUID = ПланыВидовХарактеристик.ПараметрыHTML.ПолучитьСсылку(
            Новый УникальныйИдентификатор(КодПараметра));
        
        // Проверяем, существует ли элемент
        Если СсылкаПоUUID.Пустая() = Ложь Тогда
            Возврат СсылкаПоUUID;
        КонецЕсли;
    Исключение
        // Если не удалось преобразовать в UUID - не страшно
    КонецПопытки;
    
    Возврат Неопределено;
    
КонецФункции
```

---

## 🔑 Ключевые моменты

### 1. `ПолучитьСсылку()` - магия 1С!

Каждый элемент в ПВХ `ПараметрыHTML` имеет:
- `.Код` - числовой код (например, `"000037900"`)
- `.УникальныйИдентификатор` - UUID элемента (например, `"ae584a2f-957f-11f0-a7be-eca0f0014d7d"`)

Метод `ПолучитьСсылку()` позволяет получить ссылку на элемент по UUID:

```1c
СсылкаПоUUID = ПланыВидовХарактеристик.ПараметрыHTML.ПолучитьСсылку(
    Новый УникальныйИдентификатор("ae584a2f-957f-11f0-a7be-eca0f0014d7d")
);
```

### 2. Проверка существования

Даже если UUID правильный, элемент может не существовать:

```1c
Если СсылкаПоUUID.Пустая() = Ложь Тогда
    // Элемент существует!
    Возврат СсылкаПоUUID;
КонецЕсли;
```

### 3. Обработка ошибок

Если строка не является валидным UUID, будет ошибка:

```1c
Попытка
    Новый УникальныйИдентификатор("не-uuid");
Исключение
    // Ловим ошибку и не останавливаем выполнение
КонецПопытки;
```

---

## 🧪 Как это будет работать

### Вариант 1: UUID в HTML (текущая ситуация)

**HTML шаблон:**
```html
<p>АЛАТ: <a id="ae584a2f-957f-11f0-a7be-eca0f0014d7d">???</a> ед/л</p>
<p>АСАТ: <a id="d284601d-957f-11f0-a7be-eca0f0014d7d">???</a> ед/л</p>
```

**JSON от ЛИС:**
```json
{
  "test_results": [
    {"parameter_id": "ae584a2f-957f-11f0-a7be-eca0f0014d7d", "value": "25.5"},
    {"parameter_id": "d284601d-957f-11f0-a7be-eca0f0014d7d", "value": "44.1"}
  ]
}
```

**Что делает функция:**
1. Пытается найти по `.Код` = `"ae584a2f-..."` → не находит (это не код!)
2. Пытается найти по `УникальномуИдентификатору` → **находит!** ✅
3. Возвращает ссылку на элемент ПВХ

---

### Вариант 2: Коды в HTML (рекомендуется для будущего)

**HTML шаблон:**
```html
<p>АЛАТ: <a id="000037900">???</a> ед/л</p>
<p>АСАТ: <a id="000037901">???</a> ед/л</p>
```

**JSON от ЛИС:**
```json
{
  "test_results": [
    {"parameter_id": "000037900", "value": "25.5"},
    {"parameter_id": "000037901", "value": "44.1"}
  ]
}
```

**Что делает функция:**
1. Пытается найти по `.Код` = `"000037900"` → **находит!** ✅
2. Возвращает ссылку (даже не пробует UUID)

---

## 📋 Пошаговая инструкция

### Шаг 1: Открой HTTP-сервис в 1С
- Конфигуратор → HTTP-сервисы → `LabIntegration`
- Найди метод `createAppointment`
- Открой код

### Шаг 2: Найди функцию НайтиХарактеристикуПоКоду
- Ctrl+F → "Функция НайтиХарактеристикуПоКоду"

### Шаг 3: Замени ВСЕЙ функцию
- Удали старую версию
- Вставь новую версию (см. выше)
- Сохрани (Ctrl+S)

### Шаг 4: Обнови конфигурацию БД
- Конфигурация → Обновить конфигурацию БД (Ctrl+F7)
- Дождись завершения

### Шаг 5: Протестируй!

**Тест 1: С UUID (должно работать сейчас)**
```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/createAppointment \
  -u "Администратор:1234" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Тестов Тест Тестович",
    "age": "35",
    "gender": "Мужской",
    "sample_date": "18.11.2025",
    "result_date": "18.11.2025",
    "test_results": [
      {"parameter_id": "ae584a2f-957f-11f0-a7be-eca0f0014d7d", "value": "25.5"}
    ]
  }'
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "appointment_ref": "...",
  "parameters_filled": 1  // <- Было 0, стало 1!
}
```

---

## 🚨 Что если НЕ работает?

### Проблема 1: "parameters_filled": 0

**Возможные причины:**
1. UUID из JSON **НЕ совпадает** с UUID элемента в ПВХ
2. Элемент с таким UUID не существует

**Как проверить:**
1. Открой `ПланВидовХарактеристик.ПараметрыHTML`
2. Найди элемент "АЛАТ"
3. Посмотри его UUID (в адресной строке или через свойства)
4. Сравни с UUID в HTML шаблоне

### Проблема 2: "Ошибка при выполнении метода"

**Возможные причины:**
1. Синтаксическая ошибка при копировании
2. Не обновлена конфигурация БД

**Решение:**
- Проверь синтаксис (Ctrl+Shift+F9)
- Обнови конфигурацию БД (Ctrl+F7)

---

## ✅ Итого

После замены функции система будет:
1. ✅ Искать параметры по `.Код` (если используются коды)
2. ✅ Искать параметры по `УникальномуИдентификатору` (если используются UUID)
3. ✅ Автоматически определять формат ID
4. ✅ Работать с ЛЮБЫМ форматом в HTML и JSON!

---

## 📞 Следующие шаги

1. Замени функцию в 1С
2. Протестируй через curl
3. Пришли результат:
   - Успешно создался документ?
   - Сколько параметров заполнилось?
   - Есть ли ошибки?

Если всё работает → можно переходить к автоматической обработке PDF! 🚀

