# 🔧 Настройка HTTP-сервиса в 1С - ТРЕБУЕТСЯ

## 📊 Текущий статус интеграции

### ✅ Что работает:
- OpenVPN туннель подключен
- Сервер 1С доступен (192.168.100.234)
- HTTP сервис отвечает на запросы
- Авторизация проходит (токен принимается)

### ❌ Что не работает:
- Обработка JSON данных
- Любые POST запросы возвращают **"Ошибка формата потока"**

---

## 🔍 Причина

HTTP-сервис в 1С не может обработать входящие JSON данные. Возможные причины:
1. HTTP-сервис не настроен полностью
2. Ошибка в коде обработки запросов
3. Функция `ПрочитатьJSONВЗначение()` не работает
4. Метод `attachResultPOST` не реализован

---

## 📋 Что нужно сделать в 1С

### 1️⃣ Проверить существование HTTP-сервиса

В конфигураторе 1С:
```
Меню: Конфигурация → Общие → HTTP-сервисы
```

Должен быть HTTP-сервис:
- **Имя**: `LabIntegration` (или любое другое)
- **URL шаблон**: `/lab`
- **Корневой URL**: `/hs/lab`

С методом:
- **Имя**: `attachResult`
- **HTTP метод**: `POST`
- **Функция-обработчик**: `attachResultPOST`

---

### 2️⃣ Создать простой тестовый метод

Для диагностики создайте **очень простой** метод без обработки JSON:

```1c
// HTTP-сервис: LabIntegration
// Метод: test
// URL: /lab/test

Функция testPOST(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
    
    Попытка
        // Просто возвращаем успешный ответ
        РезультатJSON = "{""status"": ""ok"", ""message"": ""HTTP service works!""}";
        Ответ.УстановитьТелоИзСтроки(РезультатJSON, "UTF-8");
        
    Исключение
        Ответ.КодСостояния = 500;
        ТекстОшибки = ОписаниеОшибки();
        Ответ.УстановитьТелоИзСтроки(
            "{""error"": """ + ТекстОшибки + """}",
            "UTF-8"
        );
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции
```

**Опубликуйте** изменения и протестируйте:

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/test \
  -H "Authorization: Bearer 36765afb2c220c3a22be90cc6e88035332a09f945f392a7ffe00307579867bda"
```

Ожидаемый результат:
```json
{"status": "ok", "message": "HTTP service works!"}
```

---

### 3️⃣ Добавить метод с обработкой JSON

После успешного теста выше, создайте метод с обработкой JSON:

```1c
Функция echoPostPOST(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
    
    Попытка
        // Читаем тело запроса
        ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
        
        // Парсим JSON
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(ТелоЗапроса);
        ДанныеЗапроса = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // Возвращаем полученные данные обратно
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, ДанныеЗапроса);
        РезультатJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(РезультатJSON, "UTF-8");
        
    Исключение
        Ответ.КодСостояния = 500;
        ТекстОшибки = ОписаниеОшибки();
        Ответ.УстановитьТелоИзСтроки(
            "{""error"": """ + ТекстОшибки + """}",
            "UTF-8"
        );
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции
```

Тест:
```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/echoPost \
  -H "Authorization: Bearer 36765afb2c220c3a22be90cc6e88035332a09f945f392a7ffe00307579867bda" \
  -H "Content-Type: application/json" \
  -d '{"test": "hello", "number": 123}'
```

Ожидается, что 1С вернёт те же данные обратно.

---

### 4️⃣ Реализовать полный метод attachResult

Если шаги выше прошли успешно, используйте полный код из:

```
/opt/lis-md/docs/1c-integration-FIXED.md
```

Этот файл содержит:
- ✅ Полный рабочий код метода `attachResultPOST`
- ✅ Все вспомогательные функции
- ✅ Обработку ошибок и валидацию
- ✅ Работу с файлами и базой данных

---

## 🧪 Тестовые запросы

### После настройки простого метода test:
```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/test \
  -H "Authorization: Bearer 36765afb2c220c3a22be90cc6e88035332a09f945f392a7ffe00307579867bda"
```

### После настройки метода echoPost:
```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/echoPost \
  -H "Authorization: Bearer 36765afb2c220c3a22be90cc6e88035332a09f945f392a7ffe00307579867bda" \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'
```

### После настройки полного метода attachResult:
```bash
cd /opt/lis-md

curl -X POST http://192.168.100.234/BITtest/hs/lab/attachResult \
  -H "Authorization: Bearer 36765afb2c220c3a22be90cc6e88035332a09f945f392a7ffe00307579867bda" \
  -H "Content-Type: application/json" \
  -d "{\"orderNo\":\"12345\",\"fileName\":\"12345.pdf\",\"fileBase64\":\"$(base64 -w 0 test-result.pdf)\",\"sendEmail\":false}"
```

---

## 🔍 Проверка логов 1С

Логи 1С обычно находятся:
- `C:\Program Files\1cv8\logs\`
- `C:\Users\USERNAME\AppData\Local\1C\1cv8\`

Найдите файлы с расширением `.log` и проверьте последние записи на наличие ошибок HTTP-сервиса.

---

## 📞 После настройки

После того как HTTP-сервис будет настроен и заработает:

1. Напишите результат тестового запроса
2. Я помогу с финальной настройкой интегратора
3. Протестируем полный цикл: файл → 1С → email

---

## 🎯 Итоговая цель

```
Файл в NAS → Watcher обнаруживает → Integrator отправляет в 1С → 
1С обрабатывает → Mailer отправляет пациенту → Файл в архив
```

✅ **Со стороны ЛИС-МД всё готово и ждёт!**

