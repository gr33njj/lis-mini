# 📋 СОЗДАНИЕ ПРИЕМА В 1С (ВЕРСИЯ 2.0 - С ПОИСКОМ ПО ФИО + ДАТА РОЖДЕНИЯ)

> ✅ **Обновлено: 21.11.2025**
> - ФИО и дата рождения извлекаются из **ИМЕНИ ФАЙЛА**
> - Формат файла: `Фамилия Имя Отчество ДД.ММ.ГГГГ.pdf`
> - Поиск пациента по уникальной комбинации: **ФИО + Дата рождения**
> - **НЕ создаём** пациентов автоматически - только поиск
> - Явная ошибка 400 если пациент не найден

---

## 🎯 Главные изменения в версии 2.0

### ✅ ЧТО НОВОГО:
- **Извлечение из имени файла**: `Иванов Иван Иванович 21.05.1982.pdf`
  - ФИО: "Иванов Иван Иванович"
  - Дата рождения: "21.05.1982"
  - Возраст: 42 (вычисляется автоматически)
  
- **Поиск по ФИО + дата рождения**: уникальная комбинация, нет дубликатов
  
- **НЕ создаём пациентов**: только поиск существующих в базе
  
- **Явные ошибки**: если пациент не найден → ошибка 400 с детальным описанием

### ❌ ЧТО УБРАНО:
- ~~Автоматическое создание пациентов~~ (приводило к дублям)
- ~~Поиск только по ФИО~~ (ненадёжно, опечатки = новый клиент)
- ~~Парсинг ФИО из PDF~~ (ненадёжно)

---

## 📋 Код для 1С

### Метод HTTP-сервиса: `createAppointmentPOST`

```1c
Функция createAppointmentPOST(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    
    Попытка
        // 1. Читаем JSON
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(Запрос.ПолучитьТелоКакСтроку());
        ДанныеJSON = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // 2. Извлекаем данные (из имени файла приходят: patient_name, birth_date, age)
        ИмяПациента = ДанныеJSON["patient_name"];        // "Иванов Иван Иванович"
        ДатаРождения = ДанныеJSON["birth_date"];         // "21.05.1982"
        Возраст = ДанныеJSON["age"];                     // "42"
        Пол = ДанныеJSON["gender"];                      // "Мужской" (из PDF, опционально)
        ДатаЗабора = ДанныеJSON["sample_date"];          // Из PDF
        ДатаРезультата = ДанныеJSON["result_date"];      // Из PDF
        РезультатыАнализов = ДанныеJSON["test_results"]; // Из PDF
        
        // 3. Валидация обязательных полей
        Если ПустаяСтрока(ИмяПациента) Тогда
            ВызватьИсключение "Не указано ФИО пациента";
        КонецЕсли;
        
        Если ПустаяСтрока(ДатаРождения) Тогда
            ВызватьИсключение "Не указана дата рождения";
        КонецЕсли;
        
        // 4. ❗ ИЩЕМ пациента (БЕЗ СОЗДАНИЯ!) по ФИО + Дата рождения
        Пациент = НайтиПациента(ИмяПациента, ДатаРождения);
        
        // 5. ❗ ПРОВЕРКА: Если пациент НЕ найден → возвращаем ошибку 400!
        Если Пациент = Неопределено Тогда
            ВызватьИсключение 
                "Пациент """ + ИмяПациента + """ (дата рождения: " + ДатаРождения + 
                ") не найден в базе! Проверьте ФИО и дату рождения или создайте клиента вручную.";
        КонецЕсли;
        
        // 6. Ищем врача "МД Lab"
        Врач = НайтиВрача("МД Lab");
        Если Врач = Неопределено Тогда
            Врач = НайтиВрача("Lab");
        КонецЕсли;
        
        // 7. Ищем шаблон
        Шаблон = НайтиШаблон("МД Биохимический анализ крови");
        
        // 8. Создаём документ "Прием"
        Прием = Документы.Прием.СоздатьДокумент();
        Прием.Дата = ТекущаяДата();
        Прием.Клиент = Пациент;
        Прием.Врач = Врач;
        
        // 9. Добавляем шаблон в табличную часть
        Если Шаблон <> Неопределено Тогда
            НоваяСтрокаШаблона = Прием.ШаблоныПриема.Добавить();
            НоваяСтрокаШаблона.ШаблонПриема = Шаблон;
        КонецЕсли;
        
        // 10. Записываем документ
        Прием.Записать(РежимЗаписиДокумента.Проведение);
        
        // 11. 🔑 КЛЮЧЕВОЙ МОМЕНТ: Заполняем регистр ЗначенияПараметровHTML напрямую!
        КоличествоЗаполненных = ЗаполнитьРегистрПараметров(Прием.Ссылка, РезультатыАнализов);
        
        // 12. Возвращаем успех
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("status", "ok");
        РезультатJSON.Вставить("appointment_ref", Строка(Прием.Ссылка));
        РезультатJSON.Вставить("appointment_number", Прием.Номер);
        РезультатJSON.Вставить("patient_name", ИмяПациента);
        РезультатJSON.Вставить("birth_date", ДатаРождения);
        РезультатJSON.Вставить("doctor", ?(Врач = Неопределено, "Не найден", Строка(Врач)));
        РезультатJSON.Вставить("template", ?(Шаблон = Неопределено, "Не найден", Строка(Шаблон)));
        РезультатJSON.Вставить("templates_count", Прием.ШаблоныПриема.Количество());
        РезультатJSON.Вставить("parameters_filled", КоличествоЗаполненных);
        РезультатJSON.Вставить("parameters_received", РезультатыАнализов.Количество());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
        
    Исключение
        // ❗ Возвращаем 400 Bad Request (не 500!)
        Ответ.КодСостояния = 400;
        
        ОшибкаJSON = Новый Структура;
        ОшибкаJSON.Вставить("status", "error");
        ОшибкаJSON.Вставить("error", ОписаниеОшибки());
        
        // Добавляем дополнительную информацию для отладки
        Попытка
            Если ДанныеJSON <> Неопределено Тогда
                ОшибкаJSON.Вставить("patient_name", ДанныеJSON["patient_name"]);
                ОшибкаJSON.Вставить("birth_date", ДанныеJSON["birth_date"]);
            КонецЕсли;
        Исключение
            // Игнорируем ошибки при добавлении доп. информации
        КонецПопытки;
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, ОшибкаJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции


// ═══════════════════════════════════════════════════════════════
// ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
// ═══════════════════════════════════════════════════════════════

Функция НайтиПациента(ИмяПолное, ДатаРождения)
    
    // ❗ ТОЛЬКО ПОИСК, БЕЗ СОЗДАНИЯ!
    // Поиск по уникальной комбинации: ФИО + Дата рождения
    
    Попытка
        // Конвертируем строку даты в формат даты 1С
        // Формат входной строки: "21.05.1982"
        ДатаРожденияДата = Неопределено;
        
        Если НЕ ПустаяСтрока(ДатаРождения) Тогда
            ЧастиДаты = СтрРазделить(ДатаРождения, ".", Ложь);
            Если ЧастиДаты.Количество() = 3 Тогда
                День = Число(ЧастиДаты[0]);
                Месяц = Число(ЧастиДаты[1]);
                Год = Число(ЧастиДаты[2]);
                ДатаРожденияДата = Дата(Год, Месяц, День);
            КонецЕсли;
        КонецЕсли;
        
        // Если дату не удалось распарсить - ищем только по ФИО
        Если ДатаРожденияДата = Неопределено Тогда
            // Поиск только по ФИО (менее надёжно!)
            Запрос = Новый Запрос;
            Запрос.Текст = 
            "ВЫБРАТЬ ПЕРВЫЕ 1
            |    Клиенты.Ссылка КАК Ссылка
            |ИЗ
            |    Справочник.Клиенты КАК Клиенты
            |ГДЕ
            |    Клиенты.Наименование = &Наименование
            |    И НЕ Клиенты.ПометкаУдаления";
            
            Запрос.УстановитьПараметр("Наименование", ИмяПолное);
        Иначе
            // Поиск по ФИО + Дата рождения (надёжно!)
            Запрос = Новый Запрос;
            Запрос.Текст = 
            "ВЫБРАТЬ ПЕРВЫЕ 1
            |    Клиенты.Ссылка КАК Ссылка
            |ИЗ
            |    Справочник.Клиенты КАК Клиенты
            |ГДЕ
            |    Клиенты.Наименование = &Наименование
            |    И Клиенты.ДатаРождения = &ДатаРождения
            |    И НЕ Клиенты.ПометкаУдаления";
            
            Запрос.УстановитьПараметр("Наименование", ИмяПолное);
            Запрос.УстановитьПараметр("ДатаРождения", ДатаРожденияДата);
        КонецЕсли;
        
        Результат = Запрос.Выполнить();
        
        Если НЕ Результат.Пустой() Тогда
            Выборка = Результат.Выбрать();
            Выборка.Следующий();
            Возврат Выборка.Ссылка;
        Иначе
            // ❗ НЕ создаём пациента, возвращаем Неопределено!
            Возврат Неопределено;
        КонецЕсли;
        
    Исключение
        // При любой ошибке возвращаем Неопределено
        Возврат Неопределено;
    КонецПопытки;
    
КонецФункции


Функция НайтиВрача(ИмяВрача)
    
    // Ищем врача в справочнике Сотрудники
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Сотрудники.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Сотрудники КАК Сотрудники
    |ГДЕ
    |    Сотрудники.Наименование ПОДОБНО &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", "%" + ИмяВрача + "%");
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция НайтиШаблон(НаименованиеШаблона)
    
    // Ищем шаблон в справочнике ШаблоныHTML
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Шаблоны.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.ШаблоныHTML КАК Шаблоны
    |ГДЕ
    |    Шаблоны.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", НаименованиеШаблона);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция ЗаполнитьРегистрПараметров(СсылкаДокумента, РезультатыАнализов)
    
    // 🔑 ПРАВИЛЬНЫЙ СПОСОБ: Прямая запись в регистр ЗначенияПараметровHTML
    // РезультатыАнализов приходит как Массив объектов:
    // [{"parameter_id": "ae584a2f-957f-11f0-a7be-eca0f0014d7d", "value": "25.5"}, ...]
    
    Счетчик = 0;
    
    Попытка
        // Создаём набор записей регистра
        Набор = РегистрыСведений.ЗначенияПараметровHTML.СоздатьНаборЗаписей();
        Набор.Отбор.Документ.Установить(СсылкаДокумента);
        
        // Читаем существующие записи (если есть)
        Набор.Прочитать();
        
        // Очищаем старые записи
        Набор.Очистить();
        
        // Заполняем новыми значениями
        Для Каждого ЭлементМассива Из РезультатыАнализов Цикл
            
            GUID = ЭлементМассива["parameter_id"];
            Значение = ЭлементМассива["value"];
            
            // Находим параметр по GUID
            Параметр = НайтиПараметрПоGUID(GUID);
            
            Если Параметр <> Неопределено Тогда
                // Добавляем запись в регистр
                НоваяЗапись = Набор.Добавить();
                НоваяЗапись.Документ = СсылкаДокумента;
                НоваяЗапись.ИндексШаблона = 0;  // Первый шаблон в табличной части
                НоваяЗапись.Параметр = Параметр;
                НоваяЗапись.Значение = Значение;
                
                Счетчик = Счетчик + 1;
            КонецЕсли;
            
        КонецЦикла;
        
        // Записываем весь набор
        Набор.Записать();
        
    Исключение
        // Если что-то пошло не так - возвращаем 0
        Счетчик = 0;
    КонецПопытки;
    
    Возврат Счетчик;
    
КонецФункции


Функция НайтиПараметрПоGUID(ИскомыйGUID)
    
    // 🔑 ПРАВИЛЬНЫЙ СПОСОБ: Поиск параметра по УникальномуИдентификатору
    // В HTML шаблоне используются GUID параметров: <a id="ae584a2f-...">
    // Этот GUID совпадает с УникальныйИдентификатор() элемента ПВХ
    
    Попытка
        // Приводим к нижнему регистру для сравнения
        ИскомыйGUIDНижний = НРег(ИскомыйGUID);
        
        // Запрашиваем все параметры
        Запрос = Новый Запрос;
        Запрос.Текст = 
        "ВЫБРАТЬ
        |    ПараметрыHTML.Ссылка КАК Ссылка
        |ИЗ
        |    ПланВидовХарактеристик.ПараметрыHTML КАК ПараметрыHTML";
        
        Результат = Запрос.Выполнить();
        Выборка = Результат.Выбрать();
        
        // Перебираем все параметры и сравниваем GUID
        Пока Выборка.Следующий() Цикл
            // Получаем GUID элемента
            GUIDЭлемента = НРег(Строка(Выборка.Ссылка.УникальныйИдентификатор()));
            
            // Сравниваем с искомым GUID
            Если GUIDЭлемента = ИскомыйGUIDНижний Тогда
                Возврат Выборка.Ссылка;
            КонецЕсли;
        КонецЦикла;
        
    Исключение
        // Если что-то пошло не так - возвращаем Неопределено
    КонецПопытки;
    
    Возврат Неопределено;
    
КонецФункции
```

---

## 🧪 Тестирование

### Тест 1: Пациент существует в базе

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/createAppointment \
  -u "Администратор:1234" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Иванов Иван Иванович",
    "birth_date": "21.05.1982",
    "age": "42",
    "gender": "Мужской",
    "sample_date": "20.11.2025",
    "result_date": "21.11.2025",
    "test_results": [
      {"parameter_id": "ae584a2f-957f-11f0-a7be-eca0f0014d7d", "value": "25.5"},
      {"parameter_id": "d284601d-957f-11f0-a7be-eca0f0014d7d", "value": "44.1"},
      {"parameter_id": "8bf99265-9580-11f0-a7be-eca0f0014d7d", "value": "5.2"}
    ]
  }'
```

**Ожидаемый ответ (успех):**
```json
{
  "status": "ok",
  "appointment_ref": "Документ.Прием.000000001",
  "appointment_number": "00000001",
  "patient_name": "Иванов Иван Иванович",
  "birth_date": "21.05.1982",
  "doctor": "МД Lab",
  "template": "МД Биохимический анализ крови",
  "templates_count": 1,
  "parameters_filled": 3,
  "parameters_received": 3
}
```

---

### Тест 2: Пациент НЕ существует в базе

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/createAppointment \
  -u "Администратор:1234" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Несуществующий Человек",
    "birth_date": "01.01.1900",
    "age": "124",
    "gender": "Мужской",
    "sample_date": "20.11.2025",
    "result_date": "21.11.2025",
    "test_results": []
  }'
```

**Ожидаемый ответ (ошибка 400):**
```json
{
  "status": "error",
  "error": "Пациент \"Несуществующий Человек\" (дата рождения: 01.01.1900) не найден в базе! Проверьте ФИО и дату рождения или создайте клиента вручную.",
  "patient_name": "Несуществующий Человек",
  "birth_date": "01.01.1900"
}
```

---

## 📊 Формат данных

### Входной JSON (от ЛИС МД):

```json
{
  "patient_name": "Иванов Иван Иванович",    // Из ИМЕНИ ФАЙЛА
  "birth_date": "21.05.1982",                // Из ИМЕНИ ФАЙЛА
  "age": "42",                                // Вычислено автоматически
  "gender": "Мужской",                        // Из PDF (опционально)
  "sample_date": "20.11.2025",                // Из PDF
  "result_date": "21.11.2025",                // Из PDF
  "test_results": [                           // Из PDF
    {
      "parameter_id": "ae584a2f-957f-11f0-a7be-eca0f0014d7d",
      "value": "25.5"
    },
    {
      "parameter_id": "d284601d-957f-11f0-a7be-eca0f0014d7d",
      "value": "44.1"
    }
  ]
}
```

### Формат имени файла (на NAS):

```
\\192.168.100.177\laba\Иванов Иван Иванович 21.05.1982.pdf
                      │                      │
                      │                      └─ Дата рождения
                      └─ ФИО пациента
```

---

## ✅ Преимущества версии 2.0

| Характеристика | Версия 1.0 | Версия 2.0 |
|----------------|------------|------------|
| **Источник ФИО** | Парсинг из PDF | **Имя файла** |
| **Надёжность ФИО** | Низкая (ошибки парсинга) | **Высокая** |
| **Поиск пациента** | Только по ФИО | **ФИО + дата рождения** |
| **Дубликаты** | Да (опечатки) | **Нет** |
| **Автосоздание** | Да (проблема!) | **Нет** |
| **Ошибки** | Тихие сбои | **Явные (400)** |

---

## 🚨 Что делать при ошибке "Пациент не найден"

### Шаг 1: Проверить имя файла

**Правильный формат:**
```
Иванов Иван Иванович 21.05.1982.pdf
```

**Неправильные форматы:**
- ❌ `123456.pdf` (нет ФИО и даты)
- ❌ `Иванов И.И. 21.05.1982.pdf` (сокращённое ФИО)
- ❌ `Иванов Иван Иванович 1982.pdf` (неверный формат даты)

### Шаг 2: Проверить в 1С

1. Открыть: Справочники → Клиенты
2. Найти по фамилии: "Иванов"
3. Проверить:
   - ✅ Наименование: `Иванов Иван Иванович` (точное совпадение!)
   - ✅ ДатаРождения: `21.05.1982` (точное совпадение!)

### Шаг 3: Исправить несоответствие

**Вариант А**: Переименовать файл (если ошибка в файле)

**Вариант Б**: Исправить данные в 1С (если ошибка в базе)

### Шаг 4: Повторить обработку

1. Открыть: https://lis.it-mydoc.ru
2. Найти файл в карантине
3. Нажать "Повторить обработку"

---

## 📋 Checklist для внедрения

- [ ] Скопирован код в HTTP-сервис 1С
- [ ] Обновлена конфигурация БД (Ctrl+F7)
- [ ] HTTP-сервис опубликован
- [ ] Тест с существующим пациентом пройден
- [ ] Тест с несуществующим пациентом показал ошибку 400
- [ ] Проверен регистр `ЗначенияПараметровHTML`
- [ ] Проверена печатная форма
- [ ] Обновлён Python код (`app/pdf_parser.py`)
- [ ] Настроено именование файлов

---

## 📞 Поддержка

При проблемах проверьте:

1. ✅ Формат имени файла: `Фамилия Имя Отчество ДД.ММ.ГГГГ.pdf`
2. ✅ Пациент есть в 1С с точным ФИО и датой рождения
3. ✅ HTTP-сервис опубликован
4. ✅ Логи в журнале регистрации 1С
5. ✅ Логи ЛИС МД: `docker compose logs -f app`

---

**Версия:** 2.0  
**Дата:** 21.11.2025  
**Статус:** ✅ Готово к продакшн  
**GitHub:** https://github.com/gr33njj/lis-mini

