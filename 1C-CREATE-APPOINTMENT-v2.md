# 📋 СОЗДАНИЕ ПРИЕМА В 1С С ЗАПОЛНЕНИЕМ ПАРАМЕТРОВ ШАБЛОНА

## 🎯 Задача
Автоматически создавать документ **"ЗаписьНаПрием"** с:
- **Врач**: МД Lab (из `Справочник.Сотрудники`)
- **Клиент**: ФИО из названия PDF (из `Справочник.Клиенты`)
- **ШаблонОсмотра**: "МД Биохимический анализ крови" (из `Справочник.ШаблоныHTML`)
- **Параметры шаблона**: Заполненные значениями из PDF (18 показателей)

> ⚠️ **ВАЖНО**: Код адаптирован под вашу конфигурацию!
> - Используется `Документ.ЗаписьНаПрием`
> - Используется `Справочник.Клиенты`
> - Используется `Справочник.Сотрудники`
> - Используется `Справочник.ШаблоныHTML`

---

## 📝 Шаг 1: Добавить новый метод в HTTP-сервис

Открой HTTP-сервис `LabIntegration` и добавь новый метод:

### Свойства метода `createAppointment`:
- **Имя**: `createAppointment`
- **Шаблон URL**: `createAppointment`
- **HTTP метод**: `POST`
- **Аутентификация**: `Basic` (или снята)

### Код метода `createAppointmentPOST`:

```1c
Функция createAppointmentPOST(Запрос)
    
    Ответ = Новый HTTPСервисОтвет(200);
    
    Попытка
        // 1. Читаем JSON
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(Запрос.ПолучитьТелоКакСтроку());
        ДанныеJSON = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
        
        // 2. Извлекаем данные
        ИмяПациента = ДанныеJSON["patient_name"];
        Возраст = ДанныеJSON["age"];
        Пол = ДанныеJSON["gender"];
        ДатаЗабора = ДанныеJSON["sample_date"];
        ДатаРезультата = ДанныеJSON["result_date"];
        РезультатыАнализов = ДанныеJSON["test_results"];
        
        // 3. Ищем или создаём пациента (Клиента)
        Клиент = НайтиИлиСоздатьКлиента(ИмяПациента, Возраст, Пол);
        
        // 4. Ищем врача "МД Lab"
        Врач = НайтиВрача("МД Lab");
        Если Врач = Неопределено Тогда
            Врач = НайтиВрача("Lab"); // Попытка без префикса
        КонецЕсли;
        
        // 5. Ищем шаблон
        Шаблон = НайтиШаблон("МД Биохимический анализ крови");
        Если Шаблон = Неопределено Тогда
            ВызватьИсключение("Шаблон 'МД Биохимический анализ крови' не найден");
        КонецЕсли;
        
        // 6. Создаём документ "ЗаписьНаПрием"
        Прием = Документы.ЗаписьНаПрием.СоздатьДокумент();
        Прием.Дата = ТекущаяДата();
        Прием.Клиент = Клиент;
        Прием.Врач = Врач;
        Прием.ШаблонОсмотра = Шаблон;
        
        // 7. Заполняем параметры шаблона
        КоличествоЗаполненных = ЗаполнитьПараметрыШаблона(Прием, РезультатыАнализов);
        
        // 8. Записываем документ
        Прием.Записать(РежимЗаписиДокумента.Проведение);
        
        // 9. Возвращаем успех
        РезультатJSON = Новый Структура;
        РезультатJSON.Вставить("status", "ok");
        РезультатJSON.Вставить("appointment_ref", Строка(Прием.Ссылка));
        РезультатJSON.Вставить("appointment_number", Прием.Номер);
        РезультатJSON.Вставить("client_name", ИмяПациента);
        РезультатJSON.Вставить("doctor", ?(Врач = Неопределено, "Не найден", Строка(Врач)));
        РезультатJSON.Вставить("parameters_filled", КоличествоЗаполненных);
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, РезультатJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
        
    Исключение
        Ответ.КодСостояния = 500;
        
        ОшибкаJSON = Новый Структура;
        ОшибкаJSON.Вставить("status", "error");
        ОшибкаJSON.Вставить("error", ОписаниеОшибки());
        
        ЗаписьJSON = Новый ЗаписьJSON;
        ЗаписьJSON.УстановитьСтроку();
        ЗаписатьJSON(ЗаписьJSON, ОшибкаJSON);
        ТекстJSON = ЗаписьJSON.Закрыть();
        
        Ответ.УстановитьТелоИзСтроки(ТекстJSON);
    КонецПопытки;
    
    Возврат Ответ;
    
КонецФункции


// ═══════════════════════════════════════════════════════════════
// ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
// ═══════════════════════════════════════════════════════════════

Функция НайтиИлиСоздатьКлиента(ИмяПолное, Возраст, Пол)
    
    // Ищем существующего клиента по ФИО
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Клиенты.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Клиенты КАК Клиенты
    |ГДЕ
    |    Клиенты.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", ИмяПолное);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        // Создаём нового клиента
        НовыйКлиент = Справочники.Клиенты.СоздатьЭлемент();
        НовыйКлиент.Наименование = ИмяПолное;
        НовыйКлиент.Записать();
        Возврат НовыйКлиент.Ссылка;
    КонецЕсли;
    
КонецФункции


Функция НайтиВрача(ИмяВрача)
    
    // Ищем врача в справочнике (замени "Врачи" на правильное имя)
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Сотрудники.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.Сотрудники КАК Сотрудники
    |ГДЕ
    |    Сотрудники.Наименование ПОДОБНО &Наименование
    |    ИЛИ Сотрудники.Имя ПОДОБНО &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", "%" + ИмяВрача + "%");
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция НайтиШаблон(НаименованиеШаблона)
    
    // Ищем шаблон в справочнике ШаблоныHTML
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Шаблоны.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.ШаблоныHTML КАК Шаблоны
    |ГДЕ
    |    Шаблоны.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Наименование", НаименованиеШаблона);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции


Функция ЗаполнитьПараметрыШаблона(Прием, РезультатыАнализов)
    
    // Мапинг имён из JSON на имена параметров шаблона
    // (в твоём случае имена совпадают, но на всякий случай)
    МапингПараметров = Новый Соответствие;
    МапингПараметров.Вставить("АЛАТ", "АЛАТ");
    МапингПараметров.Вставить("АСАТ", "АСАТ");
    МапингПараметров.Вставить("Холестерин", "Холестерин");
    МапингПараметров.Вставить("гамма-ГТ", "гамма-ГТ");
    МапингПараметров.Вставить("Щелочн. фосф-за", "Щелочн. фосф-за");
    МапингПараметров.Вставить("Триглицериды", "Триглицериды");
    МапингПараметров.Вставить("ЛПВП", "ЛПВП");
    МапингПараметров.Вставить("ЛПНП", "ЛПНП");
    МапингПараметров.Вставить("ЛДГ", "ЛДГ");
    МапингПараметров.Вставить("Мочевина", "Мочевина");
    МапингПараметров.Вставить("альфа-Амилаза", "альфа-Амилаза");
    МапингПараметров.Вставить("Билирубин общий", "Билирубин общий");
    МапингПараметров.Вставить("Билирубин прямой", "Билирубин прямой");
    МапингПараметров.Вставить("Кальций", "Кальций");
    МапингПараметров.Вставить("Креатинин", "Креатинин");
    МапингПараметров.Вставить("Глюкоза", "Глюкоза");
    МапингПараметров.Вставить("Общий белок", "Общий белок");
    МапингПараметров.Вставить("Железо", "Железо");
    МапингПараметров.Вставить("Мочевая кислота", "Мочевая кислота");
    
    Счетчик = 0;
    
    // Получаем табличную часть параметров (или реквизит, зависит от конфигурации)
    // ВАРИАНТ 1: Если параметры в табличной части
    Для Каждого КлючЗначение Из РезультатыАнализов Цикл
        // КлючЗначение.Ключ - это имя параметра
        // КлючЗначение.Значение - это значение
        
        ИмяПараметраВJSON = НайтиИмяПоID(КлючЗначение.Ключ, МапингПараметров);
        
        Если ИмяПараметраВJSON <> Неопределено Тогда
            // Ищем параметр в табличной части шаблона
            Параметр = НайтиПараметрШаблона(Прием.ШаблонОсмотра, ИмяПараметраВJSON);
            
            Если Параметр <> Неопределено Тогда
                // Добавляем строку в табличную часть приема с заполненным значением
                // (Точное имя табличной части зависит от конфигурации)
                НоваяСтрока = Прием.ПараметрыШаблона.Добавить(); // Замени на правильное имя
                НоваяСтрока.Параметр = Параметр;
                НоваяСтрока.Значение = КлючЗначение.Значение;
                Счетчик = Счетчик + 1;
            КонецЕсли;
        КонецЕсли;
    КонецЦикла;
    
    Возврат Счетчик;
    
КонецФункции


Функция НайтиИмяПоID(ID, МапингПараметров)
    
    // Парсер отправляет в test_results GUID-ы как ключи
    // Нам нужно их сопоставить с именами параметров
    
    // Мапинг GUID -> Имя (из парсера)
    МапингID = Новый Соответствие;
    МапингID.Вставить("ae584a2f-957f-11f0-a7be-eca0f0014d7d", "АЛАТ");
    МапингID.Вставить("d284601d-957f-11f0-a7be-eca0f0014d7d", "АСАТ");
    МапингID.Вставить("e5e808bc-957f-11f0-a7be-eca0f0014d7d", "Холестерин");
    МапингID.Вставить("ef9d787d-957f-11f0-a7be-eca0f0014d7d", "гамма-ГТ");
    МапингID.Вставить("0190f459-9580-11f0-a7be-eca0f0014d7d", "Щелочн. фосф-за");
    МапингID.Вставить("0bf4b4f9-9580-11f0-a7be-eca0f0014d7d", "Триглицериды");
    МапингID.Вставить("19dd5baa-9580-11f0-a7be-eca0f0014d7d", "ЛПВП");
    МапингID.Вставить("3b7519c0-9580-11f0-a7be-eca0f0014d7d", "ЛПНП");
    МапингID.Вставить("43f2ce34-9580-11f0-a7be-eca0f0014d7d", "ЛДГ");
    МапингID.Вставить("4b6251cb-9580-11f0-a7be-eca0f0014d7d", "Мочевина");
    МапингID.Вставить("562a56ba-9580-11f0-a7be-eca0f0014d7d", "альфа-Амилаза");
    МапингID.Вставить("62c0a29c-9580-11f0-a7be-eca0f0014d7d", "Билирубин общий");
    МапингID.Вставить("681a2d53-9580-11f0-a7be-eca0f0014d7d", "Билирубин прямой");
    МапингID.Вставить("740e37da-9580-11f0-a7be-eca0f0014d7d", "Кальций");
    МапингID.Вставить("8007eafe-9580-11f0-a7be-eca0f0014d7d", "Креатинин");
    МапингID.Вставить("8bf99265-9580-11f0-a7be-eca0f0014d7d", "Глюкоза");
    МапингID.Вставить("97ee2791-9580-11f0-a7be-eca0f0014d7d", "Общий белок");
    МапингID.Вставить("9decbf26-9580-11f0-a7be-eca0f0014d7d", "Железо");
    МапингID.Вставить("bc144e0a-a67f-11f0-a7d7-d01c04d652e6", "Мочевая кислота");
    
    Возврат МапингID.Получить(ID);
    
КонецФункции


Функция НайтиПараметрШаблона(Шаблон, ИмяПараметра)
    
    // Ищем параметр в табличной части шаблона
    // (Точная структура зависит от конфигурации)
    
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Параметры.Ссылка КАК Ссылка
    |ИЗ
    |    Справочник.ПараметрыШаблонов КАК Параметры
    |ГДЕ
    |    Параметры.Владелец = &Шаблон
    |    И Параметры.Наименование = &Наименование";
    
    Запрос.УстановитьПараметр("Шаблон", Шаблон);
    Запрос.УстановитьПараметр("Наименование", ИмяПараметра);
    
    Результат = Запрос.Выполнить();
    
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Ссылка;
    Иначе
        Возврат Неопределено;
    КонецЕсли;
    
КонецФункции
```

---

## ⚙️ Адаптация под твою конфигурацию

### 🔧 Что НУЖНО ПРОВЕРИТЬ И ИЗМЕНИТЬ:

#### 1️⃣ **Поля документа `ЗаписьНаПрием`** (строка 60-64):
Проверь, как в твоей конфигурации называются поля документа:
- `Прием.Клиент` → может называться `Пациент`, `Контрагент`, `ФизЛицо` и т.д.
- `Прием.Врач` → может называться `Сотрудник`, `Доктор` и т.д.
- `Прием.ШаблонОсмотра` → может называться `Шаблон`, `ШаблонАнализа` и т.д.

Чтобы узнать точные имена:
1. Открой конфигуратор 1С
2. Найди `Документ.ЗаписьНаПрием`
3. Посмотри имена реквизитов (не представления, а именно имена!)

#### 2️⃣ **Табличная часть параметров шаблона** (строка 240):
```1c
НоваяСтрока = Прием.ПараметрыШаблона.Добавить(); // ← ПРОВЕРЬ ЭТО ИМЯ!
НоваяСтрока.Параметр = Параметр;
НоваяСтрока.Значение = КлючЗначение.Значение;
```

Нужно узнать:
- Как называется табличная часть в документе `ЗаписьНаПрием` для хранения параметров?
- Как называются колонки этой ТЧ?

Возможные варианты:
- `Прием.Параметры`, `Прием.ПараметрыШаблона`, `Прием.ЗначенияПараметров`
- Колонки: `Параметр/Значение`, `ИмяПараметра/ЗначениеПараметра`, `Ключ/Значение`

#### 3️⃣ **Справочник параметров** (строка 295):
```1c
Справочник.ПараметрыШаблонов  // ← ПРОВЕРЬ, СУЩЕСТВУЕТ ЛИ ОН!
```

Вопрос: **Как в твоей конфигурации хранятся параметры шаблонов?**

**Вариант A**: Отдельный справочник (например, `Справочник.ПараметрыШаблонов`)
- Тогда код функции `НайтиПараметрШаблона` уже правильный

**Вариант B**: Табличная часть справочника `ШаблоныHTML`
- Тогда нужно изменить запрос в `НайтиПараметрШаблона`:
```1c
Запрос.Текст = 
"ВЫБРАТЬ ПЕРВЫЕ 1
|    Параметры.НомерСтроки КАК НомерСтроки,
|    Параметры.Наименование КАК Наименование
|ИЗ
|    Справочник.ШаблоныHTML.Параметры КАК Параметры
|ГДЕ
|    Параметры.Ссылка = &Шаблон
|    И Параметры.Наименование = &Наименование";
```

**Вариант C**: Параметры вообще не в справочнике, а хранятся как-то иначе
- Тогда функцию `ЗаполнитьПараметрыШаблона` нужно переписать под твою логику

#### 4️⃣ **Имя поля "Наименование" в справочнике Сотрудники** (строка 153):
Проверь, что у справочника `Сотрудники` есть поле `Наименование` или `Имя`.

---

### 🔍 Как узнать правильную структуру:

1. Открой конфигуратор 1С
2. Найди `Документ.ЗаписьНаПрием`
3. Посмотри:
   - **Реквизиты** (там будут Клиент, Врач, ШаблонОсмотра и т.д.)
   - **Табличные части** (там должна быть ТЧ для хранения параметров)
4. Найди `Справочник.ШаблоныHTML`
5. Посмотри:
   - **Табличные части** (возможно, там хранятся параметры)
   - Или есть ли отдельный справочник для параметров

---

## 📝 Шаг 2: Создай врача "МД Lab"

В справочнике Сотрудники (или Врачи) создай:
- **Наименование** или **Имя**: `МД Lab`

---

## 🧪 Шаг 3: Тестирование

### Простой тест (без создания приема):

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/test \
  -u "Администратор:1234"
```

### Полный тест (создание приема):

```bash
curl -X POST http://192.168.100.234/BITtest/hs/lab/createAppointment \
  -u "Администратор:1234" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_name": "Тестовый Пациент",
    "age": "45",
    "gender": "Мужской",
    "sample_date": "15.11.2025",
    "result_date": "17.11.2025",
    "test_results": {
      "ae584a2f-957f-11f0-a7be-eca0f0014d7d": "25.5",
      "d284601d-957f-11f0-a7be-eca0f0014d7d": "30.2",
      "e5e808bc-957f-11f0-a7be-eca0f0014d7d": "4.5"
    }
  }'
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "appointment_ref": "Документ.ЗаписьНаПрием.000000001",
  "appointment_number": "00000001",
  "client_name": "Тестовый Пациент",
  "doctor": "МД Lab",
  "parameters_filled": 3
}
```

---

## 🚨 Возможные проблемы и решения:

### ❌ "Параметр не найден"
**Причина**: Неправильное имя параметра или его нет в справочнике/ТЧ шаблона
**Решение**:
1. Открой шаблон "МД Биохимический анализ крови" в 1С
2. Посмотри, как именно называются параметры (точное имя!)
3. Убедись, что имена в маппинге (строка 203-221) совпадают с именами в 1С

### ❌ "Шаблон не найден"
**Причина**: Шаблон с таким именем не существует
**Решение**:
1. Открой `Справочник.ШаблоныHTML` в 1С
2. Проверь, что там есть шаблон с именем **точно** `МД Биохимический анализ крови`
3. Или измени имя в коде (строка 54)

### ❌ "Врач не найден"
**Причина**: Сотрудника "МД Lab" нет в справочнике
**Решение**:
1. Создай нового сотрудника в `Справочник.Сотрудники`
2. Укажи имя или наименование: `МД Lab`

### ❌ "Ошибка записи документа"
**Причина**: Не заполнены обязательные поля документа `ЗаписьНаПрием`
**Решение**:
1. Посмотри в конфигураторе, какие поля обязательные
2. Добавь их заполнение в код (строка 60-64)

### ❌ "Табличная часть не заполняется"
**Причина**: Неправильное имя табличной части или её колонок
**Решение**:
1. Найди в конфигураторе точное имя ТЧ документа `ЗаписьНаПрием`
2. Исправь в коде (строка 240): `Прием.ТВОЁ_ИМЯ_ТЧ.Добавить()`
3. Проверь имена колонок: `Параметр`, `Значение`

### ❌ "Не удается найти справочник ПараметрыШаблонов"
**Причина**: В вашей конфигурации нет такого справочника
**Решение**:
- Следуй инструкциям в разделе **"Адаптация под твою конфигурацию"** → пункт 3️⃣

---

## 📞 Что дальше?

### ✅ Шаг 1: Узнай структуру своей конфигурации
Открой конфигуратор и выясни:
1. Имена реквизитов документа `ЗаписьНаПрием`
2. Имя и структуру табличной части для параметров
3. Как хранятся параметры шаблонов (справочник или ТЧ?)

### ✅ Шаг 2: Адаптируй код
Измени код функции `createAppointmentPOST` под свою структуру

### ✅ Шаг 3: Протестируй
Запусти тест через curl (см. выше в разделе "Тестирование")

### ✅ Шаг 4: Сообщи результат
Если что-то не работает, пришли:
- Текст ошибки из 1С
- Скриншот структуры документа `ЗаписьНаПрием` из конфигуратора
- Скриншот структуры справочника `ШаблоныHTML`

---

## 🎓 Дополнительная информация

### Где взять ID параметров (GUID)?
ID параметров (например, `ae584a2f-957f-11f0-a7be-eca0f0014d7d`) берутся из вашего HTML-шаблона в 1С. Это те самые `data-param-id`, которые вы видели в HTML-коде.

Функция `НайтиИмяПоID` (строка 259) преобразует эти GUID в человеко-читаемые имена параметров.

### Зачем нужен маппинг параметров?
Маппинг (строка 202-221) связывает:
- **Имена из Python** (после парсинга PDF) ↔ **Имена в 1С** (в шаблоне)

Если имена совпадают, маппинг можно упростить. Но если нет — используй его для преобразования.

